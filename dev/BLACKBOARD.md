# BLACKBOARD.md — Distributed Factory Execution Record System

*Single writer per section. Discipline, not code-enforced. See `dev/sdd-kit-2/AGENTS.md` § The BLACKBOARD protocol. This project runs under sdd-kit-2. The contract registries (`contracts/*.yaml`) are this project's locked vocabulary — the equivalent of `signals/0.1.json` — and registry validation is the schema-at-the-speakers-mouth poka-yoke.*

---

## Surfaced for review

- **2026-08-28 Claude Code — Phase G opens.** COMPREHENSION_AFFIRMATION (AGENTS.md hard rule 5, sdd-kit-2; re-articulation on a major scope change). Phase G implements the Physical Presence UI overlay against `specs/physical-presence-ui-overlay/ui-overlay-spec-v0.9.md` — the shipping baseline after five review passes on the incoming v0.4 spec (v0.5 first grounding → v0.6 candidate → v0.7 second grounding → v0.8 candidate → v0.9 baseline). The phase adds no product truth. It patches fifteen artboards under four outcome classes (§6): two replaced (`ScanInventoryView` with five `handoff-E` mentions, `InstallInventoryView` with one — the only two source screens carrying the marker at Phase D close), five amended (`OperatorHome`, `RunStepView`, `BlockerView`, `SerialHistoryView`, `SupportDiagnosticsView`), six inspected only (`MeasurementCaptureView`, `RunCloseReadinessView`, `SupplierEvidenceChecklist`, `ReportsHome`, `RunCloseReportView`, `RunCloseReportGenerationView`). Six shared components extended, three added (`station-chip`, `presentation-expiry-strip`, `handoff-gap-card`). Four flow maps updated. Every changed artboard cites a Phase F call-log row, scan-classification rule, bench scenario, registered Phase E vocabulary, or explicit remaining handoff; if a change has no cite, the change does not land. The `handoff-E` markers get replaced with the classifier's four `ScanClass` outcomes plus the scan-layer refusal `scan_checksum_invalid` and the post-operation runtime refusal `not_found_or_not_visible`. Two triggers evaluated explicitly at close: §15 Phase M (Part / Inspection Requirement boundary — if fired, moves before Phase H) and §16 handoff-A track 2 (external_viewer caller_type registration — sharpened trigger from v0.6 fires only where the audit trail's caller identity would be materially wrong under the F2 track 1 `access_admin` workaround). Alongside Phase G, a small F2b addendum landed in `contracts/failure-classes.yaml` at commit-pending (three first-class parent-class entries: `state_transition_forbidden`, `idempotency_conflict`, `authorization_denied`) under the F2 hygiene arc, closing F2's "publish no name a hard filter refuses" rule against the last three Physical Presence surface names. The F2b addendum sits in its own commit and does not count against Phase G's product-registry-delta-zero close signal. Thirteen sprint cards (126–138) drafted up front per practice #32 and staged in `dev/sprints/`. Plan at `docs/PHASE_G_PLAN.md`. The plan uses the eleven-section shape from `dev/process-notes/phase-opening-pattern.md`. Cadence: auto-within-phase, same as Phase D, Phase E, and Phase F. Every gate at Phase F close continues to hold at Phase G close with a canvas artefact count delta only (66 → 69 from three new component files); the failure-classes count moves from 24 to 27 through the parallel F2b commit. **Update 2026-08-28:** sprint 126 (ScanInventoryView replaced) closed on the same day the phase opened (commit `6f635c4`); twelve cards remain pending (127–138). The opening entry is preserved as authored so the arc is legible; the current pending count reads off `grep -l "status: pending" dev/sprints/sprint-1[23]?-*.md`.

- **2026-08-28 Claude Code — Phase F opens.** COMPREHENSION_AFFIRMATION (AGENTS.md hard rule 5, sdd-kit-2; re-articulation on a major scope change). Phase F implements the Physical Presence Bench against `specs/physical-presence-bench/bench-spec-v0.8.md` — the shipping baseline after five review passes on the incoming v0.4 spec. The phase adds no product truth. It exercises the vocabulary Phase E already registered through the action shape a future operator app will use: label → decode → classify → present → bind → install (or refuse) → assert event/state/projection truth. The label grammar is what the shipped `decodeLabel` at `src/harness/scan-decoder.ts` speaks (`record_type:record_alias[:checksum]`), not the JSON envelope v0.4 proposed and not the `v1:` prefix v0.5 recommended — v0.6 reversed the prefix after the shipped parser refused every payload with more than three colon-separated segments; version discrimination lives at the label-file metadata layer (`fixtures/physical-presence-bench/expected-scan-results.yaml`). Ten runtime-touching scenarios ship as VF-048 through VF-057 numbered per `dev/WORKING_AGREEMENT.md § Numbering` from the highest id in use (VF-047 at Phase E close). Decoder-refusal tests ship as plain vitest under `tests/harness/`. Idempotency keys follow the shipped `vf-<NNN>-<call_id>` convention. The printed-label phone test runs under a local dev-tool session that loads a static thirteen-field `CallerContext` fixture with a warning banner naming the fixture as bench scaffolding — Phase H's real auth model has not landed. Fifteen sprint cards (111–125) drafted up front per practice #32 and staged in `dev/sprints/`. Plan at `docs/PHASE_F_PLAN.md`. The plan uses the eleven-section shape extracted at `dev/process-notes/phase-opening-pattern.md`. Cadence: auto-within-phase, same as Phase D and Phase E. Every gate at Phase E close continues to hold at Phase F close with a bench-count delta only (39 → 49 scenarios; whole-bench cross-driver diff-to-zero over 47 scenarios grows to 57; `validate:contracts` and `validate:schemas` unchanged; backend durability proofs unchanged at 15).

- **Phase E review response — four correctness fixes plus the closing durability proof and the last acceptance row (2026-08-28).** A same-day code pass on the shipped Phase E found four correctness issues, all closed at source with coupling tests that would catch regressions. (1) String-comparison expiry in `BindPresentedItemToRunStep`, `InstallInventory`, and `ConsumePresentation`: `'2026-9-1T14:00:00Z'` walked past `world.clock >= presentation.fields.expires_at` because lexical ordering ranks it above a zero-padded September clock. The three sites now route through `presentationExpired(presentation, world)`, a helper that uses `Date.parse` and fails closed on either input being unparseable — matching the discipline `VerifyCertificate` carries. Two direct-call mutation arms lock the shape. (2) Backend-vs-in-memory divergence on §12.1: the partial index in `backend.ts` enforced the one-active-per-item invariant universally, while the in-memory handler writes a `conflicted` Presentation and emits `PRESENTATION_CONFLICT_DETECTED` for non-production purposes. The index is now purpose-aware, filtering `AND json_extract(fields, '$.presentation_purpose') IN ('production_install', 'production_measurement_support')`. VF-047 exercises two receiving_review presentations at two stations on the same InventoryItem; whole-bench cross-driver diff-to-zero over 47 scenarios PASSes, all identical. (3) `access_decision_id` collision: two `EvaluateAccess` calls under one `step_id` against the same target under the same actor produced the same id. The hash material now includes `before` (pre-call `world.seq`); `tests/access/access-decision-id.test.ts` locks distinct-per-call and deterministic-across-replay. (4) `fields.presentation_status` doubled `record.state` on every write. Every mirror write is dropped and `contracts/state-machines.yaml` records `state_field: state` on Presentation. Two code-quality fixes landed: `idempotency_tuple_fields` moved into `contracts/operations.yaml` so the tuple shape is a vocabulary decision, not runtime code; the scan classifier returns `handoff_gap` when `queued_input_field` is absent rather than defaulting to `target_alias` (a field no registered operation reads). Three mutation-test descriptions rewritten to name what the mutation actually does. **The review response also closed the last two open items.** `contracts/run-close-rules.yaml` now carries `required_presentation_on_install` with `blocking: false` — the analogue of `enabled: false` in this registry's field vocabulary — which flips row 31 of `docs/PHYSICAL_PRESENCE_ACCEPTANCE.md` from `pass-in-part` to `pass`. The description records the activation path: when the first factory node opts in, that sprint sets `blocking: true` and wires the check into `RunCloseCheck` against the `InstallationEvent.presentation_id` foreign key that already lands today. `src/harness/run-backend.ts` gained the Presentation-lifecycle durability proof (VF-038 + VF-047): a fresh-from-disk instance reads back `presentation_001.state=consumed` from VF-038's walk (presented → bound → consumed), `presentation_001.state=presented` and `presentation_002.state=conflicted` from VF-047's record-conflict branch, plus both `PRESENTATION_CONSUMED` and `PRESENTATION_CONFLICT_DETECTED` events surviving in the append-only log. Backend gate durability proof count moves from 14 to 15. Acceptance file now scores 33 of 33 pass with no row pass-in-part. Detail in `dev/phase-handoffs/PHASE_E_REVIEW_HANDOFF.md`; the review-response entry lives in `dev/KIT_DIARY.md` Entry 38. Gates at close: validate:contracts ok (45 records, 138 operations, 143 events, 17 state machines, 37 authorization rules, 14 run-close rules), validate:schemas ok (162 op schemas, 99 event payload schemas), bench 39/39 both drivers, whole-bench diff-to-zero over 47 scenarios PASS, backend gate exit 0 with 15 durability proofs, vitest 466/466 across 61 files, tsc 0, format:check clean.

- **Phase E ships — Physical Presence Boundary closed (2026-08-28).** Twenty sprints (091–110) closed in one session. Two records (`Station`, `Presentation`), six operations, seven events, one state machine, four authorization rules (with `ConsumePresentation` reusing `system_lifecycle`), 31 failure classes, 25 reason codes. `InstallInventory` gains an optional `presentation_id`; every VF-001 through VF-037 traces byte-identical. Three driver changes: JSON-expression partial index on the flat `records` table in `backend.ts`, tuple-aware idempotency branch in `driver.ts`, deterministic `access_decision_id` on `EvaluateAccess`'s output. Nine scenarios (VF-038 through VF-046) plus a 17-arm coupling-mutation suite. Scan contract as a harness surface (`scan-decoder.ts`, `scan-classifier.ts`) with 12 tests. `docs/PHYSICAL_PRESENCE_ACCEPTANCE.md` scores 33 of 33 §15 criteria. Every gate green throughout: validate:contracts ok, validate:schemas ok, bench 38/38 both drivers, backend gate whole-bench cross-driver diff-to-zero over 46 scenarios PASS, vitest 462/60, tsc 0. Detail in `dev/phase-handoffs/PHASE_E_HANDOFF.md`. Four new SDD practices recorded in `dev/KIT_DIARY.md` Entry 37 (45 sprint-to-spec sync, 46 pre-execution review, 47 scenario opens audience, 48 dispatch is a compile artefact). Two boundaries remain open: Part / Inspection Requirement (handoff-F, B-Q-31 and B-Q-32) and `external_viewer` as a registered caller_type (handoff-A). Each opens on its own input specification.

- **Phase E opens — Physical Presence Boundary (2026-08-28).** COMPREHENSION_AFFIRMATION (AGENTS.md hard rule 5, sdd-kit-2). Phase E adds the vocabulary the runtime needs to say "this physical item is at this station, in front of this actor, for this run step, within a valid time window" — the fact 22 Phase D artboards carry as `handoff-E`. Scan identity is not physical presence: `readRecordAsCaller` at `driver.ts:202` resolves an alias to a record, and that resolution says nothing about where the item is or who is holding it. Two new records (`Station`, `Presentation`), six operations, seven events, one state machine (Presentation, seven states, expiry-as-predicate matching the Certificate pattern), four authorization rules, and roughly thirty-one failure classes lock the missing shape. `InstallInventory` gains an optional `presentation_id`; every pre-Phase-E install continues to trace byte-identical against the golden. `ConsumePresentation` reuses `system_lifecycle` rather than adding a fifth rule with the same audience. Three driver changes named: a JSON-expression partial index on the flat `records` table for the one-active-Presentation-per-InventoryItem invariant, a tuple-aware branch on the memoised idempotency path so a same-key different-tuple call refuses, and an `access_decision_id` on `EvaluateAccess`'s output for audit continuity.

    The boundary specification arrived from outside as `physical-presence-boundary-spec-v0.4.md` at project root. Moved to `specs/physical-presence/` and iterated through six rounds of review against the code: v0.5 (first grounding pass), v0.6 (seven load-bearing gaps closed), v0.7 (five fatal code-truth claims verified and fixed — one flat records table, no lock primitive in world.ts, memoised idempotency without tuple comparison, no nested executeOperation, presentation_consumption duplicated system_lifecycle), v0.8 (seven shape refinements including a synthetic scan contract, Phase F rename, support_diagnostics narrowing, migration policy), v0.9 (three prior line-citation drifts, two internal inconsistencies, the support_diagnostics enforcement hole, three smaller notes), v0.10 (the `access_decision_id` derivation formula folded back from the sprint draft into §4.2). v0.10 is the shipping baseline. Twenty sprint cards (091–110) drafted and staged in `dev/sprints/`. Plan at `docs/PHASE_E_PLAN.md`. Roadmap block added.



*Agent + Architect. Halts, partials, comprehension affirmations, Rubber Duck observations marked `surfaced`. The Architect reads this section.*

- **2026-08-26 Claude Code — sprint 064 opens.** COMPREHENSION_AFFIRMATION (design philosophy §3.15). BlockerView is the handheld's Blockers tab expanded to full detail. Every blocker uses the six-field shape from the sprint 057 blocker library. The screen sorts blockers into three sections per design spec §11.5: "Can resolve" (blockers the current caller can act on now); "Waiting on another actor" (blockers whose next-actor is a different caller type); "Hidden detail / summary only" (blockers the caller sees at reduced visibility). Caller `operator`. Registered names cited: caller_type `operator`, `quality_engineer`; records `RunCloseCheck`, `ReceivingCheck`, `InventoryItem`, `Nonconformance`, `Measurement`, `Certificate`; rule ids `required_measurements_present`, `certificate_of_conformance_unverified`, `failed_measurement_has_quality_path`; operations `CaptureMeasurement`, `AcceptCertificateAsEvidence`, `VerifyRework`; authorization rules `run_execution`, `receiving_decision`, `quality_disposition`; state names from `contracts/state-machines.yaml`. Refusals: none new — the screen renders blockers that already exist elsewhere in the system, so the discipline of §7 applies (no dismiss, no snooze, no false controls, no generic copy). Three tests: **recovery** — an interrupted operator sees which blockers can be resolved now vs which wait on someone else; **new-shift** — every blocker cites a registered rule id and its description verbatim; the next-actor field names the caller_type; **glove-and-glare** — "Resolve" primary at 56pt on the one operator-actionable blocker; other blockers show "View detail" secondary at 44pt.

- **2026-08-26 Claude Code — sprint 063 opens.** COMPREHENSION_AFFIRMATION (design philosophy §3.15). RedlineRequestView is where the operator raises a deviation from the floor. The screen shows the ProcedureStep's current instruction, an entry area for the proposed change and reason, and an optional attachment (photo of the problem). Primary action fires a two-op chain: CreateRedlineDraft walks the new Redline to `draft`; SubmitRedline walks it to `submitted`. Everything from `under_review` onwards happens on the Mac Engineering station under `redline_review` (manufacturing_engineer). Caller `operator`; authorization rule `redline_authoring`. Registered names cited: caller_type `operator`; record `Redline` with initial state `draft` and next state `submitted` (from `contracts/state-machines.yaml`); the terminal states `closed` and `rejected` for context; record `ProcedureStep`, `ProcedureVersion`, `RedlineDiff`; operations `CreateRedlineDraft` (rule `redline_authoring`, emits `REDLINE_DRAFT_CREATED`), `SubmitRedline` (rule `redline_authoring`, emits `REDLINE_SUBMITTED`); composite chain `CreateAttachment + LinkAttachment` (photo evidence) per §5.4; downstream operations named as read-only citations: `ReviewRedline`, `RecordApprovalDecision`, `ApplyRedline`, `MergeRedlineIntoProcedureVersion`, `CloseRedline` — these do not fire from this screen. Refusals: Submit disabled until proposed change and reason are non-empty (validation_error surfaced with the disabled-strip); Submit fires the two-op pipeline in one tap. Three tests: **recovery** — a returning operator sees the instruction, any typed draft, the reason, and the Submit state; **new-shift** — the current instruction is shown verbatim from the ProcedureStep, the operator's proposed change is a text area with a labelled purpose; **glove-and-glare** — Submit at 56pt; the text areas are 15px minimum with generous height for typed thumbs.

- **2026-08-26 Claude Code — sprint 062 opens.** COMPREHENSION_AFFIRMATION (design philosophy §3.15). InstallInventoryView is the install-a-child screen. A scanned child InventoryItem, matched against a BOMLine on the current step, transitions from `reserved` (or `kitted`, or `in_wip`) to `installed` under the parent inventory item. Caller `operator`. Registered names cited: caller_type `operator`; record `InventoryItem` with states `reserved`, `kitted`, `in_wip`, `installed`, `removed`, `quarantined`; record `BOMLine`, `EffectivityRule`, `EffectivityResolution`, `InstallationEvent`; operation `InstallInventory` (rule `run_execution`, event `INVENTORY_INSTALLED`), `RemoveInventory` (rule `inventory_planning`, event `INVENTORY_REMOVED`); the run-close rule `required_installations_present`; the generic failure class `state_transition_forbidden` for wrong-state refusals; handoff-E for the physical-presence claim. Refusals visible: Install disabled when the child state is not one of `reserved`/`kitted`/`in_wip` (state_transition_forbidden); Install disabled when the child is `quarantined` (receiving_quarantine_active); Remove disabled on this handheld because `RemoveInventory` cites `inventory_planning` (planner, not operator). Three tests: **recovery** — the returning operator sees parent, expected child, scanned child, and the transition preview; **new-shift** — every state, operation, and rule is registered; the from→to transition is drawn explicitly; **glove-and-glare** — the primary Install button is 56pt; the parent/child cards are legible at arm's length.

- **2026-08-26 Claude Code — sprint 061 opens.** COMPREHENSION_AFFIRMATION (design philosophy §3.15). MeasurementCaptureView is the numeric-entry screen. The operator enters a reading, sees the expected range, confirms the tool, and taps Capture; the driver returns one of the four Measurement result values. Caller `operator`. Registered names cited: caller_type `operator`; record `Measurement` with result values `not_evaluated`, `pass`, `fail`, `warning`; operation `CaptureMeasurement` (rule `run_execution`); event `MEASUREMENT_CAPTURED`, `MEASUREMENT_PASSED`, `MEASUREMENT_FAILED`, `MEASUREMENT_WARNING`; record `Instrument` with the `cal_status` field values `in_cal`, `overdue` (and others). The fail result activates the registered run-close rule `failed_measurement_has_quality_path` — a link to the quality path is shown after fail. Refusals visible: Capture disabled when the tool's `cal_status` is `overdue` (calibration gate from persona gap 7); Capture also refused when the value is outside a plausible range for the field (validation_error). Three tests: **recovery** — a returning operator sees the required range, the entered value if any, and the tool; **new-shift** — the range is prominent, the unit is explicit, the tool is named; **glove-and-glare** — the entered value is 48px display, the primary Capture button is 56pt.

- **2026-08-26 Claude Code — sprint 060 opens.** COMPREHENSION_AFFIRMATION (design philosophy §3.15). ScanInventoryView is the scan disambiguation screen. It resolves a scanned string against the current step's expected inventory and shows the result. Identity reads through `readRecordAsCaller(InventoryItem)`; the confirmation-of-presence at the station has no registered operation and routes to handoff-E (design spec §22; B-Q-33). Caller `operator`. Registered names cited: caller_type `operator`; record `InventoryItem` with states `expected`, `received`, `available`, `quarantined`, `reserved`, `kitted`, `in_wip`, `installed`, `removed`; record `Certificate` with state `verified` on the release-eligibility chip; the four §10 scan classifications (`identity_only`, `operation_binding`, `presence_asserting`, `handoff-gap`); reason code `handoff-E` marker for presence assertion. Refusals present: the "Confirm matched item" and "Reject unexpected item" primary actions carry the handoff-E badge — the identity read is legitimate today, the presence claim is not. Three tests: **recovery** — the returning operator sees expected vs scanned vs match result and knows the next click; **new-shift** — every state name is registered and drawn from `contracts/state-machines.yaml`; **glove-and-glare** — scan result cards are big-tap-target, expected/scanned distinction uses layout rather than colour to survive glove-hand tremor.

- **2026-08-26 Claude Code — sprint 059 opens.** COMPREHENSION_AFFIRMATION (design philosophy §3.15). RunStepView is the workhorse handheld screen; the operator lives here during step execution. The screen answers: what is the step's instruction; what does the step require (a measurement, an installation, an evidence attachment); which requirements are met and which are pending; what does the operator do next. Caller `operator`. Registered names cited: caller_type `operator`; record `RunStep` with state values `not_started`, `ready`, `in_progress`, `blocked`, `complete`, `failed`, `rework_required`, `rework_in_progress`, `skipped`; operations `StartRunStep`, `CaptureMeasurement`, `CreateAttachment` + `LinkAttachment` + optional `RouteAttachmentForReview` (composite chain for attach evidence, per design spec §5.4), `CompleteRunStep`, `SkipRunStep`, `FailRunStep`, `StartRunStepRework`; events `RUN_STEP_STARTED`, `MEASUREMENT_CAPTURED`; result values `not_evaluated`, `pass`, `fail`; run-close rule ids that block completion (`required_measurements_present`, `required_installations_present`, `failed_measurement_has_quality_path`); one reason code (`attachment_access_denied`) shown on the evidence row where the caller cannot attach. Refusals shown: Complete step disabled by `required_measurements_present` (measurement pending) — the disabled-action-strip from sprint 054 cites the rule id and names `CaptureMeasurement` as the clearing act; Attach evidence potentially disabled by `attachment_access_denied`; presence-asserting scan routes to handoff-E (no operation for asserting physical presence). Three tests: **recovery** — an interrupted operator returns and sees run alias, step alias, instruction, requirement status, current blocker; **new-shift** — the instruction text is prominent, each requirement is named in registered vocabulary, primary action is Complete step; **glove-and-glare** — instruction text 22px, measurement value entry area is big (48pt tap targets), Complete step 56pt at 17:1 contrast when enabled, disabled state greyed with reason cited beside it.

- **2026-08-26 Claude Code — sprint 058 opens.** COMPREHENSION_AFFIRMATION (design philosophy §3.15). OperatorHome is the handheld's Today tab. It serves the operator at three moments: start-of-shift, mid-shift after an interruption, and any return to the device. Screen answers Endsley's three SA questions (§3.10): perception — current run, current step, current state, top blocker; comprehension — the next-action card names the operation the primary tap invokes; projection — the RunCloseReadiness projection reveals whether close is on the near horizon. Caller: `operator`; profile: `operator_station_view`. Registered names the artboard cites: caller_type `operator`; visibility profile `operator_station_view`; record `Run`; state values `ready`, `in_progress`, `paused`, `blocked`, `complete` from `contracts/state-machines.yaml`; operations `StartRun` (rule `run_execution`), `CompleteRunStep`; projection `RunCloseReadiness`; the run-close-rule id `failed_measurement_has_quality_path` cited on the top blocker. Refusal shapes present: the empty-states patterns `no_records_visible_under_current_profile` (when the profile whitelist excludes runs) and `no_records_exist` (when the operator has no assigned work); the blocker patterns from sprint 057 in compact form. Three tests: **recovery** — the returning operator reads run alias, current step number, last-action strip, and top blocker without a menu; **new-shift** — the primary action label names the operation and the current step in registered vocabulary; **glove-and-glare** — the primary "Continue" is 56pt on the handheld tokens palette; adjacent tap targets separated by one tap-target width. Halt condition if the vocabulary cannot draw one of the required pieces: `design_pattern_missing`.

- **2026-08-26 Claude Code (Opus 4.7)** — Phase D open. COMPREHENSION_AFFIRMATION (per SDD hard rule 5 and design philosophy §3.15; re-articulation on a major scope change). Sprint 053 establishes the Phase D canvas and loads the project's registered vocabulary onto a reference artboard. The screen is for the design team and the downstream artboard-authoring sprints (054-087) — every screen after 053 reads this artboard to know which names are registered and therefore drawable. The caller of the reference artboard is the design skill itself, not a runtime caller_type; no operation fires. Registered names the artboard cites: the ten caller types from `contracts/modules.yaml` (manufacturing_engineer, planner, operator, quality_engineer, machine_integration_owner, adapter, system_worker, access_admin, service_account, support_user); the sixteen records with lifecycles from `contracts/state-machines.yaml` with their state fields and terminal states; the three status-light records (BuildCheckResult, ReceivingCheck, RunCloseCheck); the ten receiving-rule ids from `contracts/receiving-rules.yaml`; the thirteen run-close-rule ids from `contracts/run-close-rules.yaml`; the twenty caller-visible reason codes from `contracts/reason-codes.yaml`; the eight visibility profiles from `contracts/visibility-profiles.yaml`; the four visibility levels (`full`, `summary`, `denied`, `hidden_existence`) from `src/driver/visibility.ts`. Nothing refuses on this screen; the artboard is a reference, not an interactive surface. Grading: the three tests in the philosophy §6 do not apply directly (there is no operator interaction), but the artboard passes an equivalent test — a downstream artboard-authoring sprint can find every name it needs on this reference without looking outside the canvas. Halt condition: `design_pattern_missing` if the design skill cannot render a name-density table at the size the vocabulary requires.
- **2026-06-30 Claude Code (Opus 4.8, 1M context)** — COMPREHENSION_AFFIRMATION: This project is **software for running distributed factories that build complex hardware** — an operational record system for physical production, not a dashboard, ERP, or PLM. Its design model is a *manufacturing operational grammar*: factory execution modeled as typed objects, events, payloads, sessions, sequence rules, state transitions, runtime acceptance rules, evidence requirements, governed reports, version/effectivity rules, and controlled grammar change. SDD at root means description is the lossy step: typed records must preserve what happened, prose only annotates and justifies. The load-bearing realization is that this is SDD at two altitudes — dev/sdd-kit-2 is my way of working *on* the build, and the product itself is SDD applied *to* factories. The doc stack was authored in the kit's own doctrine: the research-dossier 11-layer manufacturing grammar is `grammar/PRINCIPLES.md`'s 11-layer stack; "run close as dual-contract verification" is the signal+artifact dual contract; "run close narration" (narrate → detect observations → dispose) is the Rubber Duck Pass; "a typed halt refuses to corrupt the record" is halt-and-articulate; the grammar-gap proposal taxonomy is supervised grammar evolution. The canonical loop here: the contract registries are the locked vocabulary; **no behavior is invented** — if the contract stack does not specify a behavior, the executor emits ContractGap/TODO/`not_implemented`, never a guess. Build-time authority order: Contract Spec v0.4.1 > Harness v0.1.2 > VF-003 v0.1.1 > Build Readiness v0.2 > earlier docs; product behavior governed by Product Spec v0.6. First success condition: `VF-003 ScenarioResult.status == passed` against the in-memory ProductDriver, then the backend skeleton passes the *same* scenario without weakening it or bypassing operation boundaries. The hard rule binding me: halt and surface when the contract underspecifies rather than paper over it.

- **2026-06-30 Claude Code** — Six cross-document tensions surfaced during the full read, to be resolved (as recorded decisions or typed ContractGaps) during Phase-1 registry extraction, not invented around. Detail lives in `dev/sprints/sprint-001-registry-extraction.md ## notes`. Pointers: (B-Q-1) `QualityContainmentAction` has a `required → active` lifecycle in Build Readiness §7.5 but no registered state machine in Contract Spec §8/§10; (B-Q-2) `StartQualityContainment` is invoked from `disposition_pending` in VF-003 but Contract Spec §10.3 only authorizes it from `open` — the handler must perform no NC transition there or it breaks the `after_step_028: disposition_pending` assertion; (B-Q-3) record-name conflict `ReworkRun`/`Verification` (Contract Spec, TAD) vs `ReworkRecord`/`VerificationRecord` (Build Readiness) — Contract Spec is canonical; (B-Q-4) `INVENTORY_IN_WIP` cardinality — one event per `StartRunWithInventory` call vs one per item; (B-Q-5) cross-module emission of `REDLINE_APPROVED` by `RecordApprovalDecision` (Approval Module) must be registered with an explicit producer; (B-Q-6) eleven registries required (add `observability-profiles.yaml`, `compatibility-profiles.yaml`), not nine. Architect ratifies the proposed resolutions during plan-mode review of sprint 001.

- **2026-06-30 Claude Code** — The `## Decisions` entries below are transcribed from the governing documents and this session's stack/cadence answers. `## Decisions` is Architect-only; these are drafted for ratification or correction, not asserted unilaterally.

- **2026-07-01 Claude Code** — Went through the persona reviews (`dev/persona-reviews/PERSONA_REVIEWS.md`, 14 factory roles) and checked each of the 11 gaps against the actual code. All 11 are real. Plain-language write-up + ranked to-do list in `dev/persona-reviews/PERSONA_REVIEW_PASS-round-1.md`. Biggest gap by far: the system never checks WHO does each action — `actorCallerType` is passed into the engine and then thrown away (driver.ts:35, never used again), so the same person can author a change and approve it, or do a job and verify their own work. That's the one thing the quality director, the compliance officer, and outside auditors all need. Fixing it also sets up the e-signature gap. Building it now per the user's go-ahead. Rest of the list, roughly in order after that: typed disposition kinds + who can approve them, checking the whole affected batch (not just this run) is remediated, export access by person nationality, effectivity by serial range, calibration checks on measurements. Two we're deliberately not doing (noted so they don't look like accidents): offline mode, and eBOM/design-BOM reconciliation.

- **2026-08-25** — Sprint 029 mapping table drafted at `dev/sprints/sprint-029-mapping-table.md`. All 22 rows (11 dimensions + 11 enforcement points) scored: 4 already-spoken, 6 extends-existing, 12 new-vocabulary. Same-word audit was clean — nothing the mapping proposes is a second word for something the registries already speak. Notable existing surface the mapping surfaced: the `access` module is already registered (`Access / Visibility Module`) with two records (`AccessDecision`, `AuditEntry`), one operation (`EvaluateAccess`), and four events (`ACCESS_DECISION_ALLOWED/SUMMARY/DENIED/AUDITED`) — sprint 031 generalizes rather than adding. Four concept calls recorded as B-Q-74 (access-group storage), B-Q-75 (customer identity home + Order record), B-Q-76 (contract cascade to RunCloseReport), B-Q-77 (service-account scope storage), each with a candidate answer and the sprint that decides it. Awaiting Architect ratification before sprint 030 dispatches.

---

## Decisions

*Architect-only, append-only. Transcribed 2026-06-30 from the governing doc stack + this session's answers; open to Architect correction.*

- **2026-07-01 (from the user)** — Just fix the gaps the persona reviews found. Work autonomously, keep the language plain — no version numbers, no "ratification," no ceremony. Start with the biggest gap: nothing checks WHO is doing each action, so the same person can write a change and approve it, or do a job and verify their own work — aerospace forbids that. Make the system track who did each action and refuse a same-person approval/verification, with a test proving it refuses. Then keep going down the list.

- **2026-06-30** — Project scope: build the first executable slice of a distributed-factory execution-record system. The immediate target is the VF-003 scenario ("valve body failed torque / redline / rework / run close") compiling and passing against an in-memory ProductDriver, then the same scenario passing against a backend skeleton without weakening the scenario or bypassing operation boundaries. The product is the operational record system for physical production; first version is operator-led production loop, released procedures, linear run execution, measurement capture, serialized installation, nonconformance, redline approval, dual-contract run close, machine-evidence states, typed RunCloseReport, serial history, access filtering, and bounded drill-down. Not first version: full ERP/PLM, scheduling, machine control, offline-first nodes, physical simulation.

- **2026-06-30** — Stack: TypeScript / Node. Contracts authored as YAML validated by JSON Schema; harness ProductDriver implemented against the TypeScript interface in Harness Spec §11. Rationale: matches doc 08's `npm` command targets and the TS driver interface; native JSON-Schema validation. Proposed dependency set (for ratification): TypeScript, a test runner (vitest), a YAML parser, a JSON-Schema validator (ajv). No other dependencies without surfacing.

- **2026-06-30** — Build-time authority order: Operation/Event/State Contract Spec v0.4.1 > Virtual Factory Harness Spec v0.1.2 > Executable VF-003 Scenario Spec v0.1.1 > Build Readiness Plan v0.2 > earlier research/product/architecture docs. Product behavior is governed by Product Spec v0.6. The Contract Spec wins all build-semantics conflicts; the TAD wins module ownership until amended.

- **2026-06-30** — Executor rule (non-negotiable): the executor must not invent product behavior. If a behavior is not defined by the contract registry, operation-handler contract, record/event/projection/report/access contract, or the VF-003 scenario contract, the correct output is a ContractGap, a `not_implemented` failure class, or a TODO artifact — never a guess. VF-003 must never be made to pass by weakening assertions.

- **2026-06-30** — Cadence: Phase 1 (registry extraction, the architecture-band founding act) runs plan-mode-per-sprint. Later phase cadence revisited at each phase boundary.

---

## Built

*Agent appends one entry per sprint close. Append-only.*

- **Sprint 130 — BlockerView (amended) (2026-08-28, closed).** `canvas/handheld/BlockerView.dc.html` gained two new sections beneath the existing Phase D five (can_resolve / can_view / waiting_on_another_actor / summary_only / hidden_existence). "physical_presence · product blockers" renders all nine Physical Presence refusals as compact cards, each carrying the exact throw template from `handlers.ts` per criterion 28: `presentation_conflict` (PresentInventoryAtStation), `presentation_expired` (two throw sites: BindPresentedItemToRunStep and ConsumePresentation), `presentation_terminal` (RejectPresentedItem/ClearPresentedItem), `wrong_item` (two throw sites: InstallInventory bound-item check and BindPresentedItemToRunStep expected-child check with the two-field guard called out), `state_transition_forbidden` (grey — F2b first-class parent from world.ts moveState, commit c78f730 cited), `idempotency_conflict` (grey — F2b first-class parent from driver.ts), `consuming_operation_mismatch` (ConsumePresentation), `binding_forbidden_for_purpose` (BindPresentedItemToRunStep, §4.2 narrowing preventing the support_user widening from leaking), `authorization_denied` (grey — F2b first-class parent from driver.ts callerMayInvoke, VF-043 adapter example cited). A second new section, "scan-layer / post-operation refusals · not product blockers", renders three cards in blue to visually separate them from product blockers: `scan_checksum_invalid` (decoder layer), `handoff_gap` (classifier layer), `not_found_or_not_visible` (access layer). Meta strip updated to name the layered structure. Content assertions: zero handoff-E; all nine product blockers present; all three scan-layer refusals present; every throw template matches the handler-site text verbatim (spot-checked against grep of `handlers.ts`). Gates: validate:contracts ok, tsc 0, prettier clean. Card: `dev/sprints/sprint-130-blocker-view-amended.md`.

- **Sprint 129 — RunStepView (amended) (2026-08-28, closed).** `canvas/handheld/RunStepView.dc.html` gained a StationChip in the header (`station-B4 · bench_station · 14:22`), a Presentation-bound block between the instruction card and the requirements list citing `BOMLine.part_revision (45-8829-G, rev B)`, `presentation_alias`, `station_alias`, `intended_operation` plus a green readiness line reading `BindPresentedItemToRunStep ready · run step 'in_progress', Presentation 'bound', item identity matches BOMLine`. Below the actions row, a red `wrong_item` refusal block cites the two-field guard verbatim: throws only when the operation input carries BOTH `parent_inventory_alias` AND `expected_child_inventory_alias`; if either is absent the check does not fire, matching the shipped guard at `BindPresentedItemToRunStep`. The block explicitly notes a second `wrong_item` throw lives in `InstallInventory`'s bound-item check, rendered on `InstallInventoryView`, not on this screen. A `handoff-F` amber marker names the Part / PartRevision / Drawing / InspectionRequirement vocabulary gap (B-Q-31, B-Q-32) and points at Phase M's trigger evaluation at sprint 133 → decision at sprint 137's `docs/phase-g-phase-m-trigger.md`. Content assertions: zero handoff-E; nine of nine specified names present (`part_revision`, `presentation_alias`, `station_alias`, `intended_operation`, `BindPresentedItemToRunStep`, `wrong_item`, `handoff-F`, `parent_inventory_alias`, `expected_child_inventory_alias`). Gates: validate:contracts ok, tsc 0, prettier clean. Card: `dev/sprints/sprint-129-run-step-view-amended.md`.

- **Sprint 128 — OperatorHome (amended) (2026-08-28, closed).** `canvas/handheld/OperatorHome.dc.html` gained a StationChip in the header (`station-B4 · bench_station · 14:22`) replacing the plain station text; a title attribute plus an HTML comment cite `Station.station_alias · Station.station_type` from `contracts/records.yaml`. A new "Active Presentation" panel sits between the header and the "Active run" section citing four fields (`presentation_alias`, `station_alias`, `intended_operation`, `presentation_source`) with the state pill reading `state bound`. An expiry strip below the fields renders `expires_at 14:28:00Z · world.clock 14:22:15Z · 5:45 left`. The Presentation summary reads `Presentation.state`, not the dropped `presentation_status` mirror — `grep -c presentation_status` returns 0. Content assertions: zero handoff-E; six of six named fields present; state-bound rendered; zero presentation_status references. Gates: validate:contracts ok, tsc 0, prettier clean. Card: `dev/sprints/sprint-128-operator-home-amended.md`.

- **Sprint 134 — Components pack (six extended, three new) (2026-08-28, closed).** Executed ahead of the numbered order (before sprints 128–133) so the amended screens can cite landed components rather than pending files. Six extensions and three new files land in `canvas/components/`. State-badge lede corrected from "Three of sixteen state-machined records drawn as samples" to "Four of seventeen state-machined records drawn as samples (Presentation added at Phase E close)"; a new Presentation section renders six stored states (`presented`, `bound`, `consumed`, `rejected`, `cleared`, `conflicted`) plus a lede naming `expired` as the read-time predicate the badge does not carry. Blocker-card gained three Physical Presence blocker rows (`presentation_expired`, `presentation_conflict`, `state_transition_forbidden`), the last cited as the F2b first-class parent from `world.ts moveState`. Caller-profile-chip gained a station-context variant that appends `station-B4 · bench_station` to the chip when the caller is at a Station. Visibility-badge gained a "Hidden-existence no-leak variant" section rendering an empty dashed pill matching `visibility.ts:hiddenExistenceResponse` at line 106. Disabled-action-strip gained four Physical Presence refusal strips (`presentation_expired`, `wrong_item`, `state_transition_forbidden`, `idempotency_conflict`); the last two cite the F2b first-class parents. Action-button gained two examples showing the `presentation_alias` cite line on `InstallInventory` (input-object field, not positional) and the primary "Present at station" firing `PresentInventoryAtStation`. Three new files: `station-chip.dc.html` (72 lines — header strip with present/absent variants and cite-line notes), `presentation-expiry-strip.dc.html` (65 lines — three time-remaining states calm/warn/expired against `world.clock`), `handoff-gap-card.dc.html` (63 lines — amber card for classifier's `handoff_gap` outcome, two variants: handoff-F and handoff-A track 2). Canvas artefact count grows from 66 to 69. Content assertions: all seven Presentation states rendered on state-badge (six stored + expired predicate); "seventeen" appears once in the state-badge lede; nine component files present in `canvas/components/` (was six). Gates: validate:contracts ok (unchanged), tsc 0, prettier clean. Rubber Duck Pass: every registered name (`PresentInventoryAtStation`, `InstallInventory`, `physical_presence`, `run_execution`, `required_idempotency_key`, `hiddenExistenceResponse`, `moveState`, `presentation_expired`, `presentation_conflict`, `state_transition_forbidden`, `idempotency_conflict`, `wrong_item`, `access_admin`, all Presentation states, `station_alias`, `station_type`) resolves against `contracts/*.yaml` or `src/driver/*.ts`. The design philosophy §3.1 exception-only-colour rule held: amber for warn/handoff-gap, red for hard refusal (wrong_item), grey for ground and the runtime-executor parents, green reserved for the load-bearing success state. Card: `dev/sprints/sprint-134-components-pack.md`. Files touched: nine component files + BLACKBOARD.

- **Sprint 127 — InstallInventoryView (replaced) (2026-08-28, closed).** One `handoff-E` mention on `canvas/handheld/InstallInventoryView.dc.html` replaced. The amber handoff banner became a grey bound-Presentation panel showing six named fields (`presentation_alias`, `station_alias`, `expires_at`, `intended_operation`, `presentation_source`, `presentation_purpose`) plus a state line reading "state bound → consumed via in-process ConsumePresentation inside InstallInventory's snapshot (sprint 095)". The primary cite line moved from `→ InstallInventory · rule run_execution · emits INVENTORY_INSTALLED` to `InstallInventory(world, input) · input carries child_inventory_alias, parent_inventory_alias, optional presentation_alias · rule run_execution · required_idempotency_key · emits INVENTORY_INSTALLED` — the shipped handler signature is named honestly (world, input); the three input fields are named as fields, not positional args, per v0.9 §8.2 as rewritten. The refusal legend moved from four state-machine-only rows to eight registered failure classes: `wrong_item` (InstallInventory bound-item check), `presentation_expired` (world.clock >= expires_at via presentationExpired), `presentation_not_bound`, `presentation_not_active` (terminal states), `presentation_conflict` (another active production Presentation at another station), `state_transition_forbidden` (F2b first-class parent from world.ts moveState), `idempotency_conflict` (F2b first-class parent from driver.ts), `consuming_operation_mismatch` (in-process ConsumePresentation refuses; outer snapshot rolls back). The three F2b runtime-executor parents cite their first-class entries. Content assertions: `grep -c handoff-E` returns 0; six Presentation fields present; eight refusal names present; `child_inventory_alias` and `presentation_alias` each present twice, `parent_inventory_alias` once. Gates: validate:contracts ok (138/143/45/17/37/14/27), tsc 0, prettier clean after a small fixup on the F2c validator (commit `509562f`). File grew 206 → 231 lines. Rubber Duck Pass: every refusal name resolves against `contracts/failure-classes.yaml` (including the three F2b additions from commit `c78f730`); no tone-trace drift (registered names verbatim, no softening); the amber handoff colour was retired in favour of the neutral grey ground per design philosophy §3.1; the RemoveInventory disabled affordance stayed unchanged (still off-step, still cites its rule). Card: `dev/sprints/sprint-127-install-inventory-view-replaced.md`.

- **Sprint 126 — ScanInventoryView (replaced) (2026-08-28, closed).** Five `handoff-E` mentions on `canvas/handheld/ScanInventoryView.dc.html` replaced with the shipped scan classifier's outcomes and citations. The match block now labels the scan `presence_asserting` — the classifier's rule (`src/harness/scan-classifier.ts:66-68`) returns `presence_asserting` when a run step is active and the decoded record is `InventoryItem`, which the artboard's scenario (RUN-VF-003-0812-01 step 3, gasket_003 scanned) exercises. The former amber `handoff-E` banner became a grey classifier-outcome panel citing `scan-classifier.ts:ScanClass`, `scan-classification-rules.yaml`, and VF-048. The primary button moved from "Confirm matched item · handoff-E" to "Present at station" with a cite line reading `PresentInventoryAtStation · rule physical_presence · required_idempotency_key` — registered names verbatim per design philosophy §3.13. The secondary "Reject · handoff-E" became "Rescan"; "Open summary" stayed. The classification legend lost every `handoff-E` phrase and gained the two missing outcomes: `scan_checksum_invalid` (client-side refusal, no classifier, no read, no operation) and `not_found_or_not_visible` (post-operation runtime reason code from the access boundary, rendered when the follow-on `readRecordAsCaller` returns `hidden_existence` — not a scan-layer peer, per spec §3.2). Sources footer cites the four required Phase F artefacts (`src/harness/scan-classifier.ts`, `scan-classification-rules.yaml`, `tests/harness/malformed-label.test.ts`, scenarios VF-048/049/053). Content assertions: `grep -c "handoff-E"` returns 0; all five `ScanClass` members present; `not_found_or_not_visible` present; every cited source path resolves; every spot-checked registered name (`PresentInventoryAtStation`, `readRecordAsCaller`, `physical_presence`, `required_idempotency_key`, `operator`, `InventoryItem`, `Certificate`) resolves against `contracts/*.yaml` or `src/driver/*.ts`. Gates: validate:contracts ok (unchanged — 138 ops, 143 events, 45 records, 17 state machines, 37 authz rules), validate:schemas ok (unchanged — 162 op schemas, 99 event payload schemas), tsc 0, prettier clean, vitest smoke on `tests/harness/malformed-label.test.ts` 12/12. File grew 189 → 198 lines. Rubber Duck Pass: no missing pairs, no order violations, no vocabulary gaps, no payload anomalies, no timing surprises, no tone-trace drift — primary button and cite line use registered names verbatim, no softening; the `handoff-E` amber colour was retired in favour of the neutral grey ground per design philosophy §3.1 (colour is exception). Canvas re-seed and republish deferred to a batched republish at sub-phase G.1 close (sprint 127). Row lands in `docs/phase-g-screen-to-call-log-map.md` at sprint 137. Card: `dev/sprints/sprint-126-scan-inventory-view-replaced.md`. Files touched: `canvas/handheld/ScanInventoryView.dc.html` (edit); `dev/BLACKBOARD.md` (## Built append).

- **Post-Phase-F drift close — handoff-A track 1 plus three same-shape patterns (2026-08-28).** A review pass on the two open handoffs at Phase F close (handoff-A external_viewer; handoff-F no Part record) surfaced six same-shape drifts sitting in the vocabulary. Four closed in one commit at source (commit `1dd0cdc`); two wait on their own input specifications. No runtime behaviour change; no test regression. Every gate green throughout: validate:contracts ok, validate:schemas ok, bench 49/49 both drivers, whole-bench cross-driver diff-to-zero over 57 scenarios PASS all identical, backend gate exit 0 with 15 durability proofs, vitest 507/507 across 67 files unchanged, tsc 0, format:check clean. The build philosophy the reviewer articulated: no layer publishes a name a hard filter in another layer refuses. The `receiving_inspector_view` note is the template. Handoff-A track 1 closed at every publish point: `contracts/visibility-profiles.yaml:47,59` swap `audience: [external_viewer]` for `audience: [access_admin]` and gain `intended_audience: external_viewer` matching the runtime workaround at `src/driver/driver.ts:readRecordAsCaller`; four scenarios (VF-001, VF-003, VF-009, VF-014) shift `customer_viewer_1` actor_type from `access_limited_external_viewer` to `support_user`; dead `caller_type === "external_viewer"` OR clause at `src/driver/driver.ts:readEventTraceAsCaller` dropped; EvaluateAccess service-account scope check at `src/driver/handlers.ts` reads `intended_audience` first with audience-array fallback; three test sites (`tests/access/event-replay-user-visible.test.ts:50,60`, `tests/access/visibility-levels.test.ts:92`) swap to `caller_type: "access_admin"` with a comment. Pattern 3 closed: three Presentation-context failure classes (`presentation_wrong_run`, `presentation_wrong_step`, `presentation_wrong_station`) shift `used_by_sprint` from 094 to `deferred` with notes citing the missing actor-to-run/step/station linkage. Pattern 4 closed: `report_access_stale` in `contracts/reason-codes.yaml:87` shifts to `used_by_sprint: deferred` with a note citing the emit-vs-refusal split (the runtime emits `access_policy_change_for_controlled_export` at `handlers.ts:2325` as a §19 trigger cause, not a caller-visible refusal name). Pattern 5 closed: four runtime-generic failure classes (`validation_error`, `precondition_failed`, `access_filtered`, `receiving_check_unresolvable`) added as first-class entries at the bottom of `contracts/failure-classes.yaml` under a shared header comment. Every existing `maps_to:` relationship preserved. Handoff-A track 2 (register `external_viewer` as a real caller_type), handoff-F (register `Part`), and the machine-command boundary (pattern 6) wait on their own input specifications. Detail in `dev/phase-handoffs/POST_PHASE_F_DRIFT_CLOSE_HANDOFF.md`. Two new SDD practices in `dev/KIT_DIARY.md` Entry 40 (54 publish no name a hard filter refuses; 55 runtime-generic classes belong in the failure-classes registry as first-class entries).

- **Phase F ships — Physical Presence Bench closed (2026-08-28).** Fifteen sprints (111–125) closed contiguously. Ten new runtime-touching scenarios (VF-048 through VF-057); nine decoder-refusal vitest cases plus three decoder-happy-path cases; twelve-arm bench mutation battery. Bench count grows from 39 to 49 both drivers. Whole-bench cross-driver diff-to-zero grows from 47 to 57 scenarios all identical. Vitest grows from 466/61 to 507/64. Zero registry edits: `validate:contracts` reports 138 operations, 143 events, 45 records, 17 state machines, 37 authorization rules, 14 run-close rules unchanged. Zero handler edits. Backend gate exit 0; 15 durability proofs unchanged (practice #50 stands — every Presentation state the bench produces is covered by VF-038+VF-047). Seven fixture yamls plus `generated-labels/` under `fixtures/physical-presence-bench/`. Three harness surfaces at `src/harness/`: `label-generator.ts`, `bench-call-log.ts`, `bench-app-flow.ts`. Classification rule set at `scan-classification-rules.yaml` — ten rules covering all seven KNOWN_TYPES plus two `handoff_gap` guards. Printed-label phone test plan and result template at `manual-tests/`. `docs/PHYSICAL_PRESENCE_BENCH_ACCEPTANCE.md` scores 37 of 37 pass. Two amend-in-place moves: sprint 112 shifted from QR image files to deterministic payload strings (QR wrapping deferred to sprint 122's phone test); VF-053 exercises the operation-authorization-wrapper refusal for the hidden-identity outer boundary while the app-flow-layer `not_found_or_not_visible` state lives in bench-app-flow tests. Detail in `dev/phase-handoffs/PHASE_F_HANDOFF.md`. Two new SDD practices recorded in `dev/KIT_DIARY.md` Entry 39 (52 a bench phase adds no product vocabulary; 53 amend sprint cards in place when the code proves the scope shift).

- **Sprint 090 — Phase D remediation pass B (2026-08-26, closed).** A second post-ship review returned forty-plus findings across five sections: wrong authorization rules on cited operations (eleven files), invented vocabulary and state-machine drift (thirteen files), bundle-index-versus-artboard disagreement (twenty-one files), paraphrased button labels (seven files), smaller drift on tokens, chips, flows and one prose leak in `Main.dc.html`. Every finding was verified against the four contract registries — `operations.yaml`, `authorization-rules.yaml`, `state-machines.yaml`, `reason-codes.yaml` plus `failure-classes.yaml` and `visibility-profiles.yaml` — and fixed at source. The worst were architectural: `InstallInventoryView` cited `RemoveInventory` under `inventory_planning` when the operation is `run_execution`; `BuildCheckView` rendered `ApplyBuildCheckResultToRun` as a user button when the rule is `system_lifecycle`; `RedlineDecisionView` from `under_review` enabled `ApplyRedline`, `MarkRedlineAsMergeCandidate`, `MergeRedlineIntoProcedureVersion`, and `CloseRedline`, all forbidden from that state per `state-machines.yaml:213–214`; `ProcedureAuthoringView` from draft enabled `Release` and `Retire`, both forbidden from draft; `SupplierEvidencePacketView` cited `GenerateSupplierEvidencePacket` under `report_generation · system_worker` when the operation is `receiving_decision · quality_engineer`. Invented reason codes on `InstallInventoryView` (`receiving_quarantine_active`, `state_transition_forbidden`) were replaced with state-machine transition citations. `regeneration_required` was demoted from a `GeneratedReport` state to a freshness marker. `summary_only_access` and `hidden_existence_required` were relabeled as internal outcomes, not caller-visible refusal classes. `NonconformanceView` gained its missing `cancelled` state; `ContainmentView` lost its invented `created` state; `MachineEvidenceRecordView` gained the registered `raw → quarantined` transition. `AdminPolicyView` now lists all eight visibility profiles. `page-shell.dc.html` renamed the first tab from `Today` to `Assigned` (a registered noun). `tokens/mac.dc.html` raised meta from 11px to 12px to match the manifest. Every paraphrased button label was rewritten to the exact registered operation name (`RegisterMachine`, `AcceptMachineEvidence`, `GetReport`, `GenerateCertificateOfConformance`, `SupersedeReport`, `AcceptCertificateAsEvidence`, `RunReceivingCheck`). `canvas/handoff/bundle-index.md` was deduplicated (`AdminPolicyView` listed twice). Canvas re-seeded to 66 artefacts and republished to the same URL. All 21 §25 acceptance criteria still hold; `docs/UI_SURFACE_ACCEPTANCE.md` rows 1 and 2 were updated to name 47 screens instead of eight and seventeen. Full artefact list in `dev/sprints/sprint-090-phase-d-remediation-pack-b.md`.

- **Sprint 089 — Phase D remediation pass A (2026-08-26, closed).** Twenty-two Mac artboards the design spec at v0.3 named and Phase D did not draw are now drawn: four Planning, one Quality, four Engineering, two Run Close, five Evidence, four Reports, two Support/Admin. Each cites only registered vocabulary; the strict phase-close grep across the 22 came back with zero real inventions. Canvas count 44 → 66; screen count 25 → 47. Card: `dev/sprints/sprint-089-phase-d-remediation-deferred-surfaces.md`. Both remediation passes preserve every earlier gate.

- **Phase D remediation pass (2026-08-26).** Post-review, three defects were verified and fixed. **(1)** NonconformanceView cited `failure_class: measurement_out_of_range` — not registered in `contracts/failure-classes.yaml`. Fixed at source: now reads `source event: MEASUREMENT_FAILED`, a registered event in `contracts/events.yaml`. This was the one real vocabulary invention that got through per-sprint eye-checks; caught by a strict phase-close grep across all 25 originally drawn screens. **(2)** The 22 UI surfaces the design spec at v0.3 defined and the phase did not draw are now drawn. Twenty-two new Mac artboards under `canvas/mac/`: four Planning (RunPlanningQueue, BuildCheckView, InventoryQueue, EffectivityView), one Quality (RunBlockingConsole), four Engineering (ProcedureAuthoringView, StructureAuthoringView, RedlineReviewQueue, RedlineDecisionView), two Run Close (RunCloseObservationView, RunCloseReportGenerationView), five Evidence (MachineRegistrationView, MachineEvidenceQueue, MachineEvidenceRecordView, AdapterAttributionView, InvalidationImpactView), four Reports (ReportsHome, CertificateOfConformanceView, SupplierEvidencePacketView, AsBuiltView), two Support/Admin (SupportDiagnosticsView, AdminPolicyView). Each cites only registered names — the second phase-close grep across the 22 new artboards found zero real inventions. Total canvas artefact count: 66 (was 44). **(3)** Documentation was corrected. `docs/UI_SURFACE_ACCEPTANCE.md`: row 3 upgraded from `pass-in-part` to `pass` (every registered caller type now has a primary station drawn); row 21 stays `pass-in-part` and honestly states the grep cadence (three sprint closes, plus two phase-close greps); the "What the pack does not cover" section rewritten as "What the pack covers" reflecting full 47-of-47 coverage. `docs/ROADMAP.md` rewritten to name the full 66 artefacts (was 44) and 47 screens (was 25 with a subset note). Every gate untouched throughout the remediation pass. Canvas republished to https://claude.ai/code/artifact/347f2431-d036-4bcf-a3ad-28cc928a3dda.

- **Sprints 087-088 (2026-08-26, closed) — Phase D shipped.** D.7 handoff bundle under `canvas/handoff/`: README.md (what the bundle is, what implementers inherit, what they must not do, the two handoff gaps), manifest.yaml (44 artefacts keyed by kind — vocabulary/tokens/components/patterns/screens/flows — plus tokens summary and handoff-gap questions), bundle-index.md (25-row table filling the eleven-field row shape from §24.5 for every screen — actor, data required, visible states, primary action, secondary actions, disabled states, blocker examples, access variants, events emitted, handoff gaps — every named token registered). D.8 acceptance closeout: `docs/UI_SURFACE_ACCEPTANCE.md` scores 21 of 21 §25 criteria pass; `docs/ROADMAP.md` marks Phase D shipped. **Phase D complete.** 36 sprints (053-088) in one day. 44 artefacts on the canvas: 1 vocabulary reference, 2 token sheets, 8 shared components, 3 pattern libraries, 8 handheld screens, 17 Mac screens, 4 flow maps, 1 handoff bundle. Canvas published at https://claude.ai/code/artifact/347f2431-d036-4bcf-a3ad-28cc928a3dda. Every gate untouched throughout: `validate:contracts` still 132/136/43/16/33/26; bench 29/29 both drivers; vitest 432/432; tsc 0; prettier clean. No code lands in Phase D; no registry edits. The next phase (unscoped here) is the running-code implementation against the same registries the wireframes cited; two boundaries — Physical Presence (handoff-E · B-Q-33) and Part / Inspection Requirement (handoff-F · B-Q-31, B-Q-32) — must close before scanning flows and part-master flows ship in production.

- **Sprints 083-086 (2026-08-26, closed) — D.6 flow assembly: four flow-map artboards.** Under `canvas/flows/`. Each map is a horizontal chain of screen-boxes with operations named on the edges, plus a numbered steps card below listing the scenario walk. **083 handheld-operator flows** (§21.1, §21.2, §21.5 · VF-001, VF-002, VF-003, VF-010) — three chains: normal step (five screens), failed measurement opening a quality path (four screens with the block clearly named), run close blocked then passes (five screens showing the pipeline stages). **084 receiving flow** (§21.4 · VF-025) — one five-screen chain: ReceivingQueue → SupplierEvidenceChecklist → ReceivingCheckView → InventoryQuarantineView → Release refused; every operation named on the edges (`RunReceivingCheck · ApplyReceivingCheckResultToInventory`) and every state cited verbatim. **085 quality flow** (§21.3 · VF-003 disposition and rework) — one five-screen chain: QualityQueue → NonconformanceView → DispositionView → ReworkVerificationView (twice, showing the state walk from disposition_pending through dispositioned/in_rework/verification_pending to verified/closed); the segregation-of-duties invariant surfaced on the verifier card. **086 access flows** (§21.6, §21.7 · VF-012 + Phase C) — two chains: customer summary read → bounded drill-down succeeds → deeper drill-down refuses with `bounded_drilldown_denied`; support session opens → drill-down under scope → audit lookup showing `report_audience_mismatch` → session closes with `SUPPORT_SESSION_CLOSED`. Every flow walks a scenario that passes on both drivers today (verified against `scenarios/*/`); every operation, event, state, rule id, reason code, visibility profile, and summary shape cited resolves in `contracts/*.yaml`. Republished; canvas.json extended to 43 artboards. Sprints 083-086 closed. D.7 handoff bundle opens next.

- **Sprints 078-082 (2026-08-26, closed) — D.5 access + reports pack: five Mac artboards.** Under `canvas/mac/`, each artboard sets its own caller profile in the shell — the five screens are the ones where Phase C access model is most visible. **078 RunCloseReportView** — caller `d.chen (Lockheed Prime) · external_viewer · customer_summary_access`; report_header (audience_profile, freshness, access_policy_snapshot); a full sections table showing all 13 registered RunCloseReport required_sections with per-section visibility badge — six sections `summary` (run_context, executed_steps, measurement_summary, quality_path, installed_inventory, receiving_evidence_summary, machine_evidence_summary, run_close_observations, access_policy_snapshot), two `denied` with reason `record_type_restricted` (redline_history, source_traceability), two `full` (report_header, final_close_result); action row cites `GetReport · rule production_read` and notes drill-down refuses hidden hops with `bounded_drilldown_denied`. **079 SerialHistoryView** — caller `m.tan · quality_engineer · internal_full_quality`; serial detail card + 18-row event table showing the complete lifecycle of one InventoryItem across seven state transitions, three quality events, one report generation event — every event type registered in `contracts/events.yaml`. **080 BoundedDrillDownView** — caller `s.okafor · support_user · support_diagnostics_summary` under an open SupportSession; source card (source object, source level `summary`, hop target, caller, session with expiry countdown); Allowed hops table (4 rows) showing full and summary hop outcomes; Refused hops table (2 rows) both citing `bounded_drilldown_denied` — one for CAGE code (hidden on `supplier_document_summary` shape), one for raw_payload (hidden on `machine_evidence_summary` shape); footer note that every hop writes an `ACCESS_DECISION_AUDITED` event carrying the target alias and reason but no field from the target's data (sprint 049 invariant). **081 AccessDecisionAuditView** — caller `a.ferrari · access_admin`; filter chips (allowed 122, summary 18, denied 8, hidden_existence 0, last 24h, under support session); audit-log table with 8 rows showing every decision kind and every reason code fired in the last hour: `allowed`, `summary`, `denied` with reasons `bounded_drilldown_denied`, `record_type_restricted`, `service_scope_denied`; export/filter action row cites `readProjection over ACCESS_DECISION_AUDITED`. **082 SupportSessionView** — caller `s.okafor · support_user · support_diagnostics_summary` (with the support-orange chip variant); Session card (alias, actor, reason prose, opened_at, expires_at with countdown, opened_via `OpenSupportSession · emits SUPPORT_SESSION_OPENED`, approved_authority `a.ferrari · access_admin`), Scope card (records_touched, views_generated count, attachments_accessed, exports_created, scope_invariant citing `support_context_missing`, expiry_invariant citing `support_context_expired`); recent decisions under this session table showing four ACCESS_DECISION_AUDITED rows; destructive Close-session button cites `CloseSupportSession · emits SUPPORT_SESSION_CLOSED · walks SupportSession to closed (terminal)`. Every registered name (records, reports, projections, operations, events, reason codes, visibility profiles, summary shapes, authorization rules) resolves in the registries; the four §10 summary shapes (`supplier_document_summary`, `machine_evidence_summary`, `nonconformance_summary`, `report_summary`) are cited by name. Rubber Duck clean: no missing pairs, no order violations, no vocabulary gaps, no tone drift; the support-session chip variant renders in the amber exception colour so a returning viewer sees at a glance that they are inside an audited session. Republished; canvas.json extended to 39 artboards. **Screen packs complete** — 25 screen artboards across D.2 (8 handheld), D.3 (7 Mac receiving), D.4 (5 Mac quality), D.5 (5 Mac access + reports). D.6 flow assembly opens next.

- **Sprints 073-077 (2026-08-26, closed) — D.4 quality pack: five Mac artboards.** Under `canvas/mac/`, same 1200×800 shell as D.3 with the active rail item flipped to Quality and the caller-profile chip citing `m.tan · quality_engineer · internal_full_quality`. **073 QualityQueue** — reads the registered `QualityQueue` projection (sourced from Issue · Nonconformance · QualityContainmentAction); six filter chips (needs_containment, needs_disposition, needs_verification, receiving_originated, run_blocking, close_blocking); three sections rendered — Needs disposition (3 rows citing nc_003/007/012 with source Measurement or ReceivingCheck and close-impact), Needs verification (2 rows citing ReworkRun ids and the segregation-of-duties note "verifier ≠ operator (segregation)"), Needs containment (1 row citing an affected-population size). **074 NonconformanceView** — the record's full detail: Source card (opened_by = `system_worker · via CaptureMeasurement`, source_measurement, affected run/step/inventory, failure_class, run-close-impact citing the registered rule `failed_measurement_has_quality_path`) beside Linked-records card (containment, disposition, rework, verification — all pending); Lifecycle strip drawing all nine registered Nonconformance states in order with two done, one here (`disposition_pending`), six future dashed; action row listing all seven registered Nonconformance operations verbatim with rule `quality_disposition` cited. **075 ContainmentView** — QualityContainmentAction record: Details card (parent nc, affected_population definition, containment action prose, related_inventory/related_run counts, release_condition) beside Lifecycle card drawing the two-state machine (`required` here, `active` future); paragraph explaining that active is terminal — the containment ends when the parent NC walks through the disposition-rework-verification chain to closed; cites `StartQualityContainment · emits QUALITY_CONTAINMENT_REQUIRED` and `ActivateQualityContainment · emits QUALITY_CONTAINMENT_STARTED`. **076 DispositionView** — table of all five registered disposition_kind values (`scrap`, `rework`, `repair`, `use_as_is`, `return_to_supplier`) with authority-required column: rework/scrap/return_to_supplier cite `disposition_recording · any of: operator, planner, manufacturing_engineer, quality_engineer`; repair and use_as_is cite `elevated_disposition_authority · manufacturing_engineer or quality_engineer` with the note "Gate enforced inside RecordDisposition"; each row shows the resulting allowed path; a "This nonconformance" card shows the selected kind (rework), reason text, current caller, and the authority-check result; action row cites `RecordDisposition · rule disposition_recording · guard elevated_disposition_authority when kind ∈ {repair, use_as_is}`. **077 ReworkVerificationView** — two-column layout: Rework card (ReworkRun record, status complete, started_via `StartRework`, completed_via `CompleteRework`, rework_action prose, actor with "via floor recall" note) beside Verification card (Verification record, status verification_pending, required_via `RequireVerification`, verifier_authority `quality_engineer · rule quality_disposition`, segregation note "Verifier ≠ rework actor (persona gap 1 · verified_by check)", current caller passes); Nonconformance lifecycle strip showing four done + one here (`verification_pending`) + two future; action row cites `VerifyRework · emits VERIFICATION_COMPLETED · walks Nonconformance to verified · then CloseNonconformance walks to closed`; effect-on-run-close strip citing the registered rule `nonconformance_verified_before_close_if_required` currently reading `fail` and noting that VerifyRework clears the block. Every registered name resolves in `contracts/*.yaml` (records, states, operations, events, authorization rules including the guard rule `elevated_disposition_authority`, projections). Rubber Duck clean: no missing pairs, no order violations, no vocabulary gaps, no tone drift (every label third-person; the "persona gap 1 · verified_by check" note cites the ADDITIONS.md addition by number rather than paraphrasing). Republished. Files: five new .dc.html files under `canvas/mac/`, canvas.json extended to 34 artboards, sprints 073-077 closed.

- **Sprints 066-072 (2026-08-26, closed) — D.3 receiving pack: seven Mac artboards.** Seven workstation-shaped artboards (1200×800 each) under `canvas/mac/`. **066 ReceivingQueue** — global shell (caller-profile chip citing `m.tan · quality_engineer · receiving_inspector_view`; program/contract/factory-node context ribbon; global search), 10-item left rail with counts, four sectioned queue tables (Check blocked · 4, Quarantined · 2, Received no check · 3, Ready to release · 3) each row citing a registered receiving-rule id in the blockers column and quality_engineer as next actor. **067 ShipmentView** — two-column layout: Shipment details card (supplier, CAGE code, customer/program/contract/factory-node context, packing list, PO, received_at) with `CreateShipment / ReceiveShipment · rule receiving_intake` cited beneath the action row; Shipment lines table with three lines; access-filtering section demonstrating the customer/program/contract/factory-node dimensional checks all matching. **068 ShipmentLineView** — line details (part revision, lot/serial, expected/received qty, linked inventory with quarantined badge, required_documents list), receiving checks history table, action row citing `ReceiveInventory · rule inventory_planning · planner (moves InventoryItem to received)`. **069 SupplierEvidenceChecklist** — the required_documents[] table with satisfying document, state, verified-by column; two rows required and blocking (certificate_of_conformance in state `captured`, material_test_report in state `review_required`), two rows not_required (first_article_report, process_certificate); action row citing the four Certificate lifecycle ops (`CaptureCertificate`, `RouteCertificateForReview`, `AcceptCertificateAsEvidence`, `RejectCertificateAsEvidence`) plus `AccessAttachment`; disabled strip listing both open blockers verbatim. **070 SupplierDocumentReview** — the certificate review surface: Document card (cert_type, scope, part_revision, serial_or_lot, CAGE, expires_at with "n/a · this type does not expire" note, attachment id and size), Verification-check card citing `VerifyCertificate (read)` with the four match dimensions and the reader's authority rule, Attachment-access section naming the six `AccessAttachment` outcomes, action row with Accept/Reject/Open/Route buttons citing `AcceptCertificateAsEvidence` and `RejectCertificateAsEvidence · rule receiving_decision`. **071 ReceivingCheckView** — check result card (state `blocked`, both blocker rule ids listed verbatim), required-documents table with the missing state per document, action row citing `RunReceivingCheck · rule receiving_decision`, `ApplyReceivingCheckResultToInventory · rule inventory_disposition`, `OpenReceivingNonconformance`, `OpenSupplierCorrectiveAction · rule quality_disposition`. **072 InventoryQuarantineView** — two-column layout: Inventory-item card (alias, part revision, serial, current state `quarantined`, previous state `received`, quarantined-at time, quarantined-by = `system_worker · via ApplyReceivingCheckResultToInventory`), Reason card (source check id, two blocker rule ids verbatim, "no Nonconformance opened yet", next_actor `quality_engineer · rule quarantine_release`), Release-path card explaining the refusal — release is not an override, `ReleaseFromQuarantine` refuses unless a fresh `ReceivingCheck` reads `passed`; disabled Release button with the disabled-strip explaining what would clear it. All seven use one shared shell (caller-profile chip, ctx ribbon, global search, left rail) with the standard token palette. Every registered name (records, states, operations, events, rule ids, authorization rules, reason codes) resolves in `contracts/*.yaml`. Rubber Duck across the seven: no missing pairs, no order violations, no vocabulary gaps, no tone drift; two design-time observations resolved-here: (i) the ReceivingCheck record note in records.yaml specifies `blockers[]` of registered receiving-rule ids — the queue and check views both cite them verbatim; (ii) the ReleaseFromQuarantine refusal condition ("no fresh passed ReceivingCheck") is a runtime guard that lives in the driver, not a registered rule — cited as a discipline-level constraint on the InventoryQuarantineView with the citation traced to the 2026-07-31 BLACKBOARD decision (B-Q-50). Republished. Files: seven new .dc.html files under `canvas/mac/`, canvas.json extended, `dev/sprints/sprint-066` through `sprint-072` all closed.

- **Sprint 065 (2026-08-26, closed) — RunCloseReadinessView; D.2 handheld pack complete.** One artboard at `canvas/handheld/RunCloseReadinessView.dc.html` (390×844), placed at (3150, 6400). The operator's Attempt Close surface. Run summary tiles at top (7/7 steps · 4/4 measurements · 3/3 installations · 1 blocker). All 13 registered run-close rules listed each with pass / fail / n/a state (11 pass, 2 fail: `failed_measurement_has_quality_path`, `nonconformance_verified_before_close_if_required`). Report row cites `RunCloseReport · not yet generated`. Primary "Attempt close" 56pt disabled with the standard disabled-strip citing the failing rule name and pointing at `quality_engineer` for resolution. Pipeline preview lists the five stages verbatim: `AttemptRunClose` (operator, Run.close_check) → `RunCloseCheck` → `RequestRunCloseReport` → `GenerateRunCloseReport` (walks requested → generating → generated) → `ApplyRunCloseResultToRun` (Run.closed). Every registered name resolves; no tone drift. **D.2 handheld pack complete** — eight phone artboards in a row at y=6400: OperatorHome, RunStepView, ScanInventoryView, MeasurementCaptureView, InstallInventoryView, RedlineRequestView, BlockerView, RunCloseReadinessView. Republished. Files: `canvas/handheld/RunCloseReadinessView.dc.html` (new), `canvas.json` (extended), `canvas/factory-ui-canvas.html` (re-seeded and republished), `dev/sprints/sprint-065-run-close-readiness-view.md` (closed).

- **Sprint 064 (2026-08-26, closed) — BlockerView.** One artboard at `canvas/handheld/BlockerView.dc.html` (390×844, print-fixed), placed at (2700, 6400). The handheld's Blockers tab expanded to full-card detail. Blockers tab marked active in the footer. **Header + meta**: caller-profile chip, station, run alias, "Blockers · 3" count. **Three sections per design spec §11.5**: **Can resolve** (1) — one full card citing the run-close-rule `required_measurements_present`, amber left-border, affected `RunStep step_003 · in_progress`, next-actor `operator · rule run_execution`, allowed action reads "Capture the torque reading for step 3.", primary **Resolve** button at 56pt, clearing act cited as `→ CaptureMeasurement · then Complete step`. **Waiting on another actor** (2) — two full cards: (a) `certificate_of_conformance_unverified` (red left-border) citing `valve_body_001 · InventoryItem.quarantined`, `ReceivingCheck · blocked`, next-actor `quality_engineer · rule receiving_decision`, allowed action "Open the certificate as summary (this role) or route to a quality engineer.", **View detail** secondary at 40pt, clearing chain `→ AcceptCertificateAsEvidence · then RunReceivingCheck · then ApplyReceivingCheckResultToInventory`; (b) `failed_measurement_has_quality_path` (amber) citing `torque_capture_002 · Measurement.fail`, `nc_003 · Nonconformance.disposition_pending`, next-actor `quality_engineer · rule quality_disposition`, clearing chain `RecordDisposition → StartRework → CompleteRework → VerifyRework`. **Hidden detail · summary only** (0) — dashed-border note explaining what a hidden-existence blocker looks like: compact row shows the rule id and next-actor caller_type; detail refuses with `record_type_restricted` or `controlled_data_denied`; card renders as summary shape only. Cards use full six-field shape from sprint 057. Every card carries no dismiss, no snooze, no false controls. Republished. **Rubber Duck Pass:** (1) What does the caller need to see? — three sections sorted by what the operator can act on right now vs what waits on someone else vs what is hidden by profile. Present. (2) What do they need to do? — Resolve on the one operator-actionable card; View detail on cross-actor cards; nothing on the hidden section. (3) What refuses, by what registered reason? — every card cites a registered rule id and its description verbatim; every next-actor and every clearing operation resolves in the registries. Observations: **missing pair** none. **order violation** none. **vocabulary gap** none — every rule id (`required_measurements_present` from run-close-rules.yaml; `certificate_of_conformance_unverified` from receiving-rules.yaml; `failed_measurement_has_quality_path` from run-close-rules.yaml), record and state, operation, and authorization rule resolves in `contracts/*.yaml`. **payload anomaly** none. **timing surprise** none. **tone trace** none — every label factual, third person; the allowed-action strings read as UI copy that names the record and the act, not addressed to any reader. Three tests: **recovery** — the three sections and their counts are visible at first glance; **new-shift** — every rule id is registered and its description quoted verbatim; the next-actor caller_type is named on every card; **glove-and-glare** — 56pt primary on the one resolvable, 40pt secondary on cross-actor, WCAG AAA contrast throughout; the left-border colour (red vs amber) grades severity per philosophy §3.9 ranked-and-rare. Files: `canvas/handheld/BlockerView.dc.html` (new), `canvas/canvas.json` (extended), `canvas/factory-ui-canvas.html` (re-seeded and republished), `dev/sprints/sprint-064-blocker-view.md` (closed).

- **Sprint 063 (2026-08-26, closed) — RedlineRequestView.** One artboard at `canvas/handheld/RedlineRequestView.dc.html` (390×844, print-fixed), placed at (2250, 6400). The operator's floor-side entry for raising a deviation. **Header + meta**: caller-profile chip, station, run alias, step, "Redline" label. **Current-instruction card**: ProcedureStep's own instruction shown verbatim ("Torque M8 bolt at 24-28 Nm.") with the ProcedureStep alias and the ProcedureVersion alias + state cited beneath — the operator sees exactly which released procedure they're deviating from. **Proposed-change textarea** with a filled example: the range concern and the suggested replacement values; caret at the end of the entered text. Field label carries a `→ RedlineDiff` hint so the reader knows the entry becomes a RedlineDiff record. **Reason textarea** with a filled example citing prior runs and a supplier torque chart. **Attach row** for optional photo evidence, showing the composite chain `CreateAttachment + LinkAttachment` from §5.4. **Primary Submit-for-review button** at 56pt with the two-op pipeline cited beneath: `CreateRedlineDraft → SubmitRedline · rule redline_authoring`. A single operator tap walks the Redline from null to draft to submitted in one pipeline. **Redline lifecycle chain** at the foot draws all nine registered states in order: `draft` (here, green), `submitted` (next, amber), then `under_review`, `approved`, `applied`, `merge_candidate`, `merged`, `closed` in dashed muted style — a small note names the boundary: the dashed states move under `redline_review` on the Mac Engineering station (manufacturing_engineer), so the operator sees exactly how far this screen carries the record. Republished. **Rubber Duck Pass:** (1) What does the caller need to see? — current instruction, entry areas for change and reason, attach affordance, Submit button, and where the record goes after they tap. Present. (2) What do they need to do? — type the change, type the reason, optionally attach, tap Submit. (3) What refuses, by what registered reason? — Submit disabled until both textareas hold text (validation_error); the entire downstream is out of this screen's authority — nothing here approves or applies. Observations: **missing pair** none. **order violation** none. **vocabulary gap** none — every Redline state, both operations, both events, the two rules (`redline_authoring` and `redline_review`), the two Attachment operations, and both records (`ProcedureStep`, `ProcedureVersion`, `RedlineDiff`) resolve in `contracts/*.yaml`. **payload anomaly** none. **timing surprise** none. **tone trace** none — every label factual, third person; the textarea placeholders (well, the filled example content) reads as a technician's note, not a message to the reader. Three tests: **recovery** — instruction, typed content, and Submit state all persist through the returning glance; **new-shift** — the redline lifecycle is drawn explicitly so a new operator sees what happens after they tap Submit; **glove-and-glare** — 56pt primary, textareas 15px with 88px height for typed thumbs, WCAG AAA contrast on all text. Files: `canvas/handheld/RedlineRequestView.dc.html` (new), `canvas/canvas.json` (extended), `canvas/factory-ui-canvas.html` (re-seeded and republished), `dev/sprints/sprint-063-redline-request-view.md` (closed).

- **Sprint 062 (2026-08-26, closed) — InstallInventoryView.** One artboard at `canvas/handheld/InstallInventoryView.dc.html` (390×844, print-fixed), placed at (1800, 6400). The install-a-child screen the operator reaches after a matched inventory scan. **Header + meta**: caller-profile chip, station, run alias, step number, "Install child" label. **Parent card** at the top: names the parent InventoryItem (`valve_body_001 · SN 45-8829-C-000234`) with its current state `in_wip` — the state that permits a child install per `contracts/state-machines.yaml`. **Slot vs scanned child** two-card grid: the BOM slot cites the BOMLine alias `gasket_bom_003` and the EffectivityResolution status; the scanned child cites the InventoryItem alias, its serial, and its current state `reserved`. **Transition preview** in a green strip: `reserved → installed via InstallInventory` drawn explicitly so the operator sees the state machine walk before the tap. **Handoff-E note** amber: the install acts on the scanned identity (legit today via `readRecordAsCaller` and the InventoryItem state machine); the presence-at-station claim is the gap (B-Q-33). **Primary Install button** at 56pt with the operation and event cited beneath (`InstallInventory · rule run_execution · emits INVENTORY_INSTALLED`). **Secondary Remove button** disabled with the reason cited: `RemoveInventory` names `inventory_planning` (planner), and the operator is not in that rule's caller list. **Refusal-state legend** at the foot lists four states in which Install refuses, each with its specific reason: `quarantined` (receiving_quarantine_active, in red pill); `expected`, `available`, `installed` each cite `state_transition_forbidden` with the specific fault. Republished. **Rubber Duck Pass:** (1) What does the caller need to see? — parent, BOM slot, scanned child, current state, the transition the click will walk. Answer: yes; the from→to arrow is drawn. (2) What do they need to do? — tap Install (primary) or halt if the child state refuses. Remove is planner-only and disabled here. (3) What refuses, by what registered reason? — Install refuses on four registered InventoryItem states, each with its specific failure class; Remove refuses on the authorization rule `inventory_planning`. Observations: **missing pair** none. **order violation** none. **vocabulary gap** none — every state, operation, event, and rule resolves in `contracts/*.yaml`; the transition drawn matches the registered `via` list (`reserved | kitted | in_wip → installed via InstallInventory`); BOMLine, EffectivityResolution, InstallationEvent are registered records. **payload anomaly** none. **timing surprise** none. **tone trace** none — every label factual, third person; the handoff banner reads "The install acts on the scanned identity; the presence-at-station claim is out of vocabulary today" (third-person system statement, no address). Three tests: **recovery** — parent, slot, scanned child, transition, refusal legend all present; **new-shift** — every name is registered, the state machine walk is explicit; **glove-and-glare** — 56pt primary Install, 44pt secondary Remove clearly greyed with the reason cited beside it, WCAG AAA contrast on the transition pills. Files: `canvas/handheld/InstallInventoryView.dc.html` (new), `canvas/canvas.json` (extended), `canvas/factory-ui-canvas.html` (re-seeded and republished), `dev/sprints/sprint-062-install-inventory-view.md` (closed).

- **Sprint 061 (2026-08-26, closed) — MeasurementCaptureView.** One artboard at `canvas/handheld/MeasurementCaptureView.dc.html` (390×844, print-fixed), placed at (1350, 6400). The numeric-entry screen the operator reaches after tapping Capture on RunStepView. **Header + meta**: caller-profile chip, station, run alias, step number, "Measurement" label. **Field identity**: DataCollectionField alias `torque_bolt_m8` above a 18px human label "M8 bolt torque". **Expected-range card**: "24 – 28 Nm" at 20px, unit called out separately per philosophy §3.13 (registered field values, not paraphrased). **Numeric-entry card** dominates the screen: the reading `26.5 Nm` shown at 56px monospace (glove-tap target for the number and unit both), a "Clear" affordance in the top-right. **Range indicator** green banner: "Inside expected range · projected result · pass" — the projection surfaces before the operator commits, so the tap is on a known-outcome. **Tool row**: Instrument alias `cal_torque_wrench_a` with the `cal_status` field showing `in_cal` in a green pill (persona-gap-7 calibration gate visible on the surface). **Primary Capture button** at 56pt ink-primary black; the operation `CaptureMeasurement · rule run_execution` cited below in small mono. **Result-values legend** at the foot names the four registered Measurement.result values (`pass`, `warning`, `fail`, `not_evaluated`) with a one-line note each, and a citation line names the registered run-close rule `failed_measurement_has_quality_path` that a fail activates. Republished. **Rubber Duck Pass:** (1) What does the caller need to see? — expected range, entered reading, projected result, tool cal_status, primary Capture. Present. (2) What do they need to do? — tap Capture (or Clear and re-enter). (3) What refuses, by what registered reason? — a fail result opens the quality path via `failed_measurement_has_quality_path`; an overdue tool (not shown in this state) would refuse Capture on the calibration gate. Observations: **missing pair** none. **order violation** none. **vocabulary gap** none — `Measurement`, its result values, `CaptureMeasurement`, `Instrument`, `cal_status`, and `failed_measurement_has_quality_path` all resolve in `contracts/*.yaml`; `DataCollectionField` is a registered record; `torque_bolt_m8`, `cal_torque_wrench_a`, `RUN-VF-003-0812-01` are scenario aliases. **payload anomaly** none. **timing surprise** none. **tone trace** none — every label factual, third person, no second-person address; "Inside expected range · projected result" reads as a system statement, not a message to the reader. Three tests: **recovery** — expected range, entered value, projected result all present; **new-shift** — field name, unit, tool, expected range all in registered vocabulary; **glove-and-glare** — 56px reading display, 56pt primary, 20px range, WCAG AAA contrast throughout. Files: `canvas/handheld/MeasurementCaptureView.dc.html` (new), `canvas/canvas.json` (extended), `canvas/factory-ui-canvas.html` (re-seeded and republished), `dev/sprints/sprint-061-measurement-capture-view.md` (closed).

- **Sprint 060 (2026-08-26, closed) — ScanInventoryView; handoff-E made visible.** One artboard at `canvas/handheld/ScanInventoryView.dc.html` (390×844, print-fixed), placed at (900, 6400). Reached from the Scan tab or from RunStepView when the operator scans a required inventory item. **Header** + station strip (unchanged from prior handheld screens). **Meta row**: run alias, current step, required inventory alias — `gasket_003` in this example. **Expected vs scanned** two-card grid: side-by-side comparison so the operator reads the difference by layout rather than by colour (defensible under glove-tremor). **Match result strip** in green: "Matched · identity_only" — the scan resolved to a registered `InventoryItem` and no product claim is made. **InventoryItem state chips** in a row: `reserved` (registered state), reserved-for run alias, `Certificate verified` (Certificate registered state), `receiving_check passed` (ReceivingCheck registered value). **Handoff-E banner** amber: identity resolved; physical presence at the station cannot be asserted from a scan; no operation registered; cites B-Q-33, design spec §22, and the candidate future operation name `PresentInventoryAtStation`. **Primary action** "Confirm matched item" carries an inline `handoff-E` marker in the button — the identity read is legit today but the confirmation-of-presence is the handoff. **Secondary actions**: "Reject" (also `handoff-E`); "Open summary" (legit `readRecordAsCaller`). **Classification legend** at the foot names the four §10 kinds (`identity_only`, `operation_binding`, `presence_asserting`, `handoff-gap`) with one-line descriptions. Republished. **Rubber Duck Pass:** (1) What does the caller need to see? — expected vs scanned, match result, state, whether the click will produce a real effect or a handoff. Answer: yes; the handoff-E marker sits on the exact buttons that need it. (2) What do they need to do? — Confirm matched (handoff-E), Reject (handoff-E), or Open summary (real read). The presence-based operations are visibly gapped. (3) What refuses, by what registered reason? — the presence-assertion path refuses with the handoff-E marker; the identity read succeeds. Observations: **missing pair** none. **order violation** none. **vocabulary gap** none — every state cited resolves in `contracts/state-machines.yaml`; the handoff-E marker cites B-Q-33 by number, which resolves in `contracts/CONTRACT_GAPS.md`; the candidate future operation is drawn from design spec §22's list, not invented here. **payload anomaly** none. **timing surprise** none. **tone trace** none — every label factual, no second person; the handoff banner reads "Identity resolved. Physical presence at this station cannot be asserted from a scan — no operation registered." (third-person passive, no address to reader). Three tests: **recovery** — the two cards show the operator what to compare in a returning glance; **new-shift** — every state and classification is registered, and the handoff distinction is explicit; **glove-and-glare** — 56pt primary, 44pt secondaries, expected/scanned difference by layout (position and label) rather than colour. Files: `canvas/handheld/ScanInventoryView.dc.html` (new), `canvas/canvas.json` (extended to 17 artboards), `canvas/factory-ui-canvas.html` (re-seeded and republished), `dev/sprints/sprint-060-scan-inventory-view.md` (closed).

- **Sprint 059 (2026-08-26, closed) — RunStepView, the operator's workhorse screen.** One artboard at `canvas/handheld/RunStepView.dc.html` (390×844, print-fixed), placed at (450, 6400) beside OperatorHome. Reached from OperatorHome's Continue button. **Header**: back arrow, compact caller-profile chip (`j.rivera · operator`), station identity strip. **Meta row** (warm parchment fill): run alias `RUN-VF-003-0812-01`, part revision, serial, step number, RunStep `in_progress` badge on green. **Instruction card** (white on the warm ground): "Torque M8 bolt at 24-28 Nm." at 22px per philosophy §3.8 ("sized for real viewing distance"); cites the ProcedureStep alias verbatim. **Three requirement rows** each with an SVG status icon (clock for pending, check for done, chain-link for evidence): (1) Measurement pending — expected range 24-28 Nm, tool `cal_torque_wrench_a · in_cal` (Instrument record + cal_status field), Capture button primary; (2) Installation done — `gasket_003 · rev B`, serial number, elapsed time, green tick; (3) Evidence pending — post-torque photo, cites the two-op composite chain `CreateAttachment + LinkAttachment` from §5.4, Attach button. **Complete step** primary button at 56pt, greyed disabled with the disabled-strip beneath it citing the specific rule id `required_measurements_present` and naming `CaptureMeasurement` as the clearing act. **Three secondary buttons** in a row: Skip (`SkipRunStep`), Fail (`FailRunStep`), Rework (`StartRunStepRework`) — each 44pt with the standard 8pt gap. **Footer tab strip** unchanged, Today marked active (RunStepView is a detail off Today, not its own tab). Republished. **Dual/observation contract:** `canvas/handheld/RunStepView.dc.html` (new), canvas.json extended to 16 artboards, `--check` clean (17 files). Gates untouched. **Rubber Duck Pass:** (1) What does the caller need to see? — instruction, requirements with status, primary action, why-disabled. Present. (2) What do they need to do? — Capture (Measurement pending), Attach (evidence pending), then Complete step. Skip / Fail / Rework as secondary paths. (3) What refuses, by what registered reason? — Complete step disabled by `required_measurements_present` (registered run-close rule) with `CaptureMeasurement` named as the clearing operation. Observations: **missing pair** none. **order violation** none. **vocabulary gap** none — every registered name resolves; scenario aliases (`RUN-VF-003-0812-01`, `SN 000234`, `station-B4`, `step_003`, `cal_torque_wrench_a`) are legit stand-ins; `Instrument` and its `cal_status` field are registered per `contracts/records.yaml`. **payload anomaly** none. **timing surprise** none. **tone trace** none — every label factual, no second person, no reassurance; the instruction text is the ProcedureStep's own words in imperative-without-subject form (same as a printed traveler). **Nav-state slip caught** on the footer: initial cut marked the Scan tab as active, but RunStepView is a detail reached from Today. Disposition **resolved-here** — fixed at source before republish; Today is now the active tab. Three tests: **recovery** — a returning operator sees the step number, instruction, requirement status, and why-disabled without menu-hunting; **new-shift** — every action names its registered operation, the requirement types are labelled, the disabled reason is explicit; **glove-and-glare** — 56pt primary, 44pt secondary, 22px instruction text, WCAG AAA contrast, requirements 60pt tall for gloved tap. Files: `canvas/handheld/RunStepView.dc.html` (new), `canvas/canvas.json` (extended), `canvas/factory-ui-canvas.html` (re-seeded and republished), `dev/sprints/sprint-059-run-step-view.md` (closed).

- **Sprint 058 (2026-08-26, closed) — OperatorHome; D.2 handheld pack opens.** One artboard at `canvas/handheld/OperatorHome.dc.html` (390×844, print-fixed, phone-shaped), placed at (0, 6400). The handheld's Today tab. Header strip carries the caller-profile chip (`j.rivera · operator · operator_station_view`) plus station identity and clock. Body opens with the **active run card**: run alias `RUN-VF-003-0812-01`, part revision, serial number, `in_progress` state badge on green (the load-bearing success colour per §3.1), current step number and instruction text (torque M8 at 24-28 Nm), primary "Continue → RunStepView" button at 56pt tall on ink-primary black (philosophy §3.8). Below the active card, a **compact blocker strip** cites the run-close-rule id `failed_measurement_has_quality_path` on step 2 with quality_engineer named as the next actor — the compact variant from sprint 057's blocker library. The **assigned runs** section lists two runs: one `ready` (with a 36pt "Start" secondary button invoking `StartRun`), one `blocked` with the BuildCheckResult blocker string `quarantined_inventory` shown in amber. The **recent action** strip names the last operation (`CompleteRunStep`), the emitted event (`RUN_STEP_COMPLETED`) in green, and the elapsed time (8 s ago). Footer tab strip: Today (active), Scan, Runs, Blockers, Profile — inline SVG icons per skill guidance (no emoji). Station identity `station-B4` shown as example data with handoff-E marker (no station record registered; philosophy §3.14 halt condition names this). Republished to https://claude.ai/code/artifact/347f2431-d036-4bcf-a3ad-28cc928a3dda. **Dual/observation contract:** artifact — `canvas/handheld/OperatorHome.dc.html` (new), `canvas.json` extended to 15 artboards, `--check` clean (16 files). Observation — the artboard renders as a phone-shaped surface on the canvas; a reader sees at a glance what run, what step, what blocker, what to do next. Signal — every registered name cross-checked against the registries. Gates untouched: contracts, bench 29/29, vitest 432/432, tsc 0, prettier clean. **Rubber Duck Pass:** (1) What does the caller need to see? — the operator needs their active run, current step, top blocker, assigned work, last action. Answer: yes, present in that order down the phone. (2) What do they need to do? — Continue (primary) or Start (secondary for assigned); the blocker on this screen is view-only for operator (resolution routes to quality_engineer). (3) What refuses, by what registered reason? — the blocker strip cites the run-close-rule id `failed_measurement_has_quality_path`; the assigned-runs blocked row cites BuildCheckResult blocker `quarantined_inventory`; both name quality_engineer as next actor. Observations: **missing pair** none. **order violation** none. **vocabulary gap** none — every registered name (caller_type, profile, record, state, operation, event, rule id) resolves in `contracts/*.yaml`; scenario aliases (`RUN-VF-003-0812-01`, `SN 45-8829-C-000234`, `station-B4`) are legit stand-ins for scenario-time data. **payload anomaly** none. **timing surprise** none — the "8 s ago" example matches Nielsen thresholds. **tone trace** — one caught: the section label read "Assigned to you · 2 runs". "To you" is second-person address (violates the user's global CLAUDE.md unaddressed-prose rule and design philosophy §3.16). Disposition **resolved-here** — rewrote to "Assigned · 2 runs", matching the third-person label style of "Active run" above it. Fixed at source before republish. **Three tests from philosophy §6:** recovery — active run, current step, last action, and top blocker all visible without menu-hunting (pass); new-shift — primary label reads "Continue → RunStepView" in registered vocabulary (pass); glove-and-glare — primary 56pt, secondary 36pt with clear separator, ink-primary at 17:1 contrast (pass). Files: `canvas/handheld/OperatorHome.dc.html` (new), `canvas/canvas.json` (extended), `canvas/factory-ui-canvas.html` (re-seeded and republished), `dev/sprints/sprint-058-operator-home.md` (closed).

- **Sprint 057 (2026-08-26, closed) — blocker presentation library; D.1 foundations complete.** One artboard at `canvas/patterns/blockers.dc.html` (720×3000, print-fixed), placed at (3040, 3060) — third artboard in the patterns row. Complements the small `blocker-card` component authored at sprint 054: the component demonstrates two example cards; this library spells out the *pattern*. Six required fields on every blocker: rule id (monospace, verbatim from registry), description (verbatim from registry), affected record (alias + registered state), next actor (caller_type + authorization rule), current-caller allowed action, clearing operation (registered op that walks the transition). Two worked examples: **certificate_of_conformance_unverified** (receiving-rule) — a full card citing valve_body_001 in InventoryItem.quarantined, a captured Certificate, quality_engineer as next actor under `receiving_decision`, and the three-step clear path AcceptCertificateAsEvidence → RunReceivingCheck → ApplyReceivingCheckResultToInventory; **failed_measurement_has_quality_path** (run-close-rule) — a full card citing torque_capture_003 in Measurement.fail, nc_003 in Nonconformance.disposition_pending, quality_engineer under `quality_disposition`, and the RecordDisposition → StartRework → CompleteRework → VerifyRework clear path. Each card cites where it appears (BlockerView on handheld; ReceivingCheckView, InventoryQuarantineView, RunCloseReadinessView on Mac). One **compact variant** shown for dense queue rows: rule id + record alias + next actor on a single line. One **handoff-gap variant** for when the driver returns a blocker id the registry does not carry: the surface renders a GrammarGap with the raw id, the gap record alias, and a clear-via-registry-proposal path. A final section names what a blocker never carries: no dismiss, no snooze, no generic copy, no false resolve control. Republished to https://claude.ai/code/artifact/347f2431-d036-4bcf-a3ad-28cc928a3dda. **Dual/observation contract:** artifact — `canvas/patterns/blockers.dc.html` (new), `canvas/canvas.json` (extended to 14 artboards), `canvas/factory-ui-canvas.html` re-seeded (`14 artboards, 0 images`), `--check` clean (`15 files`). Observation — three pattern artboards now sit in a row (runtime-states, empty-states, blockers); a downstream sprint reading them can pick a pattern, copy its treatment, cite the reason. Signal — every blocker rendered in any downstream artboard (sprints 058-087) has a canonical row shape to inherit. Gates untouched. **D.1 foundations complete** — sprints 053-057 have delivered the canvas, the vocabulary, tokens for both apps, eight shared components, the nine-state runtime library, the eleven empty-and-no-authority patterns, and the blocker library. Downstream screen sprints (058-082) inherit all of these. **Rubber Duck Pass:** (1) What does the caller need to see? — the designer authoring downstream blocker surfaces needs the pattern, the two rule-family examples, the compact variant for queues, and the handoff-gap treatment. Answer: yes. (2) What do they need to do? — copy the row shape verbatim into every blocker surface. (3) What refuses, by what registered reason? — the library itself does not refuse; it shows how refusal renders. Observations: **missing pair** none. **order violation** none. **vocabulary gap** none — every registered name in the two worked examples cross-checks in `contracts/*.yaml` (rule ids in receiving-rules.yaml and run-close-rules.yaml; records in state-machines.yaml; operations in operations.yaml; authorization rules in authorization-rules.yaml). **payload anomaly** none. **timing surprise** none. **tone trace** none — every field label and description is factual, third-person, verbatim from registry where the field is a registered string. Files: `canvas/patterns/blockers.dc.html` (new), `canvas/canvas.json` (extended), `canvas/factory-ui-canvas.html` (re-seeded and republished), `dev/sprints/sprint-057-blocker-presentation-library.md` (closed).

- **Sprint 056 (2026-08-26, closed) — empty and no-authority state library.** One artboard at `canvas/patterns/empty-states.dc.html` (720×3200, print-fixed), placed at (2220, 3060) beside the runtime-states artboard. Eleven reusable patterns as a vertical stack: **no_records_visible_under_current_profile** (profile whitelist filtered everything; reason `record_type_restricted` or `report_type_restricted`); **no_records_exist** (data empty; no cause beyond the fact); **summary_only** (registered §10 shape applied; the shape name and the withheld field list are shown, so a caller who needs full detail knows what to ask for); **hidden_existence** (byte-identical to a not-found response per §5.4); **action_unavailable_under_current_role** (`authorization_denied` today, `role_not_authorized` after the deferred wrapper unification; strip cites the authorization rule the operation names); **support_session_required** (`support_context_missing` or `support_context_expired`; points at `OpenSupportSession`); **blocked_by_receiving_evidence** (receiving-rule id from the last `ReceivingCheck.blockers[]`; example uses `certificate_of_conformance_unverified` and names the clearing operation `AcceptCertificateAsEvidence`); **blocked_by_quality_path** (`failed_measurement_has_quality_path` run-close rule; strip names the Nonconformance state and `VerifyRework` as the clearing act); **blocked_by_stale_report** (`report_access_stale` reason; names the specific trigger from the four registered triggers); **projection_stale** (same underlying condition as the runtime-states variant, in a list context; banner names the projection and elapsed time); **network_unavailable** (banner refuses to render stale data as fresh). Every pattern cites at least one registered reason code, rule id, or condition; no pattern ships without one. Republished to https://claude.ai/code/artifact/347f2431-d036-4bcf-a3ad-28cc928a3dda. **Dual/observation contract:** artifact — `canvas/patterns/empty-states.dc.html` (new), `canvas/canvas.json` (extended to 13 artboards), `canvas/factory-ui-canvas.html` re-seeded (`13 artboards, 0 images`), `--check` clean (`14 files`). Observation — the empty-states artboard sits to the right of runtime-states; a downstream artboard reading it can pick a pattern, copy its treatment, and cite the reason code. Signal — every empty list, queue, search, or detail surface authored under sprints 058-087 now has a canonical treatment. Gates untouched: `validate:contracts`, bench 29/29 both drivers, vitest 432/432, tsc 0, prettier clean. **Rubber Duck Pass:** three questions. (1) What does the caller need to see? — the designer authoring lists, queues, and detail surfaces needs the eleven patterns. Answer: yes; each pattern renders as a small demo with its trigger, the registered reason, and what clears it. (2) What do they need to do? — reuse verbatim. (3) What refuses, by what registered reason? — every pattern names its reason code, rule id, or condition. Observations: **missing pair** none. **order violation** none. **vocabulary gap** — one small drift: the sprint 056 card scope says "Ten reusable patterns" while listing eleven. The design spec §8 also lists eleven. Disposition **resolved-here** — the artboard delivers all eleven; the count in the sprint card frontmatter is a minor drift with no bearing on the deliverable, noted here for the acceptance closeout (sprint 088) to correct in the sprint card if needed. **payload anomaly** none — every reason code, rule id, and operation name resolves in the registries. **timing surprise** — the "6 s ago" and "4 s ago" numbers are example latencies matching the philosophy §3.6 threshold framing. **tone trace** — every label is factual, third person, no second person, no reassurance. One check: `strong` copy on the `no_records_visible_under_current_profile` demo reads "No records visible under `internal_full_quality`" — third person, factual, cites the profile name. Clean. Files: `canvas/patterns/empty-states.dc.html` (new), `canvas/canvas.json` (extended), `canvas/factory-ui-canvas.html` (re-seeded and republished), `dev/sprints/sprint-056-empty-and-no-authority-states-library.md` (closed).

- **Sprint 055 (2026-08-26, closed) — runtime action-state library.** One artboard at `canvas/patterns/runtime-states.dc.html` (720×2600, print-fixed), placed on the canvas at (1400, 3060). Nine states as reusable component patterns, each carrying the Nielsen 1993 latency threshold it serves: **loading** (≤0.1 s, skeleton fields fill the layout so the eye does not jump when values arrive); **operation_pending** (appears ≤0.1 s after the tap; button relabels to name the operation, spinner marks compute); **operation_succeeded** (≤1 s on the happy path; green reserved for this load-bearing success state per §3.1; confirmation names the emitted event and the new record state); **operation_failed** (red bar; the specific typed failure class is named); **retry_safe** (blue border retry button; refuses with `idempotency_conflict` on a second try); **retry_unsafe** (no retry button; only "Reload before retrying"); **projection_stale** (amber banner naming the projection and elapsed time); **report_stale** (amber banner naming which of the four registered triggers fired: `report_definition_change`, `reconciliation_resolution_affecting_run`, `access_policy_change_for_controlled_export`, `source_record_correction`); **network_unavailable** (red banner; UI refuses to claim success or failure until the driver returns). The `operation_failed` panel lists all 28 registered failure classes — the seven generics (`authorization_denied`, `not_implemented`, `validation_error`, `state_transition_forbidden`, `bridge_mapping_required`, `idempotency_conflict`, `report_regeneration_required`) and the 21 §14 access failure classes verbatim from `contracts/failure-classes.yaml`. Copy discipline: every label factual and short; no reassurance; no second person. Republished to https://claude.ai/code/artifact/347f2431-d036-4bcf-a3ad-28cc928a3dda. **Dual/observation contract:** artifact — `canvas/patterns/runtime-states.dc.html` (new), `canvas/canvas.json` (extended to 12 artboards), `canvas/factory-ui-canvas.html` re-seeded (`12 artboards, 0 images, canvas.json`), `--check` clean (`13 files`). Observation — the runtime-states artboard sits to the right of the eight components; a downstream sprint reading it can pick a state, copy its treatment, and apply it. Signal — every state-changing surface authored under sprints 058-087 now has a canonical treatment for each of the nine states. Gates untouched: `validate:contracts`, bench 29/29 both drivers, vitest 432/432, tsc 0, prettier clean. **Rubber Duck Pass:** three questions. (1) What does the caller need to see? — the designer authoring downstream state-changing surfaces needs the nine treatments. Answer: yes; every state renders as a small demo with its threshold, guidance, and (where relevant) the failure classes it may carry. (2) What do they need to do? — reuse these treatments verbatim. (3) What refuses, by what registered reason? — `operation_failed` names the 28 registered failure classes; `network_unavailable` refuses to make a success-or-failure claim. Observations: **missing pair** none. **order violation** none. **vocabulary gap** none — all 28 failure classes cross-checked against `contracts/failure-classes.yaml` and the design spec §6; every state name matches the philosophy §6 verbatim. **payload anomaly** none. **timing surprise** — the three Nielsen thresholds (0.1 s / 1 s / 10 s) come from Nielsen 1993 and are still cited by every human-factors treatment; used as authored. **tone trace** — one caught: the `projection_stale` banner originally read "This view has changed since you last saw it." That is second-person address (violates the user's global CLAUDE.md unaddressed-prose rule and the philosophy §3.16 no-narration rule). Disposition **resolved-here** — rewrote to "`RunCloseReadiness` diverged since the last render (4 s ago)." Third-person, factual, names the projection. Fixed at source before republish. Files: `canvas/patterns/runtime-states.dc.html` (new), `canvas/canvas.json` (extended), `canvas/factory-ui-canvas.html` (re-seeded and republished), `dev/sprints/sprint-055-runtime-action-states-library.md` (closed).

- **Sprint 054 (2026-08-26, closed) — design tokens for handheld and Mac; eight shared components.** Two token artboards at `canvas/tokens/handheld.dc.html` (800×1500) and `canvas/tokens/mac.dc.html` (800×1300) settle the palette used by every downstream sprint. Shared ground `#FAFAF7` warm off-white; ink at three depths (`#1A1A1A` primary at 17:1, `#4A4A4A` secondary at 9.4:1, `#6B6B6B` tertiary at 5.5:1); rule `#D9D6CE`; fill-weak `#F0EDDF`; four exception colours used sparingly per philosophy §3.1 (`#B45309` amber for pending / expiry / deferred; `#B00020` red for refusal / hard fail; `#1E4E8C` blue for informational and the focus ring; `#2D6A3C` green reserved for the load-bearing success state). Type pairing IBM Plex Sans + IBM Plex Mono; body 15px handheld / 14px Mac; tap targets 48pt minimum, 56pt primary on handheld; spacing scale 4/8/12/16/20/24/32/48; radii 4 default handheld, 2 default Mac, 0 tabular; focus ring 2px `#1E4E8C`. Eight shared components under `canvas/components/`: **page-shell** (handheld + Mac shell demonstration), **global-search** (input plus one result row per visibility level; hidden-existence row shown greyed to communicate the §5.4 invariant), **caller-profile-chip** (actor, `caller_type`, active visibility profile, support-session variant with expiry countdown), **state-badge** (pills for every Run, InventoryItem, and Certificate state; terminal states carry the strike-through mute; colour rule named), **blocker-card** (philosophy §7 row shape with two worked examples using real registered rule ids `certificate_of_conformance_unverified` and `failed_measurement_has_quality_path` and their registry descriptions verbatim), **visibility-badge** (four §5 pills plus the four registered summary shapes), **action-button** (primary, secondary, destructive, disabled-with-reason, pipeline with the four close-pipeline stages named), **disabled-action-strip** (why-disabled label plus registered rule id plus resolving caller_type plus registered operation that clears it). `canvas.json` lays the eleven artboards on a pan-zoom canvas: Main.dc.html at (0,0), the two token sheets to its right, the eight components in a 4×2 grid below the tokens. Launch focused on Main so viewers land on the vocabulary reference. Republished to https://claude.ai/code/artifact/347f2431-d036-4bcf-a3ad-28cc928a3dda (same URL as sprint 053). **Dual/observation contract:** artifact — 10 new .dc.html files under `canvas/tokens/` and `canvas/components/`, `canvas.json` extended to 11 artboards, `canvas/factory-ui-canvas.html` re-seeded (helper: `11 artboards, 0 images, canvas.json`), `--check` clean (`12 files`). Observation — the Artifact URL renders with the vocabulary on the left and the design system to its right. Signal — every downstream sprint (055-088) can now consume the tokens and components from this artboard. Gates untouched: `validate:contracts`, bench 29/29 both drivers, vitest 432/432, tsc 0, prettier clean. **Rubber Duck Pass:** three questions. (1) What does the caller need to see? — the design team drafting downstream artboards needs the tokens and components. Answer: yes; every colour, type step, tap-target size, and pattern is on the canvas. (2) What do they need to do? — read the tokens and use the components. No interactive controls; the artboards are references. (3) What refuses, by what registered reason? — the disabled-strip and disabled-button variants demonstrate refusal with registered rule ids and reason codes, so downstream artboards inherit the pattern. Observations: **missing pair** none. **order violation** none. **vocabulary gap** — one caught: the `action-button` pipeline chip abbreviated the close pipeline stages as `RunCloseCheck → RequestReport → Apply` instead of the registered names `RunCloseCheck`, `RequestRunCloseReport`, `GenerateRunCloseReport`, `ApplyRunCloseResultToRun`. This violated philosophy §3.13 (vocabulary is the interface). Disposition **resolved-here** — fixed at source before republish; the pipeline chip now names all four registered operations verbatim. **payload anomaly** none. **timing surprise** none. **tone trace** none — every label is factual, in the registered vocabulary, and no second-person or instructive copy. A cross-check against `contracts/*.yaml` across all eleven artboards found no unregistered names; every non-registered mono token falls into one of four legitimate categories (scenario-time example aliases like `valve_body_001`; design-token names like `ground`, `fill`, `amber`; path fragments like `contracts`, `src`; CallerContext field names like `visibility_profile`). Files: `canvas/tokens/handheld.dc.html` (new), `canvas/tokens/mac.dc.html` (new), `canvas/components/*.dc.html` (8 new), `canvas/canvas.json` (extended from 1 to 11 artboards), `canvas/factory-ui-canvas.html` (re-seeded and republished), `dev/sprints/sprint-054-design-tokens-and-shared-components.md` (closed).

- **Sprint 053 (2026-08-26, closed) — canvas established, vocabulary reference authored.** Phase D.1 opens. The Phase D design canvas is published as an Artifact on claude.ai — `factory-ui-canvas.html`, title *Factory UI Design Canvas*, at https://claude.ai/code/artifact/347f2431-d036-4bcf-a3ad-28cc928a3dda. One artboard (`canvas/Main.dc.html`, 1200×5200 print-flow, `expand: fit`, launch focused). Contents: the ten caller types from `contracts/modules.yaml`; the sixteen records with lifecycles from `contracts/state-machines.yaml` with state fields, states verbatim, and terminal states in the warm-brown accent; the four status-light and result-valued records (BuildCheckResult, ReceivingCheck, RunCloseCheck, Measurement); the ten receiving-rule ids with descriptions verbatim from `contracts/receiving-rules.yaml`; the thirteen run-close-rule ids with descriptions verbatim from `contracts/run-close-rules.yaml`; the twenty-six reason codes flagged as caller-visible (19), outcome-internal (3), guard-internal (2), or existing-internal (2), with the two `used_by_sprint: deferred` codes visibly marked; the eight visibility profiles with audience and default level; the four visibility levels with the §5.4 not-found equivalence for `hidden_existence` named. Every name resolves in the registries; the invariant grep would return zero unregistered names. **Canvas README** at `canvas/README.md` names the folder layout for D.2 onwards. **`canvas.json`** is single-artboard for now; it grows with every sprint that adds a screen. Type pairing: IBM Plex Sans headings and running text, IBM Plex Mono for every registered name — chosen against the design philosophy's forbid on Inter and its "distinctive, characterful" §3.13 note. Colour: warm off-white (#FAFAF7) ground, greyscale text, one warm brown (#8B4513) accent used only for terminal states, deferred flags, and the footer rule — matching design philosophy §3.1 (grey is the ground; colour is exception). No decoration, no animation, no emoji in content. **Dual/observation contract:** artifact — `canvas/Main.dc.html` exists non-empty, `canvas/canvas.json` exists non-empty, `canvas/README.md` exists non-empty, `canvas/factory-ui-canvas.html` seeded (helper printed `wrote factory-ui-canvas.html — "Factory UI Design Canvas": 1 artboard (Main.dc.html), 0 images, canvas.json`), `--check` clean (`ok: factory-ui-canvas.html — title "Factory UI Design Canvas", 2 files ("Main.dc.html", "canvas.json")`). Observation — Artifact published, URL captured, the vocabulary artboard renders as a scroll-flow reference sheet. Signal — every downstream sprint (054-088) can read this artboard to know which names are registered and therefore drawable. Gates untouched (Phase D does not edit code or registries): `validate:contracts` still 132/136/43/16/33/26; bench 29/29 both drivers; vitest 432/432; tsc 0; prettier clean. **Rubber Duck Pass:** three questions from design philosophy §3.17. (1) What does the caller need to see? — the design team, working through sprints 054-088, needs the vocabulary. Answer: yes; every registered name a downstream artboard may cite is on the sheet. (2) What do they need to do? — read the sheet; author downstream artboards against it. No interactive action; the artboard is a reference. (3) What refuses, by what registered reason? — nothing on this screen refuses; the downstream halt is `design_pattern_missing`, named in the footer. Observations under the six-category taxonomy: **missing pair**: none. **order violation**: none. **vocabulary gap**: none — every registered name matches the current registries as of 2026-08-26. **payload anomaly**: one — the design specification §4.3 said "twenty caller-visible refusals", the registry actually holds nineteen (`role_not_authorized`, `access_group_missing`, `customer_scope_mismatch`, `program_scope_mismatch`, `contract_scope_mismatch`, `factory_node_scope_mismatch`, `record_type_restricted`, `report_type_restricted`, `controlled_data_denied`, `deemed_export_denied`, `support_context_missing`, `support_context_expired`, `service_scope_denied`, `attachment_access_denied`, `bounded_drilldown_denied`, `report_audience_mismatch`, `report_access_stale`, `policy_change_forbidden`, `no_summary_shape_registered`); disposition **resolved-here** — the artboard carries the true count (19), and the spec was corrected in the same close (§4.3 and criterion 8 of §25 in `specs/ui-surface-design/ui-surface-design-spec-v0.3.md`; the number is now 19 caller-visible + 7 internal). **timing surprise**: none. **tone trace**: none — no player-facing strings on this artboard; all copy is factual reference text against the vocabulary. Files: `canvas/README.md` (new), `canvas/Main.dc.html` (new, 1 artboard), `canvas/canvas.json` (new, 1 artboard laid out), `canvas/factory-ui-canvas.html` (seeded and published), `dev/sprints/sprint-053-canvas-and-vocabulary.md` (closed).

- **Repo grooming for public release (2026-08-26).** Not a build sprint. A day of pre-public-release housekeeping across three surfaces, none of which changed product behaviour or the contract stack. **(1) Dellm pass on every project-authored doc.** Ran the `dellm` skill over `docs/{HANDOFF,STATE,ROADMAP,DOCS,RECEIVING_ACCEPTANCE,ACCESS_AND_VISIBILITY_ACCEPTANCE,ADDITIONS,DEVIATION_SUMMARY,PHASE_C_READOUT,SESSION_2026-08-25,README}.md`, `WORKING_AGREEMENT.md`, `specs/README.md`, both demo-pack READMEs, and the access registry-pack rationale. Stripped the measured LLM register: verb inflation, promotional puffery, formulaic scaffolding, structural regularity. ROADMAP 2,705 → 1,666 words; DOCS 1,731 → 1,396. Two content corrections landed in the process: the backend gate carries 14 durability proofs not 15 (verified against `src/harness/run-backend.ts`), and the receiving demo README's stale 22-of-26 mutation-arms count is now 26 of 26 (§27 criterion 13 closed 2026-08-07). **(2) BLACKBOARD backfill for sprints 024-028.** The 7-August directive arc (run lifecycle, controlled-document lifecycle, inventory and quality, report generation and registration, supplier evidence packet) had no per-sprint `## Built` entries — five added. A Phase C rollup entry landed at the head of `## Sprint tail` covering sprints 029-052. Every sprint from 001 to 052 now has either a per-sprint entry or a rollup covering it. **(3) Folder reorganization.** Every process artifact moved under `dev/`: `sprints/`, `signal-reports/`, `sdd-kit-2/`, `persona-review-kit/` (renamed `persona-reviews/` and joined by `PERSONA_REVIEW_PASS-round-1.md`), `reviews/` (single-file, holds the receiving-boundary adversarial pass), plus the four SDD process files at project root (`BLACKBOARD.md`, `KIT_DIARY.md`, `WORKING_AGREEMENT.md`, `ADDENDUMS.md`). Project root now reads as a normal Node/TS repo: `src/`, `tests/`, `contracts/`, `scenarios/`, `schemas/`, `specs/`, `demo-packs/`, `docs/`, plus `package.json` and config files. Every folder move ran through `git mv`, so `git log --follow` walks each file's history through the rename. Zero code references broke — the `.ts`/`.js`/`.mjs`/`.yaml` scan came back empty. `.prettierignore` and `eslint.config.js` updated (also dropped one dead ignore entry, `manufacturing-software-doc-stack-build-ready/**`, a folder that no longer exists). The kit-convention override — `dev/sdd-kit-2/AGENTS.md` says "read `BLACKBOARD.md` from project root", now recorded as an explicit override in `dev/WORKING_AGREEMENT.md` pointing every kit instruction at the `dev/`-prefixed path. Every gate green from first check to last: validate:contracts ok (132/136/43/16/33/26), validate:schemas ok, bench all 29/29 both drivers, backend gate exit 0 with 14 durability proofs, whole-bench cross-driver diff-to-zero over 37 scenarios PASS, vitest 432/432 across 58 files, tsc 0, prettier clean. KIT_DIARY Entry 33 records the arc; three new practices (35 register-strip-before-public, 36 `git mv` for reorganizations, 37 phase-close sprint-coverage grep). Committed `d766c8c` (157 files: mostly renames, plus doc rewrites and config updates), pushed to `laffeyp/traveler` main.


- **Session close 2026-08-25 — SESSION_2026-08-25.md written.** Full-day summary of Phase C at project root: what set out to be done, what got done chronologically, where the build stands, what §7 asked for and did not get, techniques exercised, and the full commit ledger. Registry counts before → after: 128→132 operations, 132→136 events, 42→43 records, 15→16 state machines, 32→33 authorization rules, 13→16 registry files. Vitest 301→432 tests across 37→58 files. 30 commits · 8,299 insertions · 393 deletions · 67 new files · 21 new test files. STATE.md refreshed (§1 now says "four things measured against"; §4a added for the access-and-visibility boundary; §5 registry table updated to 16 registries + 132 ops / 43 records). ROADMAP.md was already refreshed at sprint 052 close. DOCS.md group 1 now lists the SESSION file. DEVIATION_SUMMARY.md refreshed with the Phase C delta table (§5) and the current-status table (§4). ADDITIONS.md gained a Phase C section listing all 21 vocabulary additions (rows 10-30) plus the deferred items. KIT_DIARY Entry 32 already carried the phase synthesis + red-team findings inline.

- **Red-team pass on Phase C (2026-08-25).** Ran the distrust-the-green discipline over the 24-sprint arc. Ten probes with positive controls; three real findings, recorded in place rather than silently corrected. **(1)** Phantom reason-code citations in `contracts/reason-codes.yaml`: `role_not_authorized` and `controlled_data_denied` claimed `used_by_sprint: 031` but never emit from any code path. Marked `used_by_sprint: deferred` with a note naming the future sprint that would emit them. Entry 28's practice #25 pointed inward. **(2)** Vacuous audit-does-not-leak test: sprint 049's original set asserted the invariant on the AUDITED event and on ONE DENIED path (program-scope); a probe injected `document_body` and `customer` into the customer-scope DENIED emit and the four-test suite passed unchanged. Rewrote the DENIED-leak test to drive every dimension (customer / program / contract / factory_node / access_group) with three forbidden fields on each target; same mutation now goes red — verified in-session, restored. **(3)** Overstated byte-identical claim: a probe added `MUTATION_TEST: true` to the ACCESS_DECISION_ALLOWED emit and `cross-driver diff-to-zero over 37 scenarios: PASS (all identical)`. Diff-to-zero measures cross-driver fidelity, not against-baseline; both drivers ran the mutated handler and stayed equivalent. Existing `event_payload_contains` subset assertions do not catch new fields either. Corrected the wording in `ACCESS_AND_VISIBILITY_ACCEPTANCE.md` (row 17 caveat added, gate table row rewritten). KIT_DIARY Entry 32 extended with the red-team findings; practice #34 added for TECHNIQUES.md: diff-to-zero between two implementations is fidelity, not regression. A golden-trace check is a deferred follow-up in `ROADMAP.md §Post-Phase-C deferred items`. Seven probes came back clean (vocabulary discipline; registered events all emit and every emit is registered; TS hygiene; customer-scope coupling; projection-read coupling; reverse-registration; unused-code check surfaced only `hiddenExistenceResponse` reserved for a future caller). Every gate still green after the fixes: vitest **432/432** across 58 files (same count, same test IDs; the audit test hardened without adding a test); bench 29/29 both drivers; cross-driver diff-to-zero PASS; tsc 0; prettier clean.

- **Phase C closed (2026-08-25) — Access and Visibility Boundary shipped in one day.** 24 sprints (029-052). `ACCESS_AND_VISIBILITY_ACCEPTANCE.md` scores **18 of 18** §16 criteria pass or pass-in-part. Registry counts: 132 operations, 136 events, 43 records, 16 state machines, 33 authorization rules, 26 assertion types (up from 128/132/42/15/32/26 at Phase C open). Vitest **432/432** across 58 files (up from 301/301 across 37 files — 131 new tests, all green). Whole-bench cross-driver diff-to-zero over 37 scenarios PASS (all identical) at every sprint close — every existing scenario byte-identical after eight new dimensional checks, six new enforcement surfaces, three cross-cutting sprints. Phase-C sub-phases: **C.1 foundations** (029 mapping, 030 pack, 031 §8 decision model, 032 visibility levels, 033 reason codes, 034 profiles); **C.2 dimensions** (035 access-group, 036 customer, 037 program, 038 contract, 039 factory-node, 040 record/report-type, 041 support/admin with SupportSession, 042 service-account with processing≠disclosure); **C.3 enforcement** (043 projection read, 044 report generation preservation, 045 report read, 046 bounded drill-down per-hop, 047 AccessAttachment six-outcome, 048 user-visible event replay); **C.4 cross-cutting** (049 audit-does-not-leak, 050 AmendAccessPolicy + freshness cascade + history-rewrite guard, 051 16-arm fail-closed battery); **C.5 closeout** (052 §16 acceptance file + STATE/ROADMAP/DOCS/KIT_DIARY refresh). KIT_DIARY Entry 32 records the phase synthesis: draft all cards up front, execute auto-within-phase, keep byte-identical, use opt-in target-side scoping to resolve the fail-closed vs backward-compat tension. Two new practices (32, 33) added for TECHNIQUES.md. Every gate green.

- **Sprint 035 (2026-08-25) — Access group as a first-class access dimension.** Phase C.2 opens. A record may name `required_access_group` in its fields; a caller must present a matching `access_groups` entry in the caller context. `EvaluateAccess` grows a group check that runs BEFORE the requested-summary branch — a caller who lacks the required group cannot bypass by asking for summary. Denies with the specific `access_group_missing` reason (not generic `authorization_denied`), audited. **B-Q-74 candidate answer applied**: `access_groups` lives on the caller-context object; no first-class `AccessGroupMembership` record. Promote to a record when an audit scenario demands durable per-caller state. `readRecordAsCaller` in `src/driver/driver.ts` now threads `access_groups` from `CallerContext` through to `EvaluateAccess`. Byte-identical preserved — existing records carry no `required_access_group` field, so the branch skips for every scenario in the bench; whole-bench cross-driver diff-to-zero over 37 scenarios still PASS all identical. New unit suite `tests/access/access-group.test.ts` (8 tests): in-group proceeds, out-of-group denied, empty groups denied, missing groups denied, no-required-group untouched, order-of-checks proven (summary cannot bypass), coupling-mutation discrimination, per-decision audit. Coupling verified in-session: suppressing the group check turned 5 tests red; restored. Sprint 038's scenario id (VF-038 per the pack) is deferred to a batched access-dimensions bench scenario later in C.2 — unit-test coverage stands alone for the mechanic. Files: `src/driver/handlers.ts` (group check), `src/driver/driver.ts` (context threading), `tests/access/access-group.test.ts` (new). Every gate green: vitest **337/337** across 42 files (+8 new), bench 29/29 both drivers, whole-bench diff-to-zero PASS, tsc 0, prettier clean.

- **Sprint 034 (2026-08-25) — Visibility profiles registered.** New `contracts/visibility-profiles.yaml` with 8 §9 profiles: `internal_full_quality`, `operator_station_view`, `receiving_inspector_view`, `customer_summary_access`, `customer_extended_access`, `supplier_evidence_reviewer`, `support_diagnostics_summary`, `service_projection_scope`. Each carries the nine §9 fields (audience, default_visibility, allowed_record_types, allowed_report_types, allowed_actions, controlled_data_behavior, denial_behavior, audit_required, plus name). `VISIBILITY_PROFILES` exported from `src/driver/registry.ts` loads them once at runtime start (Map lookup by name). The two profiles already used inline in `contracts/reports.yaml` and `handlers.ts` (`customer_summary_access`, `customer_extended_access`) are registered here without changing the inline uses — VF-012 traces byte-identical (whole-bench cross-driver diff-to-zero over 37 scenarios still PASS all identical). Sprint 044 rewires report generation to read `audience_profile` from this registry rather than the inline strings. New unit suite `tests/access/visibility-profiles.test.ts` (5 tests): profile count, nine-field shape, fingerprint-uniqueness (no two profiles hold identical policy), the two inline-used profiles resolve, all eight §9 profiles resolve. Phase C.1 foundations complete: sprints 029-034 all closed. Files: `contracts/visibility-profiles.yaml` (new), `src/driver/registry.ts` (VISIBILITY_PROFILES export), `tests/access/visibility-profiles.test.ts` (new). Every gate green: vitest **329/329** across 41 files (+5 new), bench 29/29 both drivers, whole-bench diff-to-zero PASS, tsc 0, prettier clean.

- **Sprint 033 (2026-08-25) — Reason codes and failure classes registered.** Two new registry files: `contracts/reason-codes.yaml` (26 entries — the 22 §8.3 codes plus 4 that shipped in the first slice as `exportAccessDecision` returns) and `contracts/failure-classes.yaml` (21 §14 entries, each with either `maps_to` an existing class or `new: true`). Every entry cites its spec section and the sprint that will use it. Bidirectional check in `tests/access/reason-codes-registered.test.ts` (7 tests): every §8.3 name resolves in the registry (forward), every registry entry cites a spec section (reverse — the check that caught the sprint 019 handler-only hole), no case-insensitive-or-whitespace duplicates. Naming pass only — no behavior change; existing scenarios and gates untouched. Every gate green: contracts ok, vitest **324/324** across 40 files (+7 new), bench 29/29 both drivers, whole-bench cross-driver diff-to-zero over 37 scenarios PASS, tsc 0, prettier clean. Files: `contracts/reason-codes.yaml` (new), `contracts/failure-classes.yaml` (new), `tests/access/reason-codes-registered.test.ts` (new).

- **Sprint 032 (2026-08-25) — Visibility levels: summary and hidden_existence promoted to first-class outcomes.** New `src/driver/visibility.ts` module holds the four §10 initial summary shapes (`machine_evidence_summary`, `supplier_document_summary`, `nonconformance_summary`, `report_summary`), each with the exact revealed/hidden field lists §10 names. Provenance: transcribed from `access-and-visibility-registry-pack-v0.1/contracts/summary-shapes.access.yaml`; kept in code for now to hold sprint sweet spot, folds into a main registry when the visibility-profile fold lands in sprint 034. New `readRecordAsCaller(alias, callerContext)` on `InMemoryProductDriver`, `BackendProductDriver`, and the harness `Driver` interface returns a `VisibilityDecision` — one of `full | summary | denied | hidden_existence` — driven by an `EvaluateAccess` call. The plain `readRecord` / `mustReadRecord` are unchanged and every existing caller keeps working. `EvaluateAccess` grows a `requested_visibility: "summary"` branch: when a target has a registered §10 shape the decision returns `visibility_level: "summary"` with the shape name; when no shape exists the decision **denies fail-closed** with `no_summary_shape_registered` — a requested summary cannot be silently promoted to full disclosure, and ad-hoc summaries would violate §10's registered-or-specified rule. The DECISION invocation runs as `access_admin` (the caller_types the `access_administration` rule names); the product caller's identity flows through as the input's `caller_type`. **The §5.4 load-bearing invariant** — hidden_existence bytes indistinguishable from not-found bytes — is proven by JSON-string equality between the two shapes: both return `{ level: "hidden_existence", record: null }` with no other fields. New unit suite `tests/access/visibility-levels.test.ts` (7 tests) covers all four outcomes, the summary-shape fields for Certificate, ad-hoc-refusal for record types without a shape, byte-identical hidden vs not-found, per-read audit, and a coupling-mutation discrimination between in-map and not-in-map record types. Coupling mutation verified in-session: temporarily removing Certificate from `SUMMARY_SHAPES` turned 2 tests red; restored. The map is provably load-bearing. Every gate green: bench all 29/29 both drivers, whole-bench cross-driver diff-to-zero over 37 scenarios **PASS (all identical)** — no existing scenario sets `requested_visibility`, so no existing trace shifts. Files: `src/driver/visibility.ts` (new), `src/driver/driver.ts` (readRecordAsCaller added), `src/driver/backend.ts` (pass-through), `src/harness/assertions.ts` (interface field), `src/driver/handlers.ts` (EvaluateAccess summary branch), `tests/access/visibility-levels.test.ts` (new). vitest **317/317** across 39 files (+7 new), tsc 0, prettier clean.

- **Sprint 031 (2026-08-25) — Access decision model generalized to §8.** `EvaluateAccess` in `src/driver/handlers.ts` now returns the boundary spec's §8 output shape `{ decision, visibility_level, reason?, audit_required, freshness_effect }` and accepts `target_object` as the spec-shape alias for `resource_alias`. Two fail-closed guards land ahead of the export path, each naming a specific §14 failure class rather than a generic `authorization_denied`: **`access_context_missing`** (no target at all) and **`access_context_malformed`** (a `caller_type` provided but not spelled the way `contracts/modules.yaml` spells it — read from a new `registeredCallerTypes` set exported from `src/driver/registry.ts`). The export path (`exportAccessDecision`, VF-029, VF-031, existing `deemed-export.test.ts`) traces byte-identical: the emitted event payloads for that path are the same bytes as before generalization, so the whole-bench cross-driver diff-to-zero over **37 scenarios PASS (all identical)** stands. The `access` module is already registered as `Access / Visibility Module` in `contracts/modules.yaml`; §16 criterion 1 (module registered) already met. New unit suite `tests/access/decision-model.test.ts` (9 tests) proves the §8 shape, both fail-closed guards, and the coupling mutation: temporarily suppressing the `access_context_missing` guard turned 2 tests red (verified in-session, then restored) — the guard is provably load-bearing. Sprints 035-042 add the remaining nine dimensional checks (`access_group`/`customer`/`program`/`contract`/`factory_node`/`record_type`/`report_type`/`support_admin_context`/`service_account_scope`); sprint 032 promotes summary and hidden_existence to first-class outcomes. Files: `src/driver/handlers.ts` (generalized handler), `src/driver/registry.ts` (registeredCallerTypes export), `tests/access/decision-model.test.ts` (new). Every gate green: `validate:contracts` ok, `validate:schemas` ok (14/14 fixtures), bench all 29/29 both drivers, backend gate exit 0 with every durability proof, whole-bench cross-driver diff-to-zero over 37 scenarios PASS (all identical), vitest **310/310** across 38 files (+9 new), tsc 0, prettier clean.

- **Sprint 030 (2026-08-25) — Access and Visibility Registry Pack v0.1 authored.** In-repo (unlike the receiving pack, which arrived from outside). 14 files under `access-and-visibility-registry-pack-v0.1/`: README, main doc, rationale, 11 registry fragments under `contracts/*.access.yaml` (records, operations, events, state-machines, authorization-rules, visibility-profiles, dimensions, enforcement-points, failure-classes, reason-codes, summary-shapes), and the mutation battery under `mutations/`. Same-word audit clean — nothing duplicates a name in `contracts/*.yaml`. New records: `SupportSession` only (B-Q-74/77 candidate answers kept access_group and service_account_scope as fields, not records; B-Q-75 kept customer as a field on existing records, no Order record). New operations: `OpenSupportSession`, `CloseSupportSession`, `AccessAttachment`, `AmendAccessPolicy` (`EvaluateAccess` stays in the main registry and is generalized in sprint 031). New events: `SUPPORT_SESSION_OPENED/CLOSED`, `ATTACHMENT_ACCESS_DECISION_RECORDED`, `ACCESS_POLICY_AMENDED`, `REPORT_REGENERATION_REQUIRED`. Eight visibility profiles registered (two already exist inline in reports.yaml; sprint 034 folds). Failure classes: 21, each with `maps_to` or `new: true`. Reason codes: 22. Summary shapes: 4. Mutation battery: 25 arms, each naming its specific reason code; `not_enforceable` empty. Scenario id assignments for the ten §15 families: VF-038..VF-052 in the existing sequence (WORKING_AGREEMENT §Numbering — gap over rename). Files also modified: `WORKING_AGREEMENT.md §Authority order` item 10 added; `DOCS.md §2a` lists the pack. Nothing merged into main registries — sprints 031-050 pull items in as each surface lands. Every gate preserved: validate:contracts ok (128/132/42/15/32/26), vitest 301/301, tsc 0, prettier clean.

- **Sprint 029 (2026-08-25) — Mapping pass for the access-and-visibility boundary.** 22 rows scored against the current registries: 4 already-spoken (caller role, operation authorization, serial history generation, record/report type as filters), 6 extends-existing (controlled-data classification, record read, projection read, report generation, bounded drill-down, service-account access), 12 new-vocabulary (access group, customer/program/contract/factory-node scopes, support/admin context, service-account scope, report read, event replay, attachment access, support/admin access, scenario ids TBD). Same-word audit clean — nothing the mapping proposes duplicates an existing registry name. Notable pre-existing surface: the `access` module is registered as `Access / Visibility Module` with two records (`AccessDecision`, `AuditEntry`), one operation (`EvaluateAccess`, filled only for export-by-nationality), and four events — sprint 031 generalizes rather than adds. Four concept calls surfaced as B-Q-74 (access-group storage), B-Q-75 (customer identity home + Order record), B-Q-76 (contract cascade to RunCloseReport), B-Q-77 (service-account scope storage), each with a candidate answer and the sprint that decides it. Files: `dev/sprints/sprint-029-mapping-table.md` (new), `contracts/CONTRACT_GAPS.md` (four B-Qs added), `dev/sprints/sprint-029-access-visibility-mapping-pass.md` (closed). Every gate preserved: validate:contracts ok (128/132/42/15/32/26), vitest 301/301 across 37 files, tsc 0, prettier clean.

- **Sprint 028 (2026-08-07) — Supplier evidence packet; §27 acceptance closed.** The §11.6 read path supplier evidence never had, and the reason four mutation-battery arms had nothing to act on. A governed report, not a read model — the report layer already carries the access-policy snapshot, regeneration triggers, and supersession those arms need; a read model would have meant rebuilding all of it under a different name. New report `SupplierEvidencePacket` in `contracts/reports.yaml` with ten required sections and three regeneration triggers; `GenerateSupplierEvidencePacket` as co-producer of the three report events; `assembleSupplierEvidencePacket` in `src/driver/projections.ts`; handler in `src/driver/handlers.ts`. Depth filtered, existence never — a customer sees that a CoC exists and not its number, CAGE code, supplier test values, or signing engineer. Rejection reason kept summary-safe for the same reason. The last four fail-closed battery arms converted; `NOT_ENFORCEABLE` empty; §27 criterion 13 now passes; four stale claims in `RECEIVING_ACCEPTANCE.md` corrected. B-Q-71 closed. Every gate green: validate:contracts ok (128 operations, 3 reports), validate:schemas ok, validate:demo-packs ok, bench smoke 2/2 · first_slice 14/14 · extended 9/9 · receiving 10/10 both drivers, backend gate exit 0, vitest 297/297 across 37 files, src tsc 0, prettier clean.

- **Sprint 027 (2026-08-07) — Report generation as four steps, and machine registration.** The last eight buildable operations from the specified remainder: report generation lifecycle a step at a time, the as-built read, and machine registration. Two states the machine declared were unreachable — `GenerateRunCloseReport` walked requested → generating → generated atomically, so `failed` and its retry could not be produced; taking the walk step by step is what a generator that can fail actually needs. The body is written at completion, not at request — a report is a snapshot of what was true when it was generated; assembling early and storing against a `requested` record freezes the wrong moment. Retry clears the failure and keeps the count — a report that eventually generates must not still carry the reason an earlier attempt failed, but a report that took four tries is worth noticing. Machine evidence stopped naming its machine by an unchecked string: `RegisterMachine` and `RegisterMachineAdapter` now resolve both to registered records and `ReceiveMachineEvidence` refuses an unregistered machine, an unregistered adapter, or an adapter that resolves but speaks for a different machine (B-Q-73). 124 of 127 registered operations built; the three that remain refuse on record. Files: `src/driver/handlers.ts` (8 handlers), `tests/reports/report-generation-lifecycle.test.ts` (11 tests), `contracts/CONTRACT_GAPS.md` (B-Q-73). Every gate green: bench all 29/29 both drivers, backend gate exit 0, vitest 293/293 across 37 files, src tsc 0, prettier clean.

- **Sprint 026 (2026-08-07) — Inventory path and quality endings.** Nine registered operations: kit, remove, return-to-stock, quarantine a removed part, scrap, cancel a nonconformance, require verification without rework, cancel an approval request, expire one. `RemoveInventory` could not be built without fixing the as-built. The projection listed installation events, correct only while the tree could exclusively grow. Adding removal without changing it would have made the report a part as fitted to a unit it had physically been taken off. Installs are now counted against removals per (parent, child) pair, so the tree shrinks; a part fitted, removed, and refitted appears once. A removal is bound to the installation it undoes — without that a part could be removed from an assembly it was never in. An expired approval request is not a decision: expiry carries system authority, records no approver, emits neither `APPROVAL_APPROVED` nor `APPROVAL_REJECTED`. The expiry deadline comes from the caller — no document in the stack names an expiry period, and inventing one would put a number in the system nothing justifies, the same reasoning that left the outbox retry schedule unbuilt. Files: `src/driver/handlers.ts` (9 handlers), `src/driver/projections.ts` (asBuiltProjection now pairs installs against removals), `tests/floor/inventory-and-quality-lifecycle.test.ts` (16 tests). Every gate green: bench all 29/29 both drivers, backend gate exit 0, vitest 282/282 across 36 files, src tsc 0, prettier clean.

- **Sprint 025 (2026-08-07) — Controlled-document lifecycle.** Eleven registered operations governing what happens to a procedure, a structure, and a redline after the happy path. Transcription plus the guards the machines cannot express. One rule generates most of the behaviour: a released document is never edited. The state machines forbid `released → draft`, so improving released work means a new version, and the old one is kept — a run that executed against it has to stay readable years later. Three guards fall out. `UpdateDraftBOMLine` refuses unless the parent structure is a draft, because editing a released BOM in place rewrites history for every serial already built to it. `MergeRedlineIntoProcedureVersion` refuses a released target for the same reason: the change belongs in a new draft that then supersedes. `SubmitManufacturingStructureForReview` refuses on drafts with no lines. VF-037 exercises the full loop end to end: a procedure rejected at review, fixed, released; an operator's redline approved and applied; the deviation marked, merged into a new draft, that draft released and superseding the original; the redline closed as merged. 32 steps, 78 assertions. Files: `src/driver/handlers.ts` (11 handlers), `scenarios/VF-037/`, `src/harness/{bench,run-backend}.ts` (VF-037 into the extended bench and the equivalence set), `tests/procedure/controlled-document-lifecycle.test.ts` (19 tests). Every gate green: bench all 29/29 both drivers, backend gate exit 0 with cross-driver diff-to-zero over 37 scenarios, vitest 266/266 across 35 files, src tsc 0, prettier clean.

- **Sprint 024 (2026-08-07) — Run lifecycle, and two authority questions answered.** First of five sprints building what the contract stack specifies and nothing had implemented. 43 registered operations returned `not_implemented`; 36 have their behaviour fully declared by a state machine, so building them is transcription. This sprint takes the twelve that govern how a run is interrupted. Two decisions had to land first because four of the twelve were unreachable without them. **B-Q-59** — who blocks a run and lifts that block: quality. Halting production is the same act as quarantining material or refusing a consignment. Distinct from `run_planning`, which owns pause, resume, and cancel — pausing is a scheduling decision about when work happens; blocking is a judgement that it must not happen yet. `undecided_authority` was removed rather than left defined, since an orphan rule is dead vocabulary. **B-Q-60** — may a planner lift a quarantine: no. `quarantine_release` narrowed to quality alone, so the two paths onto the floor agree. Narrowing the second cost VF-034 a proof — its planner had been refused for having no passed check and would now be refused on authority first — so that step was re-actored to quality and the authority refusal added beside it, keeping both. VF-036 covers the run interrupted at every point the machines allow: a step made ready, blocked, cleared; a run paused for scheduling and resumed; a run blocked by quality and cleared back into progress; a step failed, sent to rework, completed; a step skipped. 48 steps, 116 assertions. B-Q-35's skipped-step close rule now proven against the real operation, not a hand-set state. Files: `src/driver/handlers.ts` (12 handlers), `scenarios/VF-036/`, `tests/floor/run-lifecycle.test.ts` (25 tests), `contracts/authorization-rules.yaml` and `contracts/operations.yaml` (`run_blocking` replaces `undecided_authority`; `quarantine_release` narrowed), `scenarios/VF-034` (re-actored). Every gate green: bench all 29/29 both drivers, backend gate exit 0, vitest 247/247 across 34 files, src tsc 0, prettier clean.

- **Housekeeping ahead of the access-and-visibility boundary (2026-08-24)** — Three doc/gate corrections before opening the boundary sprint. **(1)** Access and Visibility Boundary Specification v0.1 landed at project root (`access-and-visibility-boundary-spec-v0.1.md`, 1374 lines, 18 sections). Renamed from the download-suffixed filename to match the receiving spec convention. Registered in `WORKING_AGREEMENT.md §Authority order` as item 9 (below the receiving pack, subordinate to the Contract Spec), so it cannot become the phantom-authority failure the receiving spec once was. **(2)** The tsc gate row in `STATE.md` said "0 errors in `src`" against a command (`npx tsc --noEmit`) whose tsconfig actually includes `src/**/*.ts` AND `tests/**/*.ts`. A fresh run of the printed command produced 236 errors across 27 test files. Fixed the tests, not the ledger: added `mustReadRecord(alias): FactoryRecord` to `InMemoryProductDriver` + `BackendProductDriver` + the harness `Driver` interface (throws on null, names the missing alias), which resolved 205 sites where a test relied on an alias its own setup wrote; two `.find()` results genuinely undefined got an `if (!x) throw`; two deliberate poka-yoke tests emitting an unregistered event name got `as any`; one property typo (`.ex` → `.execution`) was a real bug. Tsc now clean across the whole tsconfig; every other gate identical (vitest 301/301, bench 29/29 both drivers, backend gate exit 0, demo packs 118/118 registered, prettier clean). Gate row updated to match the command. **(3)** STATE.md re-measured 2026-08-24 (from 2026-08-08); §10 rewritten to name the boundary spec as the governing document for the unbuilt TAD §18 surface. KIT_DIARY Entry 31 records the pattern — a gate row's command and result must be a single-command round trip; test-fixture strictness is the same "invert conditional guards to fail-closed" law recorded three product-side times, now at a fourth altitude. Files: `src/driver/driver.ts`, `src/driver/backend.ts`, `src/harness/assertions.ts`, 26 test files, `STATE.md`, `WORKING_AGREEMENT.md`, `KIT_DIARY.md`. No product behavior changed.

- **Sprint 001 (2026-06-30)** — Contract registry extraction (founding act). Authored the 11 `contracts/*.yaml` registries (21 modules, 37 records, 113 operations, 121 events, 13 state machines, 5 projections, 1 report, 13 run-close rules, 22 assertion types, 2×3 profiles), the VF-003 reference manifest (`scenarios/VF-003/references.yaml`), the static validator (`src/registry/load.ts` + `validate.ts`), the decision log (`contracts/CONTRACT_GAPS.md`), and the TS/Node scaffold. Dual contract: signal (vacuous-runtime, meta-trace narrated) + artifact (`npm run validate:contracts` exit 0; consistency ok; all VF-003 references resolve). Observation contract: validator clean + 6-critic adversarial verification 5/6 dimensions clean, 1 minor finding (SerialHistory rework-name) dispositioned resolved-here as B-Q-3 (confirmed non-dangling). Findings B-Q-1..7 all resolved as recorded decisions; nothing invented. Closed clean. Full report: `dev/signal-reports/sprint-001-report.md`.

- **Sprint 002 (2026-06-30)** — JSON Schema generation (doc 08 Phase 2). Authored `src/schemas/generate.ts` (generates schemas from the registries per Build Readiness §8.1) → 94 operation schemas (47 input + 47 output), 58 event payload schemas, 1 RunCloseReport schema; `tests/fixtures/schema-fixtures.json` (14 fixtures) + `src/schemas/validate-schemas.ts` (the `validate:schemas` gate). Five tight input schemas verbatim from §8.2–§8.6; standard envelope baseline elsewhere (nothing invented). Dual contract: signal (meta-trace narrated) + artifact (`generate:schemas` exit 0; `validate:schemas` exit 0 — 94 op + 58 event + 1 report compile, 14/14 fixtures discriminate; `validate:contracts` still exit 0). Observation contract: quick 2-critic review (w9s39rz1o) → 5 findings (3 major, 2 minor), all applied and re-verified: output `required` aligned to Harness §11 (B-Q-8), idempotency_key minLength verbatim (B-Q-9), gate made fail-closed + output-schema coverage + blank-ref guard. Closed clean. Full report: `dev/signal-reports/sprint-002-report.md`.

- **Sprint 003 (2026-06-30)** — VF-003 scenario package + scenario compiler (doc 08 Phases 3–4). Authored `scenarios/VF-003/scenario.yaml` (58 steps, inline inputs, actors, aliases, world, 24 post-assertions; five doc-specified inputs verbatim) + `src/compiler/compile.ts` (resolves references, expands 128 inline expects, validates actor→caller + controlled clock, emits ScenarioCompilationResult) + `scenarios/NEG-001` (permanent negative fixture). Dual contract: signal (narrated) + artifact (`compile:scenario VF-003` exit 0, status passed, 152 assertions; `compile:scenario NEG-001` exit 1, 2 ContractGaps). Observation: compiler resolves all VF-003 references + fails closed on the negative fixture. Closed clean. Report: `dev/signal-reports/sprint-003-report.md`.

- **Six loose ends closed, and a pressure test that found three more (2026-07-31)** — Before taking up the backlog, probed the day's own gates with inputs no scenario supplies. Three fail-open holes, all the shape practice #19 predicts: a certificate of conformance could be issued for **quarantined** goods (attesting conformity for material on quality hold); a certificate for one part **released a different part sharing a serial number** (the identical cross-part collision the affected-population check closed in sprint 019, reintroduced in new code); and a shipment line could reference an inventory item that does not exist. All fixed fail-closed with an ALLOW-list rather than a deny-list, matched on serial AND part revision, and the line resolved at creation — each locked with its not-blanket case so a guard cannot pass by refusing everything (B-Q-49). Then the six: **(1)** `ReleaseFromQuarantine` was unbuilt, so the boundary was a TRAP — VF-025 quarantined goods and nothing could ever let them out. Built, but release is not an override: goods that arrived on a shipment line may leave only when a `ReceivingCheck` for that line reads passed. VF-030 proves both halves (B-Q-50). **(2)** `document_matches_part_revision` was registered with no `cert_type` so nothing could ever select it — dead vocabulary. Now enforced on every document match, which turned out to be the INBOUND twin of the outbound collision above. **(3)** RunCloseReport listed `Attachment` among its source records and carried none; accepted attachments are now in `source_traceability` (B-Q-51). **(4)** The claim that AS9163's revision rule maps onto report supersession was an assertion until VF-028 exercised it: a revised certificate with a new number supersedes the prior one, which is not overwritten. Building it exposed that the allow-list refused to certify `shipped` goods — wrong, since revisions exist precisely to correct a certificate after the goods have gone. **(5)** Durability proofs for the outbound certificate and attachments, gated into the backend exit: fresh from disk the certificate still authorises the shipment and the restricted document still withholds its reference. **(6)** The `Issue` lifecycle, so a quality issue can be worked instead of accumulating as a dead-end record — open/triaged/resolved/closed with cancel, each step recording who, refusing a resolution with nothing stated and a cancellation with no reason (B-Q-52). Green: validate:contracts ok; validate:schemas ok; bench all **29/29 both drivers**; diff-to-zero over **29**; backend gate exit 0 with 8 durability proofs; vitest **155/155 across 28 files**; src tsc 0; prettier clean.

- **Receiving: corrections first, then the new scope (2026-07-31)** — Pressure-tested the plan against the actual architecture before building, which produced three corrections, all made before any new work. (1) **Mine:** `ReceivingCheck` had been given a state machine whose initial state `passed` was also terminal — incoherent, and a divergence from the architecture, since `BuildCheckResult` and `RunCloseCheck` are both `state_machine: false` status-light records. Written in the same increment as the diary entry saying an imported concept must match the shape you already repeat. Now status-light like its siblings. (2) **Pre-existing, surfaced by the new work (B-Q-40):** `RunBuildCheck` names a quarantined CHILD precisely (`quarantined_inventory:<part>`, with a comment citing B-Q-14 that missing/quarantined/wrong-part must not collapse), while twenty lines earlier a quarantined TARGET reported the generic `target_inventory_not_available` — the same blurring the neighbouring code exists to prevent. Latent because nothing routinely quarantined a target; receiving does exactly that as its ordinary behaviour, so the first real use of the boundary landed on the one path that discarded the reason. Fixed, with a test for both branches; nothing asserted the old string. (3) `CaptureCertificate` could not carry `export_control`, so the export-control scenario needed a handler change, not just a scenario (B-Q-41). Then the new scope. **VF-026** is the self-teething pair to VF-025: the same consignment with the certificate present and in date, so the check passes and the goods are RELEASED — proving the boundary discriminates rather than refusing everything, and giving the release path something that can regress it. **VF-027** proves export control is PER DOCUMENT: an uncontrolled certificate of conformance and an export-controlled dimensional report on the same consignment; the goods release on the paperwork, the dimensional report is denied to a foreign person and allowed to a permitted one through the existing persona-gap-5 nationality gate, and the uncontrolled certificate stays readable by that same foreign person. Every decision audited. Research drove two decisions: ITAR 22 CFR 120.33 covers drawings, diagrams, tables and engineering specifications, which is what a dimensional report contains and a certificate of conformance is not — so control is declared per document, not per type; and a token hiding supplier documents from a customer summary was deliberately NOT built, because supplier quality flow-downs (Collins, Howmet, L3Harris) reserve customer and regulatory right of access to records at every level of the supply chain. The replay primitive also caught VF-027 pointing its idempotency check at `EvaluateAccess`, which is registered `not_idempotent` on purpose — a decision must re-evaluate against current policy and be audited each time it is asked, so a second audit record is correct behaviour, not a duplicate fact; retargeted at the receiving check. Coupling mutations added for both new paths (force a blocker -> VF-026 red; sweep export control onto every document -> VF-027 red). Green: validate:contracts ok; validate:schemas ok; bench all **26/26 both drivers**; diff-to-zero over **26**; backend gate exit 0; vitest **143/143 across 26 files**; src tsc 0; lint 0 errors; prettier clean.

- **Completing the adaptation, not just the adoption (2026-07-31)** — The pack arrived as a formed thing, so the work stopped where IT stopped rather than where this project's pattern stops. Four things finished. (1) **PHANTOM AUTHORITY, mine, today:** the receiving handlers cited `B-Q-34` and `B-Q-35`, ids written the day before for entirely different decisions (the RunStep/ProcedureStep pairing and the skipped-step fail-safe). Those citations RESOLVE — to the wrong entries — which is worse than dangling, and it was committed in the same session as the diary entry about practice #7. Written properly as **B-Q-37** (receiving decides, inventory transitions), **B-Q-38** (an unspecified consignment requires a certificate of conformance), **B-Q-39** (expiry and scope are properties of the document type, and absent is not expired), and the citations corrected. (2) **SerialHistory**: the closure walks records that point AT the item, so it reached the ShipmentLine but never the Shipment — the serial's story never said which consignment it arrived on or from whom. Header pulled in explicitly, the same way machine evidence is pulled in by linked serial; the arrival now reads end to end (8 entries, SHIPMENT_CREATED through INVENTORY_QUARANTINED) and VF-025 asserts it. That is the product's central promise applied to the new boundary. (3) **Idempotency replay**: Harness §14 says every scenario carries one and VF-025 had none. Declared as `idempotency_replay_checks` (scenario data, which is how this project does it — it is not a post-assertion evaluator) and tested. (4) An `access_full` assertion on the serial history. Green: bench all 24/24 both drivers with VF-025 now 23/23; diff-to-zero over 24; backend gate exit 0; vitest **139/139 across 26 files**; src tsc 0; prettier clean.

- **Closing the treatment gap on both increments (2026-07-31)** — Asked whether the new work had actually had the full end-to-end treatment, and checked rather than claimed. The standing gates passed, but three of this project's own standing practices had been skipped on it. (1) The whole-bench cross-driver diff-to-zero covered 23 scenarios, not 24 — VF-025 was passing on both drivers but was never graded BYTE-IDENTICAL, which is the weaker "both green" claim the project deliberately replaced (Cascade D1 intake). Added. (2) There was NO receiving durability proof, while VF-003/006/008/009/012/013/015 + the write boundary + the record-id counter + VF-003D + Phase A all have one — and the parity tell (sprint 010) says an asymmetry like that is exactly where a fail-open hides: a reload that lost the shipment line's `required_documents` or the check's `blockers` would leave quarantined goods releasable with nothing recording why. Added and gated into the backend exit: fresh-from-disk, item=quarantined, check=blocked, blockers=[certificate_of_conformance_present]. (3) The coupling suite had ZERO coverage of either increment, so both mutation batteries were throwaway when practice #6 says convert the probes into permanent tests. Added two: suppressing the receiving check's missing-document blocker turns VF-025 red, and neutering the CompleteRunStep precondition lets an unmeasured step complete. Green: bench all 24/24 both drivers; whole-bench diff-to-zero over **24** scenarios; backend gate exit 0 with the new proof; vitest **138/138 across 26 files**; src tsc 0; lint 0 errors; prettier clean.

- **Receiving evidence boundary (2026-07-31)** — An outside registry pack (`receiving-evidence-registry-pack-v0.1`, copied in) proposed a Receiving module: 13 records, 21 operations, a bespoke event taxonomy. Taken as INPUT, not adopted: nothing was carried over verbatim. Worked into this project's own vocabulary instead, where most of it already had a name. Its `SupplierDocument` is our **Certificate**; its `SupplierDocumentVerification` is our **Verification**; its `ReceivingInspection` with a status/result split is a shape we use nowhere, so it became a check shaped like **RunCloseCheck** (status + `blockers[]` naming registered rule ids); its `ReceivingQuarantine` is a blocker string plus the existing inventory quarantine state; its `SupplierCorrectiveAction` is our **Issue**. Its release split — receiving decides, inventory transitions — is our **ApplyBuildCheckResultToRun** pattern, so `RunReceivingCheck` decides and `ApplyReceivingCheckResultToInventory` walks the InventoryItem transition (added to the existing `received -> available` / `received -> quarantined` rows as a second `via`). Thirteen records became **three** (Shipment, ShipmentLine, ReceivingCheck); twenty-one operations became **five**. New `contracts/receiving-rules.yaml` mirrors run-close-rules: registered rule ids carried as blockers. PRESSURE-TESTING THE MAPPING FOUND TWO REAL FAULTS before a line was written: `VerifyCertificate` treats a missing expiry as INVALID (right for a calibration cert, wrong for a material test report or first article report — neither expires, so reuse would have refused valid paperwork), and it matches strictly on `serial_or_lot` (a first article report is scoped to a part revision, so it could never be found). The pack's `scope` idea was better than ours and was adopted; rules now carry `expires` and `scope`. THE MUTATION BATTERY THEN FOUND A THIRD, in my own design: suppressing the absent-document branch left VF-025 GREEN, because for a certificate of conformance `expires: true` meant the expiry branch caught an absent document as a side effect — absent and expired had collapsed into one blocker, which is the state-blurring VF-004/005/006 exist to forbid. Split into distinct registered ids (`certificate_of_conformance_present` vs `certificate_of_conformance_expired`); re-running the same mutation now fails VF-025 on both drivers (20/21) and turns 3 unit tests red. Also extended `event_payload_contains` to do actual containment on list payloads — `got === value` was reference equality, so `RECEIVING_CHECK_BLOCKED.blockers` was unassertable (sibling of the sprint-010 record_field_equals array problem); shipped with its own negative case. Green: validate:contracts ok (121 operations / 128 events / 42 records / 15 state machines / 5 receiving rules); validate:schemas ok; bench all **24/24 both drivers**; backend gate exit 0; vitest **136/136 across 26 files**; src tsc 0; lint 0 errors; prettier clean.

- **Standards citations checked and removed; STATE.md written (2026-07-30)** — The nine persona additions were each logged in `ADDITIONS.md` against a standard said to require them. Nobody had read those standards. Checked at source, four did not hold: **21 CFR Part 11** is FDA law (pharma, medical devices, biotech, food) with no force in aerospace — the cited authority for the e-signature fields; **ISO/IEC 17025** accredits testing and calibration laboratories, not manufacturers (a factory's own obligation for measuring equipment is AS9100/ISO 9001 7.1.5) — the cited authority for the calibration gate; **MESA-11** is a 1997 functional reference model, a taxonomy requiring nothing; **EIA-649C** is real but could not be confirmed to specify serial cut-in effectivity. Three held: **AS9102** (verifier != producer; Rev C Field 22 != Field 20), **ITAR 22 CFR 120.50(a)(2)** (deemed export), **AS9100 8.4.2** (evaluate external provider test reports). The citations were REMOVED rather than patched, with the correction recorded in place, and the project memory calling the study "standards-grounded" was corrected. No feature changed. Wider research then established what they were reaching for: a part ships with a data package (CoC per AS9163/EN 9163, material test report, NDT and dimensional reports, process certificates, first article report), and a missing document quarantines the shipment at receiving — none of which vocabulary appears in the nine governing documents. Closed by writing `STATE.md`.

- **Required step work must have happened (2026-07-30)** — A probe-driven find, then the fix. Reading the engine showed `RunCloseCheck` evaluates 2 of the 13 registered close rules; a PROBE (drive the real driver over VF-001's real steps with one step dropped, plus a positive control) turned that into a defect: with the torque capture dropped, the step still completed, the run reached `closed`, `RUN_CLOSED` fired, the close check reported ZERO blockers, and the report's `measurement_summary` was `[]` — the record certified a build whose characteristic was never inspected. Same shape with the required child's installation dropped (`installed_inventory: []`, empty as-built). A sweep over the other unevaluated rules found the rest HELD (required_steps_complete via the `CompleteRunSteps` precondition; redline_approved_before_applied via the state machine; the two NC rules via the rework chain), leaving `access_policy_available` open on the close side and four rules untested. Fixed both, faithfully: Build Readiness §CreateRun's "RunStep records FROM ProcedureVersion steps" was half-implemented (provenance dropped), so `CreateRun` now records `procedure_step` per run step (B-Q-34) and `InstallInventory` stops discarding the `run_step_alias` it already receives; `CompleteRunStep` gained the precondition Build Readiness §7 states (B-Q-36 records why the "waived by approved redline" half is NOT built — no waiver is representable, and the dossier says a waiver and a redline are different objects); and `RunCloseCheck` now evaluates both registered rules, which is NOT redundant because `CompleteRunSteps` accepts complete OR SKIPPED and only the close gate covers a skipped step (B-Q-35, held fail-safe). Failure classes are the REGISTERED rule ids, not a generic `precondition_failed`, so "never measured" and "never installed" stay distinct (the VF-004/005/006 no-blurring rule). One existing test went red and was re-based, not reverted: its fixture hand-seeded a RunStep with no procedure version, which the fail-closed gate correctly refuses — the test's subject was operator identity, so the stale fixture was the defect (Entry 10 fossil discipline). Green: validate:contracts ok (116/122/39/13/26), validate:schemas ok, bench all 23/23 both drivers, backend gate exit 0 (incl. whole-bench diff-to-zero), vitest 127/127 across 25 files, src tsc 0, lint 0 errors, prettier clean. Both guards spot-checked RED-capable (neuter the precondition -> 3 red; neuter the close loop -> 1 red; restore -> 6 green). Honest gap: the skipped-step close path is unit-proven only — `SkipRunStep` is registered-but-unimplemented so no scenario can reach it (practice #12 shortfall, logged in ROADMAP).

- **Valve-body demo pack + the SDD general-process note (2026-07-07/08, recorded 2026-07-30)** — Two doc/data artifacts that had been authored but never recorded here or committed; recorded now because an unrecorded artifact is exactly the phantom-close the project's own practice #13 forbids. (a) `SDD_GENERAL_PROCESS.md` — a theory note setting SDD against the settled fields that already do what it does (process mining, spec mining, grammatical inference, model extraction, model-driven reverse engineering; Rules as Code; computer-interpretable guidelines), naming what is a rename versus what is new, and locating the method's hard limit (Polanyi / Dreyfus / Moravec / the expert-systems collapse) with a four-question reach test. Its honesty is the point: it says most of the core is borrowed, which is what makes the four genuinely new moves survivable. (b) `demo-packs/valve-body-assembly-v0.1/` — the valve body VF-003 already builds, written out as plain files (part, BOM, procedure, tool + calibration, two serials, torque requirement, late tool reading, quality path, customer view, expected report). Pure data: changes no code, runs nothing at build time, wires nothing. `check.mjs` reads `manifest.yaml` and proves all 72 names it leans on are registered in `contracts/` — the no-invention rule enforced on the DATA side for the first time. Writing the domain out plainly found three real vocabulary gaps, now recorded as B-Q-31 (no standalone Part record), B-Q-32 (the inspection requirement has no record of its own), B-Q-33 (no operation for scanning a serial) — recorded, deliberately unbuilt, none blocking. Verified green at recording time: validate:contracts ok (116/122/39/13/26), validate:schemas ok, bench first_slice 14/14 both drivers, backend gate exit 0, vitest 121/121 (24 files), demo-pack check 72/72.

- **Documentation index + repo legibility (2026-07-01)** — Added `README.md` (front door: identity, the no-invention rule, current status, quickstart gate commands, and a grouped repo map explaining every top-level folder) + `DOCS.md` (a grouped catalog of every document in six purpose groups — status ledgers / SDD process state / sprint history / reviews / governing doc stack / vendored kit — each with a one-line purpose). Grounded in an ultracode doc-inventory workflow (5 family agents in parallel + a completeness critic). Deliberately did NOT physically reshuffle the tree: `src/ contracts/ scenarios/ schemas/ tests/` are spec-mandated (Build Readiness Plan repo layout) AND code-referenced (`readYaml("contracts/…")`, scenario/schema loaders), and `BLACKBOARD.md`/`WORKING_AGREEMENT.md`/`KIT_DIARY.md` sit at root by SDD-kit convention — so legibility is an index/map over a governed layout, not a file move (the critic independently recommended index-not-shuffle). All 388 files git-tracked, tree clean. No code or gate change. Committed f1bd112. Two naming warts left for a decision: the verbose governing-docs folder name + its near-duplicate redundant zip.

- **Phase A — outbox delivery leg (2026-07-01)** — Second of the two roadmap phases. The backend wrote outbox rows transactionally but nothing consumed them (TAD §12 at-least-once was aspirational). Built `deliverOutbox()` (backend plumbing, below the operation layer — no registry change): reads undelivered rows in seq order, drives an idempotent projection handler (`delivery_projection` keyed by event_seq + a materialized `projection_counts` incremented only on first apply), marks delivered. Proven by a backend durability proof (gated into exit; red-capability spot-checked on idempotency AND ordering). ADVERSARIAL REVIEW (a quick skeptic subagent, per the user's standing ask) earned its keep hard: it caught that the first cut was effectively EXACTLY-once (apply+mark in one transaction -> the "crash between apply and mark" state was unreachable, so the at-least-once idempotency defended a path only the test poke could reach), that the ordering proof was VACUOUS (counter projection is order-insensitive; dropping ORDER BY still passed), and a mark-without-apply orphan hole. ALL fixed: apply and mark are now SEPARATE transactions (making at-least-once genuinely reachable + necessitating idempotency), ordering is falsifiably proven (scramble outbox row-order -> delivery still ascends; dropping ORDER BY fails it), orphan rows are never marked delivered (fail-safe), `outbox.event_seq` is UNIQUE, and "crash"/"reload" are honestly labelled in-process simulations. Deferred (B-Q-30): retries-with-backoff + dead-letter (magnitudes unspecified -> would be invention). Green: contracts ok, schemas ok, bench first_slice 14/14, backend gate exit 0 (all proofs incl. Phase A + diff-to-zero 23 scenarios), vitest 121/121 (24 files). Both roadmap phases now shipped. Details in ADDITIONS.md + CONTRACT_GAPS.md (B-Q-30) + ROADMAP.md.

- **Phase B — §18 evidence-invalidation auto-cascades (2026-07-01)** — First of the two roadmap phases (run sequentially, Phase B first). Contract Spec §18 lists six effects when accepted evidence is invalidated; VF-003D shipped four. Phase B builds the two deferred, extending `InvalidateAcceptedEvidence`: (a) "create run close observation if run still open" — creates a RunCloseObservation + RUN_CLOSE_OBSERVATION_CREATED when the run is not terminal; (b) "create quality issue if physical product may be affected" — opens a quality Issue + ISSUE_OPENED, fail-safe (the accepted evidence's acceptability depended on it; Issue not Nonconformance, proportionate to "may"). NO invention — RunCloseObservation, Issue, and both events already existed; `InvalidateAcceptedEvidence` was registered as an additional producer of the two events (multi-producer precedent: NormalizeMachineEvidence + CreateGrammarGap). Both effects idempotent + gated behind the fail-closed run-resolution guard. The underspecified "physical product may be affected" is encoded fail-safe (B-Q-29). Proven by: unit suite (both obligations, run-open vs run-closed, fail-safe, idempotency, evidence linkage, fail-closed on unresolvable run; red-capability spot-checked), scenario VF-003F (open-run cascade, both drivers, in the whole-bench diff-to-zero), VF-003D augmented (the Issue on the closed-run path), and a coupling mutation that turns VF-003F red if the cascade is suppressed. Green: validate:contracts ok, validate:schemas ok (regenerated — ISSUE_OPENED payload schema added), bench first_slice 14/14 + extended 7/7 both drivers, whole-bench diff-to-zero over 23 scenarios, backend gate exit 0, vitest 121/121 (24 files). B-Q-29 recorded. Next: Phase A (outbox delivery leg). Details in ADDITIONS.md + CONTRACT_GAPS.md + ROADMAP.md.

- **Close-out — registry reconciliation + docs finalized (2026-07-01)** — Closing the line surfaced one more thing to distrust: `CaptureCertificate` / `VerifyCertificate` and the `Certificate` / `Instrument` record types (persona gaps 7-8) had been HANDLER-ONLY for several sprints — real, tested, review-hardened behavior the locked registries never named. They ran because the driver dispatches any handler present; the contract validator passed because it only checks the FORWARD direction (every registered name resolves), never the reverse (every handler maps to a registered op). For a vocabulary-as-contract build that is the sharpest breach, and it was invisible precisely because the green gate wasn't looking that way. Fixed: registered `Instrument` + `Certificate` in records.yaml and the two ops in operations.yaml (neither emits an event — a capture and a read); added `tests/consolidation/handler-registration.test.ts`, a reverse-direction poka-yoke asserting every handler maps to a registered op (red-capable; asymmetry explicit — a registered op with no handler is fine, it returns not_implemented, but a handler with no registered op is behavior outside the contract). Also finalized the close-out docs: ADDITIONS.md (deferred-items + both review passes + this registry fix), CONTRACT_GAPS.md (B-Q-22/27/28 RESOLVED), ADDENDUMS.md (end-of-line stamp), KIT_DIARY.md (Entry 20 + practice #20), and a new DEVIATION_SUMMARY.md (how far/how/status). Final measured green: `validate:contracts` ok (**116 operations / 122 events / 39 records / 13 state machines / 26 assertion types**; consistency ok); `validate:schemas` ok (14/14 fixtures); bench first_slice 14/14 both drivers; whole-bench cross-driver diff-to-zero over 22 scenarios PASS (byte-identical); backend gate exit 0 (all durability proofs incl. VF-003D reconciliation reload + record-id counter reload); **vitest 115/115 across 23 files**. Open ContractGaps: none. The line is closed: everything the spec deferred is built, reviewed, hardened, and every handler is now accounted for in the vocabulary. Supersedes the provisional numbers in the reconciliation-build entry below (the cert-ops registration moved ops 114->116, records 37->39, tests to 115/23).

- **Deferred items built + reviewed + line closed (2026-07-01)** — Built everything deferred, beyond the original spec (user is sole authority): the §18 reconciliation cascade (`InvalidateAcceptedEvidence` — accepted evidence -> invalidated, marking the run's reports regeneration_required), the report-freshness read (`GetReport` surfaces staleness fail-closed), the §19 two-mode contrast (controlled_export goes stale on a policy change, dynamic_view_filter does not), the temporal `world.access_policy_changes` (B-Q-27), and a new `operation_output_contains` assertion. Scenario VF-003D exercises the accepted->invalidated cascade end-to-end on both drivers. Registered the new vocabulary (op + event + state + transition); B-Q-22/27/28 marked RESOLVED. Value+path audit: clean (the 3 presence-only assertions are legitimate count checks). Then ran the adversarial review of this build (2 critics + verify) — 8 confirmed, the SAME fail-open family as the persona gaps (silent no-op when the run was unresolvable; report immune to staleness with no generated_at; cascade trusting the caller run over the evidence's linkage; unvalidated filtering_mode; swallowed unparseable policy dates; VF-003D not asserting GetReport's output; no backend-reload proof; coupling suite not extended). ALL fixed fail-closed, each with a test (fail-closed unit tests, a backend reconciliation reload proof, a coupling mutation that turns VF-003D red if the cascade is defeated, red-capability spot-checked). Green: gates 0; vitest 113/113 (22 files); bench 22/22 both drivers; backend gate 0 (incl. VF-003D reconciliation reload proof + whole-bench diff-to-zero across 22 scenarios + record-id reload). Details in ADDITIONS.md.

- **Persona additions — adversarial review + hardening (2026-07-01)** — Ran the distrust-the-green review over the nine additions (4 critics grounded in probes + an independent verify pass). It found 17 real defects, all reproduced — nearly all the same shape: the new checks were CONDITIONAL and fell OPEN on the ugly input. Fixed everything to fail CLOSED, each with a test proving it (and one flipped test that had wrongly blessed the fail-open): approval/verification now require an identified signer; disposition authority refuses an absent/empty role; deemed-export denies an unresolvable/malformed control; calibration accepts only a confirmed in-cal instrument (overdue/expired/unknown/missing all refuse); certificate expiry compares dates chronologically and fails closed on missing expiry; serial-range effectivity matches within one part-family prefix (a foreign family with the same number no longer matches); affected-batch remediation matches part identity not just the serial string. Also fixed a real latent BACKEND bug the review surfaced: the record-id counter wasn't restored on reload, so a post-reload write could overwrite a committed record — now reseeded past the persisted max (proven by a new reload proof). Green: vitest 103/103 (21 files), bench 21/21 both drivers, backend gate 0 (8 durability proofs + whole-bench diff-to-zero + record-id reload proof), static gates 0. Headline fix spot-checked red-capable (revert the require-signer guard -> the fails-closed test goes red). Details in ADDITIONS.md "Hardening".

- **Persona gap 2 — electronic signature (2026-07-01)** — Each approval/verification sign-off now records who (from gap 1), when (`signed_at`), and what it attests (`signature_meaning`) — a complete signature (the three parts a signature needs). VF-016 + the segregation-of-duties suite assert it. Green: vitest 83->still green, bench 21/21 (VF-016 55/55).

- **Persona gap 3 — typed disposition kinds + authority (2026-07-01)** — "disposition" was free text; now it's one of five named kinds (scrap / rework / repair / use_as_is / return_to_supplier) and an unregistered kind is refused. use_as_is and repair require quality or engineering authority (an operator or planner attempting them fails with `disposition_authority_violation`) — the AS9100-8.7 rule that accepting or repairing off-nominal product needs the design-responsible org. Threaded the caller's role to the handlers for this. Records who dispositioned. Changed VF-003's disposition from the free-text "rework_required" to the typed "rework" (nothing asserted the value). Green: vitest 87/87 (16 files, new disposition-authority suite), bench 21/21 both drivers, backend 8 proofs, gates 0. Files: handlers.ts, driver.ts, scenarios/VF-003, tests/authority/disposition-authority.test.ts.

- **Persona gap 1 — who-does-what / segregation of duties (2026-07-01)** — Fixed the biggest gap the reviews found: nothing checked who did each action. Now the system carries the person's id through to the handlers (it was accepted and thrown away), records who authored a redline / who approved / who did a rework / who verified, and REFUSES a same-person sign-off: an approval by the redline's own author, or a rework-verification by the person who did the rework, fails with `segregation_of_duties_violation` and changes nothing. Fixed VF-003 (where one quality engineer verified their own rework) to use a distinct verifier — the realistic fix. New scenario VF-016 proves a self-approval is refused and a two-person approval passes (self-teething: the two steps differ only in who approves). Plain, no version ceremony. Green: vitest 83/83 (15 files, incl. a new segregation-of-duties suite + a coupling mutation that turns VF-016 red if the check is dropped), bench 21/21 both drivers, backend 8 proofs, gates 0. Files: handlers.ts, driver.ts, backend.ts, run.ts, assertions.ts, scenarios/VF-003 (distinct verifier), scenarios/VF-016 (+refs), bench.ts, tests/authority/segregation-of-duties.test.ts, tests/consolidation/coupling.test.ts.

- **Sprint 018 (2026-07-01)** — MULTI-AGENT ADVERSARIAL REVIEW of sprints 014-017 (the "distrust-the-green" pass deferred while the weekly subagent limit was exhausted; run once it reset). Four parallel critics, each tasked to REFUTE a dimension with real probes: (B) VF-012 supersession — could not refute (hardcoding the report snapshot -> VF-012 red; overwriting the frozen snapshot -> caught from a fresh-from-disk backend instance; report_field_equals 13/13 discrimination incl. prototype-safe path walk; terminal + trigger genuine); (C) coupling-suite soundness — sound (reverted the serialHistory fail-closed fix + the RecordApprovalDecision fix in-source and confirmed the safety tests go red, then restored; all 6 mutations proven real; no state leak); (D + A) assertion-extraction + refactor fidelity — CONVERGED on ONE LOW finding. Finding: the map DISPATCH lookups were prototype-unsafe — a plain `EVALUATORS[type]` / `HANDLERS[op]` walks the prototype chain, so a type/op named after an Object.prototype member ("toString"/"constructor"/"__proto__") bypasses the unknown/not_implemented guard. Message-only for the assertion dispatch (no pass/fail flip; compiler-gated), but the handler dispatch (pre-existing, not refactor-introduced) is more severe — it would falsely SUCCEED by invoking an inherited Object method. FIXED at all three sites with `Object.hasOwn` (matching the engine's own established prototype-safe discipline — sprint-012 B-Q-26, report_field_equals), locked by `tests/consolidation/prototype-safety.test.ts` (3 tests) proven RED-capable (reverting the guard turns it red — verified, then restored). Critic A additionally confirmed the split faithful on every observable dimension (barrel live-bindings preserved `engine.HANDLERS === handlers.HANDLERS`; all `import type` correct; no non-erasable syntax; acyclic DAG; no transcription drift). Three minor non-defect observations reviewed + accepted (no fix). Dual/observation contract: `vitest run` 78/78 (14 files); `bench all` 20/20 both drivers; backend 8 proofs; gates 0. Closed clean. Report: `dev/signal-reports/sprint-018-report.md`.

- **Sprint 017 (2026-07-01)** — READABILITY (arc 4, second half — COMPLETES arc 4): behavior-preserving extraction of the ~200-line assertion-evaluator switch out of `src/harness/run.ts` into `src/harness/assertions.ts` — a family-grouped keyed `EVALUATORS` map (operation outcomes / events / records / projections / access / reports / grammar) with TSDoc + section headers, plus the `Driver`/`AssertContext` types + the `recEq` helper. run.ts reduced to thin orchestration (373 -> 173 lines; the switch became a dispatch loop over EVALUATORS with the shared context + try/catch + unknown-type default). No public export removed (`runScenario*`, `evaluateAssertions`, `evaluateDurable`, `runIdempotencyReplay` intact; `Driver` relocated + re-exported). Realized as ONE cohesive `assertions.ts` module with family SECTION HEADERS rather than a folder of tiny per-evaluator files — grouping-by-section reads better than folder-fragmentation for a reader tracing one assertion (consistent with the Architect's "no blog dogma / no third-party tooling" steer). Dual/observation contract: PURE behavior-preserving — `bench all` 20/20 both drivers; `vitest run` 75/75 (13 files); backend 8 durability proofs; static gates 0 — AND teeth-preserving: the discrimination suites that call the assertion engine DIRECTLY (assertion-primitives, report-supersession's rfeFailures, discrimination.test) still turn red on crafted bad inputs, so the extracted evaluators kept their discrimination (a broken evaluator would drop vitest below 75). Closed clean. Report: `dev/signal-reports/sprint-017-report.md`.

- **Sprint 016 (2026-07-01)** — READABILITY (arc 4, first half): behavior-preserving modular split of the dense ~660-line `src/driver/engine.ts` into single-responsibility modules — `registry.ts` (loaded registry tables + normalization grammar), `world.ts` (World + Rec/Evt + state-machine transitions + createGrammarGap), `projections.ts` (serialHistory/asBuiltProjection/assembleRunCloseReport), `handlers.ts` (the 47 ops, moved verbatim), `driver.ts` (InMemoryProductDriver + OperationResult) — with `engine.ts` reduced to a thin re-export barrel so backend.ts / run.ts / the 10 test files need ZERO import changes. Grounded in researched TS best practices from PRIMARY sources (Node.js type-stripping docs: mandatory `.ts` extensions, erasable-only syntax, `import type` mandatory for pure-type cross-module imports; tsdoc.org for doc comments) + first-principles reasoning for THIS codebase (a minimal barrel facade is justified because Node runs the `.ts` directly — no bundler, tree-shaking moot — and the one real barrel hazard, cycles, is avoided by an acyclic internal DAG with siblings importing directly, never through the barrel). Sourcing rule: primary docs + first principles only; opinion tech-blogs treated as hearsay, no third-party tooling adopted. Dual/observation contract: PURE behavior-preserving (technique #43) — `bench all` 20/20 both drivers; `vitest run` 75/75 (13 files); backend 8 durability proofs; static gates 0 — AND teeth-preserving: the sprint-015 coupling suite still turns scenarios RED through the barrel (the `export *` preserves the shared HANDLERS reference driver.ts dispatches through; a broken reference would fail the mutation assertions and drop vitest below 75). Accreted "why" comments moved verbatim (technique #44). Closed clean. Report: `dev/signal-reports/sprint-016-report.md`.

- **Sprint 015 (2026-07-01)** — CONSOLIDATION AUDIT (observation pass, no product-behavior change): a distrust-the-green sweep across the whole engine + assertion engine + all 20 scenarios for fossil/decoupled/vacuous/fail-open greens that fell between the per-increment reviews. Grounded in external check surfaces (technique #5), not self-reflection: a MUTATION BATTERY (inject a defect, confirm the scenario/assertion goes red) proved all 6 headline behaviors coupled — RecordApprovalDecision force-approve -> VF-013 red; GenerateRunCloseReport hardcoded snapshot -> VF-012 red; SupersedeReport no-op -> VF-012 red; ResolveEffectivity ambiguity-throws -> VF-007 red; CreateRun input-literal snapshot -> provenance decoy red; RunBuildCheck collapse-blockers -> VF-004 red. A direct probe confirmed all 5 accreted SAFETY invariants still hold (fail-closed access, write-boundary idempotency, op-scoping, emit poka-yoke, no-force-approve). A grep audit confirmed `report_payload_contains` is used only for honest section-presence and the only non-HANDLERS scenario op is NEG-001's intended `ValidateExternalPayload`. RESULT: the first distrust-the-green pass in the project to come back EMPTY — expected, because the swept code already ran the per-increment gauntlet and carries the teeth. The durable output is `tests/consolidation/coupling.test.ts` (11 permanent tests) converting the throwaway probes into a regression suite, so a future refactor that silently decouples an assertion turns it red rather than passing quietly. Dual/observation contract: `vitest run` 75/75 (13 files); `bench all` 20/20 both drivers; no mutation leakage. Closed clean. Report: `dev/signal-reports/sprint-015-report.md`.

- **Sprint 014 (2026-07-01)** — Report supersession after an access-policy change (VF-012, Harness §16/§17/§24): the deferred report-lifecycle scenario, now buildable because sprint 014 also filled the value-level assertion primitive B-Q-23 named as missing. A controlled_export RunCloseReport generated at T0 bound to scope S0 (customer_summary_access) is regeneration-required after an access-policy change at T1 > T0 (a REGISTERED trigger, `access_policy_change_for_controlled_export`), so a NEW report is generated bound to S1 (customer_extended_access) and the prior report is SUPERSEDED (generated -> superseded, terminal) — never overwritten (Contract Spec §15/§18/§19; Harness §16). Filled the registered-but-unhandled `SupersedeReport` (`REPORT_SUPERSEDED{superseded_report_id, superseding_report_alias, trigger}`), de-hardcoded `GenerateRunCloseReport`'s `access_policy_snapshot` (derived from the bound scope, so T0/T1 differ — killing a false-certainty hardcode), captured `generated_at`, and added `report_field_equals` (a report-scoped dotted-path VALUE-level read; B-Q-23(c)). The self-teething pair (t0 snapshot frozen at S0 != t1 snapshot S1) cannot pass against a hardcode. Honest scope: the OPERATOR-DRIVEN supersede+regenerate is built; the automatic-detection cascade + controlled_export GetReport read semantics are deferred (B-Q-27/B-Q-28), declared in VF-012's purpose. Dual contract: signal + artifact (`bench all` 20/20 both drivers; vitest 64; gates 0; backend now 8 durability proofs). Observation: distrust-the-green ran INLINE (weekly subagent limit; resets 12pm PT) — grounded in fresh-from-disk probes + code + docs (technique #5), it found two real items: (A) PHANTOM AUTHORITY — the code cited B-Q-23(c)/27/28 before the ledger held them (practice #7); fixed by writing all three; (B) no VF-012 reload proof while every sibling durable path had one; fixed as the 8th backend proof (frozen snapshot survives a cold start, S0 != S1 from disk). Extended arc COMPLETE for the buildable set + VF-012; only VF-003D remains deferred (B-Q-22). Closed clean. Report: `dev/signal-reports/sprint-014-report.md`.

- **Sprint 013 (2026-07-01)** — HARDENING pass (no new product scenario): closed the two highest-leverage systemic gaps. (A) Emit poka-yoke (SDD technique #2, B-Q-16): `World.emit` now enforces the vocabulary at RUNTIME — an unregistered event or a producer not registered for it throws + rolls back the op. All 18 scenarios passed unchanged (handlers were already registry-consistent — the static discipline held), teeth-tested. (B) Write-boundary idempotency (B-Q-13): `transactional_unique_constraint` ops dedup by a PERSISTED key set returning `idempotency_conflict` (zero facts), surviving a cold reload — distinct from the in-instance memo. Locked by IDEM-001 (a duplicate-write bench scenario, both drivers). Dual contract: signal + artifact (`bench all` 19/19 both drivers; vitest 60; gates 0; backend now 7 durability proofs). Observation: adversarial review (wkxxaohrk, 8 confirmed incl. 1 major) found the write-boundary + memo keys were UN-OP-SCOPED (two different ops sharing a key string collided), the memo cached FAILED results (a transient failure poisoned the key), and the headline change had no bench lock. ALL fixed: op-scoped `${op}:${key}` keys, success-only recording, IDEM-001. B-Q-13 + B-Q-16 marked RESOLVED. Closed clean. Report: `dev/signal-reports/sprint-013-report.md`.

- **Sprint 012 (2026-07-01)** — GrammarGap escalation (Harness §21/§24): VF-015 unsupported machine payload creates a GrammarGap — the executor rule AS A PRODUCT FEATURE. Today NormalizeMachineEvidence blindly emitted MACHINE_EVIDENCE_NORMALIZED with undefined fields for ANY payload (false certainty, B-Q-16); now an un-normalizable payload auto-escalates a typed GrammarGap (record stays raw, no normalize, no measurement) while a well-formed torque_trace still normalizes (the trigger discriminates). Filled the registered CreateGrammarGap + the grammar_gap_created evaluator; cleaned up B-Q-24 (dangling NormalizeMachineEvidence producer of MACHINE_EVIDENCE_QUARANTINED). Dual contract: signal + artifact (`bench all` 18/18 both drivers; vitest 49; gates 0; backend now 6 durability proofs). Observation: adversarial review (w503b13xs, 4 confirmed incl. 2 major) found the fix's own holes — a present-but-null/NaN/wrong-type field still normalized to garbage (false certainty for a failed sensor reading), and an attacker-controlled prototype-name payload_type (toString/__proto__) CRASHED the normalizer instead of escalating. Both fixed to the contract (type-aware validity per Build Readiness §8.4; prototype-safe lookup) + a record-idempotent escalation guard + a VF-015 reload-durability proof. B-Q-26 recorded, B-Q-24 resolved. Buildable extended adversarial set COMPLETE (VF-011/013/014/015); VF-003D/VF-012 deferred (B-Q-22/23). Closed clean. Report: `dev/signal-reports/sprint-012-report.md`.

- **Sprint 011 (2026-07-01)** — extended adversarial (Harness §24): VF-011 duplicate-payload idempotency, VF-013 redline rejected cannot be applied, VF-014 bounded drill-down audits+filters. VF-013 surfaced a REAL controlled-change safety bug: RecordApprovalDecision hard-coded "approved" and ignored the decision, so a reject force-approved and the rejected redline was APPLIED (red captured: decision=rejected -> REDLINE_APPLIED). Fixed to honor the registered reject transitions; VF-014 filled the registered BOUNDED_DRILL_DOWN_REQUESTED audit (no prior producer); VF-011 exercises the in-instance receive-boundary memo (B-Q-13 scope). Dual contract: signal + artifact (`bench all` 17/17 both drivers; vitest 42; gates 0; backend now 5 durability proofs). Observation: adversarial review (wumje2dyj, 7 confirmed incl. 2 major) caught the fix's own coercion hazard (any non-"approved"/absent decision -> irreversible rejection; now validation_error), a cold-reload checkpoint bug (alias-carrying redline/approval transitions never replayed; fixed + VF-013 durability proof), and that the registry-aware memo fix EXPOSED a sprint-007 test leaning on the over-broad memo (re-based onto a required_idempotency_key op). VF-003D deferred (B-Q-22, reserved op), VF-012 deferred (B-Q-23, multi-sprint); B-Q-24/25 recorded. VF-015 (GrammarGap) next. Closed clean. Report: `dev/signal-reports/sprint-011-report.md`.

- **Sprint 010 (2026-07-01)** — access-filtered serial history (VF-009) + run close blocked by missing report definition (VF-010): **FIRST-SLICE BENCH COMPLETE (VF-001..010, 14/14 both drivers, rate 1.0)**. The two biggest unexercised-path fills yet: the entire access-read path (serialHistory was access-blind; readProjection dropped the Harness §11 actorContext; access_full/summary/denied evaluators auto-failed) and the run-close rule registry (RunCloseCheck evaluated only the quality-path rule, never report_definition_available). VF-009: the SAME serial reads full | summary | denied by reader (Contract Spec §19) — full sees controlled_machine_evidence_payload, customer_summary_access strips it while keeping the review STATUS + RUN_CLOSED (Build Readiness §11.1). VF-010: a clean run blocks close SOLELY on the registered report_definition_available rule (default-available so VF-003 is unaffected; VF-010 withholds). Dual contract: signal + artifact (`bench first_slice` 14/14 both drivers; vitest 38; all gates 0; backend now 4 durability proofs). Observation: adversarial review (wqkz3n1m1, 3 critics, 7 confirmed incl. **2 SECURITY**) found serialHistory fail-OPEN on an unresolvable profile (leaking controlled payload to a revoked credential) and the backend losing access policies across a reload (reloaded summary reader saw full) — BOTH fixed to fail CLOSED (unresolvable -> denied) + the backend now persists the world config, proven by a VF-009 reload-durability proof + fail-closed unit/scenario tests. Also token-family redaction (raw_machine_payload live), full-relative non-vacuity guard, compiler world-key + access-profile checks. B-Q-20/21 recorded. Preceded by an understanding sweep (wifkoo4lu). Closed clean. Report: `dev/signal-reports/sprint-010-report.md`.

- **Sprint 009 (2026-06-30)** — effectivity family (Harness §19/§22): VF-007 ambiguous effectivity blocks the run, VF-008 snapshot survives a later rule change. Contract-first: VF-007 surfaced the registered EFFECTIVITY_AMBIGUOUS event's unexercised producer path (the engine threw on equal-priority matches instead of emitting it — the VF-003A dynamic, 5th occurrence). Fixed: ResolveEffectivity models ambiguity as a produced outcome (status ambiguous + EFFECTIVITY_AMBIGUOUS) per Contract Spec §17 "create ambiguity"; RunBuildCheck names `effectivity_ambiguous` as the sole isolating blocker (inventory staged) and the run goes planned -> blocked. Red captured before green (VF-007 50/56 pre-fix). Dual contract: signal (ambiguity/immutability spines) + artifact (`bench effectivity` + `first_slice` 12/12 both drivers; vitest 33; all gates 0; backend now proves VF-003 closed + VF-006 blocked + VF-008 snapshot durability). Observation: adversarial review (wq5s8d22e, 3 critics, 7 confirmed) caught VF-008's immutability proof VACUOUS — the RunContextSnapshot echoed the CreateRun input literal, not the resolution, so the §17 clause was untested (injection mis-selecting the resolution left the assertion green). Fixed at the root (CreateRun snapshots the resolution's selection) + a provenance discrimination test that fails if the snapshot ever echoes the input again; also no-match/ambiguity precedence (B-Q-18), flat selected fields (B-Q-17), effectivity blocker strings (B-Q-19), and a false VF-006 cross-ref in VF-007's prose. Closed clean. Report: `dev/signal-reports/sprint-009-report.md`.

- **Sprint 008 (2026-06-30)** — build-check-blocker family (Harness §22): VF-004 wrong child, VF-005 quarantined child, VF-006 missing child. Each drives the build-check BLOCKED path (unexercised by VF-003, which only walks BUILD_CHECK_PASSED) and the `planned -> blocked` run transition. Contract-first: two unexercised-path gaps surfaced as predicted — `QuarantineInventory` registered-but-unimplemented (filled: received->quarantined, INVENTORY_QUARANTINED) and the build check collapsing wrong/quarantined/missing into one `missing_bom_inventory` label (B-Q-14: now names three distinct blockers scoped by part identity). The red was captured BEFORE the green (VF-004 48/49, VF-005 46/50 pre-fix — each scenario can fail on the exact decision). Dual contract: signal (distinct blocker spines + RUN_BLOCKED) + artifact (`bench build_check` + `first_slice` 10/10 both drivers; vitest 27; all gates 0; backend now proves VF-003 closed + VF-006 blocked durability). Observation: adversarial review (w5dmishnx, 3 critics, 9 confirmed) caught the first-cut wrong_part heuristic as WORLD-GLOBAL (a missing child mislabeled wrong whenever any stray item existed; multi-BOM cross-attribution) — fixed by scoping detection to part identity (part_number + revision); also a phantom B-Q-14 (cited before written), a cosmetic schema gate (validated VF-003's set only), and a VF-003-only durability proof — ALL resolved. B-Q-14/15/16 recorded. Closed clean. Report: `dev/signal-reports/sprint-008-report.md`.

- **Sprint 007 (2026-06-30)** — SDD test primitives + machine-evidence variants. Re-read the SDD testing techniques and implemented the three that were missing: `event_payload_contains` (assert_signal-with-payload, Foundation 02), `event_sequence_matches` (filtered-exact confirmed-good-capture regression, #42), `idempotent_replay` (doc 08 Phase 9 / §18). Retrofitted VF-003 (162/162). Built VF-003A/B/C (machine-evidence accepted/rejected/quarantined) + VF-003E (forbidden-transition negative test) + `operation_failed` assertion. SDD loop surfaced a real gap: Accept/Reject/Quarantine handlers were registered but unimplemented (VF-003 only walked the review path) — filled per §10.2/§7.6. Dual contract: signal (distinct evidence spines) + artifact (`bench first_slice` 7/7 both drivers; vitest 18; all gates 0). Observation: adversarial review (wg5nwl2rh, 10 findings incl. blocker) found idempotent_replay was a tautology, the sequence matcher too weak, an ambiguous registry transition, forbidden transitions untested, and a decoupled no-overwrite assertion — ALL resolved (honest idempotency scope + negative test; filtered-exact sequence; deleted ambiguous transition; VF-003E; after_step no-overwrite; validator forbidden-check). B-Q-13 (write-boundary idempotency deferred). Closed clean. Report: `dev/signal-reports/sprint-007-report.md`.

- **Sprint 006 (2026-06-30)** — Smoke bench (VF-001 happy path, VF-002 failed-measurement→NC) + bench runner (Harness §22/§24). Authored `scenarios/VF-001/scenario.yaml` (35 steps) + `scenarios/VF-002/scenario.yaml` (36 steps) — new scenarios exercising the existing 47 handlers on new paths (VF-001 clean close-on-first-attempt; VF-002 failure→NC→refuse-to-close). `src/harness/bench.ts` runs each scenario on BOTH drivers; `bench:smoke`/`bench` scripts; `tests/bench/smoke.test.ts`. Added `projection_not_contains` assertion type + alias resolution in `record_field_equals`. Dual contract: signal (distinct clean vs failure traces) + artifact (`bench:smoke` + `bench` exit 0; vitest 14/14; all gates 0). Observation: smoke bench PASS + first_slice bench 3/3 PASS on both drivers (VF-001 95/95, VF-002 93/93, VF-003 156/156). Adversarial review (wpc291i33) found vacuous discriminators (VF-002 "no-close" couldn't fail; NC-linkage unasserted) — all resolved (VF-002 now attempts close + proves RUN_CLOSE_CHECK_BLOCKED; NC↔measurement linkage; VF-001 absence check). Closed clean. Report: `dev/signal-reports/sprint-006-report.md`.

- **Sprint 005 (2026-06-30)** — Backend skeleton, same VF-003 (doc 08 Phase 10; the FIRST SLICE COMPLETE against both drivers). Made the runner driver-agnostic (`executeScenario`/`evaluateAssertions`/`evaluateDurable`); built `src/driver/backend.ts` (`BackendProductDriver`, `node:sqlite`, per-op transactional records+events+outbox, `loadFromDisk`, `rebuildCheckpointsFromEvents` event replay); `src/harness/run-backend.ts` gate + `vitest.config.ts`; added per-op rollback to the engine (§8). Dual contract: signal (backend emits the identical 70-event VF-003 spine) + artifact (`test:vf003:backend` exit 0; in-memory VF-003 156/156 unchanged; vitest 10/10). Observation: VF-003 156/156 on backend; **96/96 persisted-state assertions re-pass on a FRESH disk-loaded instance**; checkpoint `run_001 @after_step_047 = close_blocked` genuinely replayed from the event log. Adversarial review (w4zjlovog, 2 critics) found the first durability claim OVERSTATED (blocker: ~60/156 assertions used cached maps, checkpoint un-durable) + a latent no-rollback bug — all resolved (durability scoped to persisted state, checkpoint from log replay, per-op rollback + fault-injection test). B-Q-11 recorded (outbox writer-only, consumer deferred). Closed clean. Report: `dev/signal-reports/sprint-005-report.md`.

- **Sprint 004 (2026-06-30)** — In-memory ProductDriver + VF-003 end-to-end (doc 08 Phases 5–9; the FIRST SUCCESS CONDITION). Authored `src/driver/engine.ts` (World store + registry-driven state-machine executor + 47 handlers + projections + InMemoryProductDriver), `src/harness/run.ts` (runner + assertion engine, 10 assertion types incl. temporal window + per-step checkpoints), `tests/vf-003/vf003.test.ts` + `tests/vf-003/discrimination.test.ts`. SDD loop: first run 133/152 (5 rework handlers missing → deltas pointed straight at them), fix cascaded to 152/152. Adversarial review (wra2lfkva, 3 critics) found the green partly false (11 findings, 1 blocker): SerialHistory ignored its key, RunBuildCheck rubber-stamped, BoundedDrillDown hardcoded, evidence guard mis-pinned, two §15 late-evidence invariants only coincidentally true. All fixed + locked by discrimination tests. Genuine run: 156/156, `ScenarioResult.status == passed`. All gates exit 0 (contracts/schemas/compile/run/vitest 9/9). B-Q-10 (idempotency memo) recorded. Closed clean. Report: `dev/signal-reports/sprint-004-report.md`.

---

## Deferred

*Anyone may append. Re-visit conditions noted.*

- **2026-06-30 (Agent)** — Backend skeleton (Phase 10 / doc 08) is deferred until VF-003 passes against the in-memory ProductDriver. Re-visit condition: `VF-003 ScenarioResult.status == passed` in memory with all blocking assertions green.

- **2026-06-30 (Agent)** — Non-VF-003 operations in the full registry need not be implemented in the first slice; they must fail explicitly with `not_implemented`. Re-visit per scenario as VF-001/002/004+ are added.

---

## Open questions

*Anyone may append.*

- **B-Q-1 .. B-Q-7** — RESOLVED (sprint 001). The six surfaced cross-document tensions plus B-Q-7 (`BUILD_CHECK_FAILED` vs `BUILD_CHECK_BLOCKED`, discovered during extraction) are each resolved as recorded registry decisions in `contracts/CONTRACT_GAPS.md` and verified faithfully encoded by the 6-critic adversarial pass. Two carry forward as Architect awareness items (not blockers): B-Q-5 (event owner ≠ producer for `REDLINE_APPROVED`) and the Build Readiness §5.2/§9.3 vs Contract Spec §15 record-name divergence (B-Q-3) — both warrant a future TAD/Build-Readiness amendment, not a registry change.

---

## Drift watchlist

*Agent maintains. Patterns to monitor across sprints.*

- **No-invention guard** — every operation/event/state/projection/report/assertion an executor writes must trace to a registered contract entry. If an executor reaches for unregistered behavior, that is a halt (ContractGap/TODO), not a sprint output. Watch every sprint.
- **Record-name canonicalization** — `ReworkRun`/`Verification` (canonical) vs `ReworkRecord`/`VerificationRecord` (Build Readiness divergence). Sprint 001: validator + grep confirm no divergent names in `contracts/`; alias map recorded in `CONTRACT_GAPS.md`. Keep watching as schemas and operation handlers are authored in later sprints (Build Readiness §7/§9 still uses the divergent spelling).
- **State-machine completeness** — records that VF-003 mutates but that the Contract Spec does not list as first-slice state machines (e.g. `QualityContainmentAction`). Watch that each such transition is either registered or explicitly declared status-light.

---

## Sprint tail

*Agent maintains. Kit convention is last-10; this tail has kept per-sprint detail longer because rolling it into `## Built` would compress unique observations that Built's paragraph form did not carry. Every closed sprint from 001 through 052 has an entry either here or under `## Built`. New sprints append at the top.*

### Phase C (2026-08-25, closed) — access-and-visibility boundary, 24 sprints in one day
- **Scope:** the third governing boundary the build satisfies, after receiving. `specs/access-and-visibility/boundary-spec-v0.1.md` arrived 2026-08-24 and closed the next day. 24 sprints, 029-052. §16 scores **18 of 18** pass or pass-in-part (`docs/ACCESS_AND_VISIBILITY_ACCEPTANCE.md`).
- **Built, five sub-phases.** C.1 foundations — 029 mapping pass (22 rows, 4 already-spoken / 6 extends-existing / 12 new-vocabulary), 030 in-repo registry pack (14 files), 031 §8 decision model, 032 visibility levels (full / summary / denied / hidden_existence), 033 reason codes + failure classes, 034 visibility profiles. C.2 dimensions (035-042) — access-group, customer, program, contract, factory-node, record + report-type, support/admin (SupportSession new record), service-account (processing ≠ disclosure). C.3 enforcement (043-048) — projection read, report generation preservation, report read, bounded drill-down per-hop, AccessAttachment (six outcomes), event replay to user-visible views. C.4 cross-cutting (049-051) — audit invariants, AmendAccessPolicy + freshness cascade, 16-arm fail-closed battery. C.5 closeout (052) — §16 acceptance file.
- **Registry delta:** +4 operations, +4 events, +1 record (`SupportSession`), +1 state machine, +1 authorization rule, +3 registry files. Only one new record because B-Q-74/75/77 candidate answers kept `access_group`, `customer`, `program`, `contract`, `factory_node`, and `service_account_scope` as fields on caller context or existing records — the receiving pack's 13→3 collapse repeated.
- **Byte-identical held.** Every existing scenario (VF-001..016 variants + VF-024..034 + IDEM-001 + NEG-001) traces cross-driver-equivalent after every access change. Whole-bench cross-driver diff-to-zero over 37 scenarios PASS at every sprint close. Practice #33 (opt-in on target-side scoping) resolved the fail-closed vs backward-compat tension: dimension checks fire fail-closed against callers who don't provide the matching context, and stay dormant against targets that don't declare the scoping field.
- **Rubber Duck Pass + red-team.** Ten probes with positive controls. Three findings, all in claims about the code rather than in the code itself. (1) `role_not_authorized` and `controlled_data_denied` cited `used_by_sprint: 031` but never emit — marked `used_by_sprint: deferred`. (2) The audit-does-not-leak test asserted no leak on one DENIED path; hardened to drive every dimension with three forbidden fields each. (3) "Byte-identical" overstated: diff-to-zero measures cross-driver fidelity, not against-baseline; a stored-golden-trace check is a deferred follow-up. Practice #34 for TECHNIQUES.md: diff-to-zero between two implementations is fidelity, not regression.
- **Dual/observation contract:** validate:contracts ok (132/136/43/16/33/26); validate:schemas ok; validate:demo-packs ok; bench all 29/29 both drivers; backend gate exit 0 with 14 durability proofs; whole-bench cross-driver diff-to-zero over 37 scenarios PASS; vitest **432/432** across 58 files (+131 tests, +21 files); src tsc 0; prettier clean. Full narrative in `docs/SESSION_2026-08-25.md`; per-sprint detail in `dev/sprints/sprint-029..052-*.md`; readout in `docs/PHASE_C_READOUT.md`.
- **Files:** `contracts/reason-codes.yaml` + `contracts/failure-classes.yaml` + `contracts/visibility-profiles.yaml` (new); `contracts/records.yaml` + `contracts/operations.yaml` + `contracts/events.yaml` + `contracts/state-machines.yaml` + `contracts/authorization-rules.yaml` (extended); `src/driver/visibility.ts` (new); `src/driver/handlers.ts` + `src/driver/driver.ts` + `src/driver/backend.ts` + `src/harness/assertions.ts` (dimension checks and readAsCaller pass-throughs); the whole `tests/access/` directory (21 new files); `specs/access-and-visibility/registry-pack-v0.1/*` (in-repo pack); `dev/sprints/sprint-029..052-*.md` + `dev/sprints/sprint-029-mapping-table.md`; `docs/{STATE,ROADMAP,DOCS,HANDOFF,PHASE_C_READOUT,SESSION_2026-08-25,ADDITIONS,DEVIATION_SUMMARY,ACCESS_AND_VISIBILITY_ACCEPTANCE}.md`; `KIT_DIARY.md` Entry 32.

### Machine binding + numbering (2026-08-07, closed) — the guard I had deferred on an unchecked estimate
- **Scope:** close B-Q-73 properly (machine evidence naming its machine by a checked, resolved reference) and settle the numbering scheme.
- **Built:** `ReceiveMachineEvidence` resolves both machine and adapter to registered records and refuses three ways — unregistered machine, unregistered adapter, and the mismatch where both resolve but the adapter speaks for a different machine. That third is the one worth having: nothing looks wrong and the attribution is still incorrect, which is how a reading from one tool is filed against another and how a recall on the second tool misses it. Ten scenarios registered their equipment; VF-015 needed a second adapter rather than a looser rule, since one adapter speaking for two machines would make the mismatch check unenforceable.
- **The estimate was wrong, and it cost a week.** B-Q-73 deferred the guard on my claim that closing it meant renumbering ten scenarios including VF-003. Suffix step ids were already in use in this project — added by me to VF-034 and VF-037 days earlier — so the registration steps prepend as `000a`/`000b` and every later step keeps its number. Nothing moved. The cost was estimated from memory of what the harness allowed rather than from the harness.
- **Numbering, three problems and two of them mine:** `dev/sprints/` skipped 022 (that work landed with a blackboard entry and no file, while sprint 023 cited the number) — backfilled, and the file says it was backfilled rather than pretending to predate the work. `dev/signal-reports/` stops at 018 while DOCS.md called these 1:1 pairs; from 019 the pairing lapsed and the sprint file absorbed both halves — recorded rather than backfilled, since ten retrospective reports derived from the files that replaced them would add pages and no facts. The scenario gap at VF-017..023 stays and `WORKING_AGREEMENT.md §Numbering` says why: closing it would renumber scenarios cited across the doc stack, benches, tests and ledger to make a number nobody counts consecutive.
- **Dual/observation contract:** validate:contracts ok; validate:schemas ok; validate:demo-packs ok; bench smoke 2/2, first_slice 14/14, extended 9/9, receiving 10/10 both drivers; backend gate exit 0 with cross-driver diff-to-zero over 37 scenarios; vitest 301/301 (37 files); src tsc 0; prettier clean.
- **Rubber Duck Pass:** enforcing the binding turned 8 unit tests red — fixtures receiving evidence from equipment nobody had registered, which is the guard biting where it should. One of those fixtures would have gone on passing on an empty world: the idempotency-replay test had no fact to duplicate once nothing could arrive, which is the vacuous green that file exists to rule out. Every mechanical gate stayed green through the whole bookkeeping drift — the project has a poka-yoke for a handler escaping the registry and none for a sprint escaping the sprint log. No halts.
- **Files:** src/driver/handlers.ts, scenarios/VF-003|003A|003B|003C|003D|003E|003F|009|011|015, tests/{grammar-gap,hardening,harness,vf-003,reports}/*, contracts/CONTRACT_GAPS.md (B-Q-73 closed), dev/sprints/sprint-022-scenario-pack-and-close-report.md (backfilled), DOCS.md, WORKING_AGREEMENT.md (§Numbering).

### Sprints 024-028 (2026-08-07, closed) — building the specified remainder
- **Scope:** 43 registered operations returned `not_implemented`. 36 had their behaviour fully declared by a state machine, so building them was transcription; the rest were judged one at a time. Five sprints: the run lifecycle, the controlled-document lifecycle, inventory and quality, report generation and registration, the supplier evidence packet.
- **Result: 124 of 128 operations built.** Three are refused on record — `EvaluateMeasurement` is already implemented inside `CaptureMeasurement`, `GenerateRunCloseNarration` emits nothing so its text would be invention, `EscalateGrammarGap` has no lifecycle to escalate into. **§27 now scores 15 of 15**: the evidence packet gave the last four mutation arms a surface to act on, so all 26 execute against the real driver and the not-enforceable list is empty.
- **Two authority questions answered (B-Q-59, B-Q-60):** stopping a run is quality's act, not the planner's — pausing is a scheduling decision about when work happens, blocking is a judgement that it must not happen yet. And material held for a quality reason is released by quality, so the two paths onto the floor agree. Narrowing the second cost VF-034 a proof, which was re-actored rather than dropped.
- **Three defects found by building, not by review.** The as-built projection listed installation events, which was correct only while the tree could exclusively grow — `RemoveInventory` ends that, and without the fix a part physically taken off a unit would still read as fitted to it. `failed` and its retry were report states the machine declared and nothing could produce, because an atomic generate cannot fail half-way. And a five-week coverage shortfall closed: B-Q-35's skipped-step close rule had been proven against a hand-set state because `SkipRunStep` did not exist.
- **Dual/observation contract:** validate:contracts ok (128 operations, 132 events, 3 reports); validate:schemas ok; validate:demo-packs ok; bench smoke 2/2, first_slice 14/14, extended 9/9, receiving 10/10 both drivers; backend gate exit 0 with cross-driver diff-to-zero over 37 scenarios; vitest 297/297 (37 files); src tsc 0; prettier clean.
- **Rubber Duck Pass:** two of my own tests would have passed for the wrong reason and were caught before landing — an anonymous-scrap case that passed `undefined` to a defaulted parameter and so was never anonymous, and two battery arms written from memory of the system's shape rather than from the code (a CAGE code the fixture never sets; `output.stale` and `access_policy_changes`, when the system exposes `regeneration_required` and `accessPolicyChanges.effective_at`). The compiler caught an invented event in VF-037. One guard was deliberately left open and recorded rather than fudged (B-Q-73): binding machine evidence to a registered machine means renumbering ten scenarios including VF-003, and the "enforce only once something is registered" alternative fails open by default. No halts.
- **Files:** src/driver/{handlers,projections}.ts, contracts/{operations,events,reports,authorization-rules}.yaml, contracts/CONTRACT_GAPS.md (B-Q-71 closed, B-Q-72/73), scenarios/VF-036|037 (new), scenarios/VF-034, tests/floor/{run-lifecycle,inventory-and-quality-lifecycle,step-requirements}.test.ts, tests/procedure/controlled-document-lifecycle.test.ts, tests/reports/report-generation-lifecycle.test.ts, tests/receiving/fail-closed-battery.test.ts, tests/access/operation-authorization.test.ts, RECEIVING_ACCEPTANCE.md, ROADMAP.md, dev/sprints/sprint-024..028.

### Sprint 023 (2026-08-01, closed) — the demo pack, the ungated check, and the acceptance sweep
- **Scope:** boundary spec §24's demo pack and a written sweep of §27. The ROADMAP's open scope call — the demo-pack check sitting in no npm script, no gate and no suite — was answered rather than doubled by adding a second ungated artifact.
- **Built:** `demo-packs/receiving-evidence-valve-body-v0.1/` (16 files, one consignment in our own vocabulary, with a manifest recording what §24 assumes and this build deliberately lacks); `demo-packs/check.mjs` covering EVERY pack, replacing the per-pack copy; `tests/consolidation/demo-pack-registration.test.ts`; `npm run validate:demo-packs`; `RECEIVING_ACCEPTANCE.md`.
- **§27 scores 14 of 15.** The fifteenth is stated precisely rather than hedged: 22 of 26 mutation arms execute against the real driver, and the four that do not all wait on B-Q-71 — supplier evidence has no access-filtered read path because §11.6's evidence packet is unbuilt.
- **Dual/observation contract:** validate:contracts ok; validate:schemas ok; validate:demo-packs ok (118 names, 2 packs); verify:types up to date; bench smoke 2/2, first_slice 14/14, extended 7/7, receiving 10/10 both drivers; backend gate exit 0 with cross-driver diff-to-zero over 35 scenarios and 14 durability proofs; vitest 222/222 (33 files); src tsc 0; prettier clean.
- **Rubber Duck Pass:** the new gate was red-capability checked before being trusted — an invented record name fails the build, and the checker refuses an empty sweep rather than passing vacuously (the vacuous-pass shape this project keeps finding in its own gates). B-Q-60's revisit trigger had fired in sprint 020 and nobody noticed: clearing a consignment is now a quality act while lifting a quarantine stays open to a planner, so the two paths onto the floor no longer agree. Updated rather than left — a revisit condition that passes unremarked is how a deferral becomes a decision nobody made. B-Q-72 records why a supplier is a reference and not a record, and when that stops being true. No halts.
- **Files:** demo-packs/receiving-evidence-valve-body-v0.1/* (new, 16), demo-packs/check.mjs (new; the per-pack check.mjs removed), tests/consolidation/demo-pack-registration.test.ts (new), RECEIVING_ACCEPTANCE.md (new), package.json, ROADMAP.md, DOCS.md, contracts/CONTRACT_GAPS.md (B-Q-72; B-Q-60 updated), dev/sprints/sprint-023-demo-pack-and-acceptance.md (new).

### Sprint 022 (2026-07-31, closed) — the scenario pack completed and the close report told where the parts came from
- **Scope:** the last three §13 scenarios (§17 VF-027, §19 VF-029, §20 VF-030) and acceptance criterion 12 (§23.3, the close report's receiving-evidence summary).
- **Built:** a `receiving_evidence_summary` section on RunCloseReport, tracing every installed child back through its shipment line to the check that cleared it and the documents that were verified; `process_certificate_present` / `_unverified` receiving rules; VF-027, VF-029, VF-030 and VF-035.
- **VF-035** is criterion 12 end to end — VF-001's production spine with the gasket arriving from a supplier instead of appearing by fiat, so one report covers a supplier-received child and an in-house parent. The section names material that never came from a supplier rather than omitting it (B-Q-70): a blank row and an internally-made row must not look the same, or "we make this ourselves" reads identically to "we lost the paperwork".
- **VF-027** drives both arms of the first article requirement, and the report is filed against a DIFFERENT lot of the same revision — correct for a first article, and invisible to a check that matched on the serial that arrived. **VF-029** puts §9.6 on the bench: a foreign person verifies the uncontrolled certificate and is refused the controlled first article, so the denial is about the data and not the person. **VF-030** captures a verified process certificate for a neighbouring lot and shows it satisfies nothing — a first article covers a design, a process certificate covers a furnace load (B-Q-69).
- **Dual/observation contract:** validate:contracts ok (127 operations, 10 receiving rules); validate:schemas ok; bench smoke 2/2, first_slice 14/14, extended 7/7, receiving 10/10 both drivers; backend gate exit 0 with cross-driver diff-to-zero over 35 scenarios; vitest 212/212 (32 files); src tsc 0; prettier clean.
- **Rubber Duck Pass:** every scenario carries a control that would fail if the boundary simply refused everything — VF-027 asserts the good certificate was NOT blamed, VF-029 that the same foreign person verified the uncontrolled document, VF-030 that only the process certificate is named among three requirements. The access contrast for §23.3 is a unit test rather than a scenario, because one report cannot be read at two depths in a single run: the customer scope keeps the fact of verification and drops the certificate number and the signer, and the same run at full depth carries both, so the redaction assertions cannot pass against a report that never populated the fields. One assertion id in VF-030 said `quarantined_meanwhile` while asserting `available`; renamed and split. No halts.
- **Files:** contracts/{receiving-rules,reports}.yaml, contracts/CONTRACT_GAPS.md (B-Q-69/70), src/driver/projections.ts, src/harness/{bench,run-backend}.ts, scenarios/VF-027|029|030|035/* (new), tests/receiving/close-report-receiving-evidence.test.ts (new).

### Sprint 021 (2026-07-31, closed) — carrying a receiving refusal outward
- **Scope:** boundary spec §18 (VF-028), §10.13, §10.14. Sprint 020 ended at the refusal — document rejected, goods quarantined — which leaves the material in a corner and the supplier never told. The receipt is safe and the factory learns nothing.
- **Built:** `OpenReceivingNonconformance` and `OpenSupplierCorrectiveAction`, registered as co-producers of the existing `NONCONFORMANCE_OPENED` / `ISSUE_OPENED` rather than inventing an event taxonomy for facts already named. The receiving NC is our `Nonconformance` with receiving provenance (a separate operation, so the production path's run+measurement guard is not loosened — B-Q-67); the corrective action is our `Issue` with a typed source and the supplier named (B-Q-66). `ReceivingCheck` now distinguishes `failed` from `blocked` and carries `rejected_documents` (B-Q-68): blocked means the file is incomplete and producing the document resolves it, failed means somebody looked and said no — and you do not raise an action against a supplier because your own inspector has not opened the file yet.
- **VF-028** — two serials on one shipment from one supplier: one certificate certifies the wrong revision and is rejected, the other is correct. The bad line fails, quarantines, opens a nonconformance and a corrective action that runs to closed; the clean line still releases, so a boundary that condemned the whole consignment would pass every other assertion and fail that one. Three refusals are driven alongside the success: no action against a consignment that passed, none against a document nobody rejected, none without a named supplier — plus an assertion that the refused records were never written.
- **Dual/observation contract:** validate:contracts ok (127 operations); validate:schemas ok; verify:types up to date; bench smoke 2/2, first_slice 14/14, extended 7/7, receiving 6/6 both drivers; backend gate exit 0 with cross-driver diff-to-zero over 31 scenarios and 15 durability proofs; vitest 207/207 (31 files); src tsc 0; prettier clean.
- **Rubber Duck Pass:** TWO harness primitives could not express what the scenario needed, both broken the way this project has seen before. `record_field_equals` compared with `===`, reference equality on arrays, so any list expectation on a record field was unsatisfiable regardless of what the record held — it could not pass, so it could not discriminate. `event_payload_contains` had the identical defect on list payloads and was fixed on that leg alone; the parity tell said check the sibling, and the sibling was still broken. Separately `record_exists` ignored its own `expected` and could only assert PRESENCE, so a scenario could not state that a refused operation wrote nothing — a core fail-closed claim. Both now discriminate in every direction with legacy meaning preserved. Red-capability proven twice: neutering the failed/blocked distinction turns the VF-028 reload proof red and the backend gate to exit 1; stripping the trigger requirement turns VF-028 red in the coupling suite. No halts.
- **Deliberately absent:** §8.6's `supplier_response_pending` and `response_under_review`. §26.2 rules out a supplier portal in v0.1, so the supplier cannot respond to this system and a "waiting for the supplier" state is one the record enters and never leaves for reasons nothing can observe (B-Q-66).
- **Files:** contracts/{operations,events}.yaml, contracts/CONTRACT_GAPS.md (B-Q-66/67/68), src/driver/handlers.ts, src/harness/{assertions,bench,run-backend}.ts, scenarios/VF-028/* (new), scenarios/VF-026 (failed-status assertions), tests/receiving/*, tests/consolidation/coupling.test.ts, dev/sprints/sprint-021-supplier-quality.md (new).

### Sprint 020 (2026-07-31, closed) — supplier document verification: attached is not verified
- **Scope:** the boundary spec's central invariant §9.4 — a document is not production evidence until a person verifies it — plus §9.5 (mismatch) and §9.6 (an actor cannot verify what they cannot see), which belong at the same act because verification IS comparing a document against the goods.
- **The gap:** `RunReceivingCheck` asked whether a `Certificate` EXISTED. Capture is clerical, so a receiving clerk typing in a certificate number satisfied a release requirement nobody had read. A registered read operation already called `VerifyCertificate` (no events, no writes, computes whether a document looks valid) made the absence easy to miss — the word was there and the act was not.
- **Built:** a `Certificate` state machine mirroring the Attachment review lifecycle (captured → review_required → verified | rejected | superseded); `RouteCertificateForReview` / `AcceptCertificateAsEvidence` / `RejectCertificateAsEvidence`; four `CERTIFICATE_*` events, since capturing supplier paperwork previously emitted nothing at all into the audit trail; a registered `document_types` list (§9.3) separate from the gating rules, because a dimensional report is a real document type that no rule requires; `unverified_id` per rule, so absent / unverified / stale stay three distinct facts; `exportAccessDecision` extracted so `EvaluateAccess` and verification decide by ONE export policy; SerialHistory now carries the certificates covering a serial (§23.4).
- **VF-026** — the first §13 scenario built under the id the specification assigns it. A correct CoC and a mill certificate from the wrong CAGE code arrive together: the CoC verifies, the MTR's verification is refused as a mismatch, it is rejected, the check blocks on `material_test_report_unverified`. One document passes and one fails in the same consignment, so it cannot pass by refusing everything.
- **Dual/observation contract:** validate:contracts ok (125 operations, 132 events, 15 state machines, 8 receiving rules); validate:schemas ok; verify:types up to date; bench smoke 2/2, first_slice 14/14, extended 7/7, receiving 5/5 both drivers; backend gate exit 0 with cross-driver diff-to-zero over 30 scenarios; vitest 196/196 (31 files); src tsc 0.
- **Rubber Duck Pass:** red captured first — requiring verification turned 12 unit tests and 3 of 4 receiving scenarios red at once, every path that released goods. VF-025 stayed green, the right discrimination. Red-capability proven by neutering the verified filter: VF-026 goes red on both drivers plus three unit tests and a coupling mutation. The expiry case changed shape and the new shape is the true one — verification refuses already-stale paperwork, so a verified-but-expired document can only come from time passing after the sign-off (B-Q-63). A vacuous battery arm was caught before it landed: the first cut of "reject then release" computed its alias arithmetically, and would have reported a refusal it never tested. Two ad-hoc cert types (`"cofc"`, `"mill_cert"`) were found in unit tests and the backend record-id proof — strings matching no registry, so those fixtures exercised a certificate that could never satisfy anything. No halts.
- **Carried, not fixed:** `VerifyCertificate` is still a read sitting next to the act that shares its name. Renaming a registered operation is a vocabulary change, so it goes to the sprint that next touches that surface.
- **Files:** contracts/{state-machines,events,operations,records,receiving-rules,scenario-assertions}.yaml, contracts/CONTRACT_GAPS.md (B-Q-62/63/64/65), src/registry/validate.ts, src/driver/{handlers,registry,projections}.ts, src/harness/{bench,run-backend}.ts, scenarios/VF-026/* (new), scenarios/VF-024|031|034 (verification act added; stale vf026_/vf027_/vf028_/vf029_/vf030_ prefixes repointed), tests/receiving/*, tests/floor/supplier-cert.test.ts, tests/consolidation/coupling.test.ts, tests/access/operation-authorization.test.ts, dev/sprints/sprint-020-supplier-document-verification.md (new).

### Sprint 019 (2026-07-31, closed) — operation authorization: the merge rule that had never been enforced
- **Scope:** first of five sprints closing the receiving boundary against its §27 acceptance criteria. Authority comes first because §9.1's sixth eligibility clause, §9.6's access invariant and criterion 9 all wait on it — a verification step with no notion of who may verify is the rubber stamp B-Q-53 declined to build.
- **The finding:** Contract Spec §6 has carried `authorization_rule` and `caller_types` on the canonical operation contract since the first slice, and §3 states "No mutating operation without authorization rule, no merge." Neither field was populated on any of the 122 operations and no validator checked. The merge rule had been inert for the entire build. The only authority check in the system was a hardcoded Set of two role strings inside one handler, on one operation.
- **Built:** `contracts/authorization-rules.yaml` (32 rules) with caller lists DERIVED in three labelled tiers — observed from the 551 scenario steps that pair 72 operations with a caller type; sibling-inherited where an unexercised operation is the same act by the same hands; `undecided_authority` (empty list, refuses everyone) where the caller is a real open question. `authorization_rule` stamped on all 122 operations; a bidirectional gate (missing rule / unresolvable rule / unregistered caller type / orphan rule / dead guard); the wrapper in `driver.ts` placed AFTER the `not_implemented` guard so an unbuilt operation still says it is unbuilt; the hardcoded Set replaced by a registered `guard: true` rule.
- **Dual/observation contract:** validate:contracts ok (13 registries, 32 authorization rules); validate:schemas ok; bench smoke 2/2, first_slice 14/14, extended 7/7, receiving 4/4 both drivers; backend gate exit 0 with cross-driver diff-to-zero over 29 scenarios; vitest 190/190 (31 files); src tsc 0; prettier clean.
- **Rubber Duck Pass:** the green here is TAUTOLOGICAL and is labelled so — allow-lists derived from the bench are satisfied by the bench by construction, so the rules are load-bearing only through their refusals. `tests/access/operation-authorization.test.ts` drives every built operation with a caller type its own rule excludes and requires a denial, with a positive control so a driver that denied everything could not pass. Both the wrapper and the registry gate were verified red-capable by neutering them. Two of my own first-cut rules were wrong and the tests found them: `GetReport` was filed as report generation (a scenario showing who DOES read is not evidence of who MAY), and `CreateGrammarGap` was filed undecided, which denied a built capability — Harness §4.1 settles it in four words, "product creates GrammarGap". `RecordDisposition` turned out to carry two authority questions rather than one, and was split. No halts.
- **Also found:** 195 test call sites were passing caller types like `"pl"`, `"eng"`, `"op"`, `"worker"` — none registered, nothing checking; 56 were rewritten. The backend outbound proof passed `"report"`, never a caller type. Ten code comments cited a `sprint-019 review` with no sprint file (practice #7) and were repointed at the persona-gap review. B-Q-58 still read "resolution pending" after the renumbering had already landed; corrected.
- **Files:** contracts/authorization-rules.yaml (new), contracts/operations.yaml, contracts/CONTRACT_GAPS.md (B-Q-59/60/61), src/registry/{load,validate}.ts, src/driver/{registry,driver,handlers}.ts, src/harness/run-backend.ts, tests/access/operation-authorization.test.ts (new) + 12 test files, dev/sprints/sprint-019-operation-authorization.md (new), README.md, WORKING_AGREEMENT.md.

### Receiving evidence boundary (2026-07-31, closed) — an outside pack worked into our own vocabulary
- **Scope:** take `receiving-evidence-registry-pack-v0.1` as input and express the inbound boundary in this project's idiom. Nothing carried over verbatim.
- **Result:** 13 proposed records -> 3 (Shipment, ShipmentLine, ReceivingCheck); 21 operations -> 5. Their SupplierDocument is our Certificate, their verification record is our Verification, their inspection is a RunCloseCheck-shaped check, their quarantine is a blocker plus existing inventory state, their corrective action is our Issue. New `contracts/receiving-rules.yaml` mirrors run-close-rules.
- **Dual/observation contract:** validate:contracts ok; validate:schemas ok; bench all 24/24 both drivers; backend gate exit 0; vitest 136/136 (26 files); src tsc 0; prettier clean.
- **Rubber Duck Pass:** pressure-testing the mapping BEFORE writing found two faults (expiry is a property of the document type, not of all documents; a first article report is scoped to a part revision, not the lot) — the pack's `scope` idea was better than ours and was adopted. The mutation battery then found a third in my own design: absent and expired had collapsed into one blocker, so suppressing the absent branch left VF-025 green. Split into distinct registered ids; the same mutation now turns VF-025 red on both drivers. No halts.
- **Files:** contracts/{modules,records,operations,events,state-machines}.yaml, contracts/receiving-rules.yaml (new), src/registry/{load,validate}.ts, src/driver/{handlers,registry}.ts, src/harness/{assertions,bench}.ts, scenarios/VF-025/* (new), tests/receiving/receiving-evidence.test.ts (new).

### Standards citations + STATE.md (2026-07-30, closed) — the authority check pointed outward for the first time
- **Scope:** verify the standards cited for the nine persona additions; record the project's state against its own documents.
- **Result:** four of eight citations wrong or unsupportable; three held; one secondary-sourced only. Column removed, not patched.
- **Dual contract:** signal (n/a — documents); artifact (validate:contracts ok; vitest 127/127 at the time).
- **Rubber Duck Pass:** governance finding, no behaviour changed. Practice #7 had been enforced only on internal B-Q ids, never on the external standards an auditor follows. No halts.
- **Files:** ADDITIONS.md, ROADMAP.md, DEVIATION_SUMMARY.md, STATE.md (new), DOCS.md, project memory.

### Required step work must have happened (2026-07-30, closed) — the two unevaluated close rules
- **Scope:** `required_measurements_present` + `required_installations_present` (Contract Spec §16) and the `CompleteRunStep` precondition (Build Readiness §7) — all registered, none implemented. Found by probe, not by reading.
- **Red captured first:** VF-001 with the measurement dropped closed the run with an empty `measurement_summary`; with the install dropped, an empty as-built. Positive control (drop a step COMPLETION) refused hard, proving the harness could show a refusal.
- **Built:** RunStep now carries its `procedure_step` (B-Q-34, restoring provenance Build Readiness already specified); InstallationEvent records its `run_step`; `CompleteRunStep` refuses unsatisfied requirements, failing closed on an unresolvable step; `RunCloseCheck` evaluates both rules over complete-or-skipped steps (B-Q-35). Waiver half deliberately unbuilt (B-Q-36).
- **Dual/observation contract:** validate:contracts ok; validate:schemas ok; bench all 23/23 both drivers; backend gate exit 0 incl. whole-bench diff-to-zero; vitest 127/127 (25 files); src tsc 0; lint 0 errors; prettier clean. New suite `tests/floor/step-requirements.test.ts` (6 tests: both refusals, the allow-once-satisfied non-blanket case, fail-closed-on-unresolvable, the skipped-step close block, and a no-over-blocking case).
- **Rubber Duck Pass:** the sweep's own result was triaged rather than assumed — 4 of the 9 arms came back "held" and were traced to the mechanism that held them (state machine / precondition / rework chain), and one arm (machine_evidence_reviewed_if_required) was declared INCONCLUSIVE because it never built the rule's precondition, rather than counted as a pass. Both new guards mutation-proven red-capable. Deliberate non-claim: the skipped-step path has no bench scenario because `SkipRunStep` is unimplemented. No halts.
- **Files:** src/driver/handlers.ts, tests/floor/step-requirements.test.ts (new), tests/floor/calibration-and-identity.test.ts (fixture re-based), contracts/CONTRACT_GAPS.md (B-Q-34/35/36), ROADMAP.md, KIT_DIARY.md.

### Demo pack + theory note (2026-07-07/08, recorded 2026-07-30) — the domain written out as plain data
- **Scope:** two artifacts authored after the readability arc and left unrecorded/uncommitted until now. `SDD_GENERAL_PROCESS.md` (theory note: what SDD renames, what is new in it, and where it stops) and `demo-packs/valve-body-assembly-v0.1/` (VF-003's valve body as plain files + a check proving it invents nothing).
- **Dual contract:** signal (n/a — docs + data, no runtime change); artifact (`check.mjs` exit 0, 72/72 names registered; all existing gates re-run unchanged at recording time: validate:contracts ok, validate:schemas ok, bench first_slice 14/14 both drivers, backend gate exit 0, vitest 121/121).
- **Result:** the no-invention rule now has a DATA-side enforcement point, not only a code-side one. Writing the part out plainly surfaced three vocabulary gaps the built system had never needed to face — recorded as B-Q-31/32/33, unbuilt.
- **Rubber Duck Pass:** the recording itself was the finding — both artifacts sat outside git and outside the ledgers, and the demo pack's three gaps were named only in its own README, not in `CONTRACT_GAPS.md` where the discipline puts them. That is phantom-close (practice #13) in its plainest form: the work was real, the record of it was not. Fixed by this entry + the three B-Q entries + DOCS/ROADMAP updates. One open weakness, deliberately surfaced not silently patched: `check.mjs` is standing outside the gate set, so nothing turns red if the pack drifts from the registries (see KIT_DIARY Entry 26).
- **Files:** SDD_GENERAL_PROCESS.md, demo-packs/valve-body-assembly-v0.1/* (18 files), contracts/CONTRACT_GAPS.md (B-Q-31/32/33), DOCS.md, ROADMAP.md, KIT_DIARY.md.

### Readability arc (2026-07-02, closed) — lint/format/type toolchain + descriptive-identifier refactor
- **Scope:** make the dense TS human-readable + stand up the linting that was entirely absent (no ESLint/Prettier, TypeScript never installed or run). Four behavior-preserving sprints (1, 2, 3a-d); commit per boundary.
- **Sprint 1 (b7b519f):** ESLint v10 flat + typescript-eslint v8 + Prettier + `tsc --noEmit`; scripts typecheck/lint/format/format:check. Prettier over src/+tests/ only (governed YAML/JSON/docs + dev/sdd-kit-2/ untouched). Format-only = PROVABLY behavior-preserving (adversarial review: prettier(HEAD)==worktree byte-for-byte; residual is associativity-neutral paren removal). Fixed a `.prettierignore` bug (bare `schemas` shadowed src/schemas/*.ts).
- **Sprint 2 (f8f95a1):** drove src `tsc` 3->0 (handlers role fail-closed type-narrow; ajv named import + FormatsPlugin cast) = the rename safety net. 82 test strict-null errors remain (tracked).
- **Sprint 3a-d:** descriptive renames, word-boundary + single-meaning-audited, verified by src tsc staying 0 (orphaned ref / redeclare = "Cannot find name") + full gates each boundary. handlers.ts 634 sites (3a, 71390bb); driver/ package incl. Rec->FactoryRecord/Evt->FactoryEvent cross-file (3b, 1a7ee5c); harness/ multi-char (3c, ca8ffa8); leaf tooling (3d, 3a442e0).
- **Dual contract (every boundary):** signal (n/a — tooling/refactor); artifact (src tsc 0; validate:contracts+schemas+compile exit 0; bench first_slice 14/14 both drivers; backend end-to-end PASS; vitest 121/121; format:check clean; generate:schemas byte-identical).
- **Rubber Duck Pass:** quick adversarial review (briefed to read the SDD kit; taken as advice) proved format-only preserving + surfaced the `.prettierignore` shadow (fixed) + advised types-before-rename ordering (adopted) + a codegen staleness gate (for the deferred Sprint 4).
- **Sprint 4 (0c2e5b8):** vocabulary union types (EventType | OperationName | RecordType, 277 members) GENERATED from contracts/*.yaml (`generate:types`) + a `verify:types` staleness gate; World.emit typed to EventType so every emitted tag is a registered event at COMPILE time (red-capability verified: a typo'd tag fails tsc with a "Did you mean" suggestion). Generated file git-tracked, prettier/eslint-ignored.
- **Sprint 5a-e (4a9a8c9, c12346b, d8c00d9, 587907a, +5e):** eliminated single-letter locals across ALL of src — the item Sprint 3 had deferred as needing a code-aware rename. Since the harness LSP tool is read-only, built a rename tool on the TypeScript compiler API (`ts.LanguageService.findRenameLocations`) = programmatic IDE "rename symbol" (per-symbol, scope-correct, ignores strings/comments). ~1000 sites: world/assertions (5a), driver package incl. backend.ts which 3b had MISSED (5b), run.ts+validators (5c), handlers.ts+build tools (5d), callback/destructuring singles (5e). Multi-meaning letters split per-scope (m=match/structureVersion/measurement; t=topRule/transition; a=actor/assertion; e=error/event); the semantic tool catches wrong-BINDING (tsc) but NOT wrong-NAMING, so two Prettier-wrapped anchors that mislabeled an actors loop + an effectivity candidate were caught by eye and fixed.
- **Deferred (NOT done, honest):** (a) idiomatic loop/index vars in the two least-read files only — run-backend.ts (CLI proof script: f=file/failure loops, `(v,i)` in `.every`) + validate.ts (s/v/p in a few overloaded gate loops); every domain-logic + orchestration file is single-letter-free; (b) 82 test strict-null tsc errors; (c) flipping no-explicit-any / prevent-abbreviations / naming-convention from warn to error; (d) extending the vocabulary types to producer (OperationName) + create (RecordType) — cascades into the string-typed op/dispatch params.

### Documentation index (2026-07-01, closed) — README front door + grouped DOCS catalog
- **Scope:** make the repo navigable and every document grouped + tracked, after both roadmap phases shipped.
- **Built:** README.md (identity + no-invention rule + status + quickstart + repo map) + DOCS.md (six-group document catalog, one-line purpose each). No file moves — the code/spec layout is mandated + code-referenced and the SDD-kit files stay at root by convention.
- **Method:** an ultracode doc-inventory workflow (5 parallel family agents + a completeness critic) produced accurate per-family summaries and independently recommended index-not-shuffle.
- **State:** all 388 files git-tracked, tree clean; no code/gate change. Committed f1bd112. Left for a decision: rename the verbose governing-docs folder + drop the redundant zip.

### Phase A (2026-07-01, closed) — outbox delivery leg (at-least-once eventing)
- **Scope:** build the outbox DELIVERY leg (TAD §12) — a consumer that drives an idempotent projection and marks rows delivered — the one place the design claimed more (at-least-once eventing) than the code delivered. Second of the two sequential roadmap phases.
- **Built:** `deliverOutbox()` — idempotent projection handler (dedup on event_seq; materialized counts incremented only on first apply), at-least-once via a two-transaction apply-then-mark split, seq ordering, orphan guard, reload durability. Backend plumbing; no registry change. Retries-with-backoff + dead-letter deferred (B-Q-30, unspecified magnitudes).
- **Dual/observation contract:** backend durability proof gated into exit (idempotency: no double count under redelivery; ordering: scrambled outbox still ascends; orphan: never mark-without-apply; reload no-op). validate:contracts ok; validate:schemas ok; bench first_slice 14/14; whole-bench diff-to-zero 23 scenarios; backend gate exit 0; vitest 121/121 (24 files).
- **Rubber Duck Pass:** a quick adversarial skeptic (per the user's standing ask) returned SOUND-BUT-NOTES and found the first cut was effectively exactly-once (apply+mark atomic — idempotency defended an unreachable path), the ordering proof vacuous, and a mark-without-apply orphan hole. ALL fixed (two-transaction split, falsifiable ordering proof, orphan fail-safe, UNIQUE event_seq, honest crash/reload labelling); both new assertions red-capability spot-checked. No halts.
- **Files:** src/driver/backend.ts (deliverOutbox + tables + test helpers), src/harness/run-backend.ts (Phase A proof), CONTRACT_GAPS.md (B-Q-30), ADDITIONS.md, ROADMAP.md.

### Phase B (2026-07-01, closed) — §18 evidence-invalidation auto-cascades
- **Scope:** the two §18 obligations deferred after VF-003D — "create run close observation if run still open" + "create quality issue if physical product may be affected" — extending InvalidateAcceptedEvidence. First of the two sequential roadmap phases.
- **Built:** RunCloseObservation + RUN_CLOSE_OBSERVATION_CREATED when the run is non-terminal; a quality Issue + ISSUE_OPENED fail-safe (accepted evidence's acceptability depended on it). No invention — both records + events pre-existed; InvalidateAcceptedEvidence registered as a co-producer of each. Idempotent, gated behind the fail-closed run-resolution guard. Underspecified "physical product may be affected" encoded fail-safe (B-Q-29).
- **Dual/observation contract:** validate:contracts ok; validate:schemas ok (regenerated, ISSUE_OPENED payload added); bench first_slice 14/14 + extended 7/7 both drivers; whole-bench diff-to-zero over 23 scenarios; backend gate exit 0; vitest 121/121 (24 files).
- **Rubber Duck Pass:** red-capability spot-checked (neutering both obligations turned the unit tests red; restored). Adversarial review surfaced the open-run path having only unit coverage -> added VF-003F (bench + cross-driver) + a coupling mutation; and the Issue-linkage being unasserted -> tightened the unit test. No halts.
- **Files:** contracts/{events,operations}.yaml, src/driver/handlers.ts, src/harness/{bench,run-backend}.ts, scenarios/VF-003D (Issue assertions), scenarios/VF-003F (new), schemas/events/ISSUE_OPENED.payload.schema.json, tests/reconciliation/evidence-invalidation-cascade.test.ts, tests/consolidation/coupling.test.ts, CONTRACT_GAPS.md, ADDITIONS.md, ROADMAP.md.

### Close-out (2026-07-01, closed) — deferred items built + line closed + registry reconciliation
- **Scope:** build everything the spec deferred (§18 reconciliation cascade + §19 report freshness; B-Q-22/27/28), then close the line — finalize the ledgers/addendums and write the deviation summary.
- **Built:** `InvalidateAcceptedEvidence` (accepted evidence -> invalidated, cascading reports to regeneration_required), `GetReport` freshness read (stale controlled_export not served fresh), the §19 two-mode contrast, temporal `world.access_policy_changes`, `operation_output_contains`. Scenario VF-003D exercises it end-to-end on both drivers.
- **Reviews:** the reconciliation build's own distrust-the-green pass found 8 fail-open holes (same family as the persona batch) — all inverted to fail-closed with tests. The CLOSE-OUT then surfaced a forward-only-validator gap: two persona-gap ops + two record types were handler-only (outside the registries); registered them + added the reverse-direction poka-yoke.
- **Dual/observation contract:** `validate:contracts` ok (116 ops / 122 events / 39 records / 13 SM / 26 assertion types); `validate:schemas` ok; bench first_slice 14/14 both drivers; whole-bench diff-to-zero over 22 scenarios PASS (byte-identical); backend gate exit 0 (all durability proofs incl. VF-003D reconciliation reload); vitest 115/115 (23 files). Open ContractGaps: none.
- **Rubber Duck Pass:** the fail-open default recurred a THIRD independent time (practice #19 confirmed as a law); the handler-only-vocabulary find added practice #20 (a correspondence poka-yoke must be bidirectional). No halts.
- **Files:** contracts/{operations,records,events,state-machines,scenario-assertions}.yaml, src/driver/{handlers,world,driver,backend}.ts, src/harness/{run,run-backend,assertions}.ts, src/compiler/compile.ts, scenarios/VF-003D (+references), tests/reconciliation/report-freshness.test.ts, tests/consolidation/handler-registration.test.ts, ADDITIONS.md, ADDENDUMS.md, DEVIATION_SUMMARY.md, CONTRACT_GAPS.md.

### Sprint 018 (2026-07-01, closed) — multi-agent adversarial review of sprints 014-017 + prototype-safety fix
- **Scope:** the deferred distrust-the-green review over VF-012 + coupling suite + engine split + harness split, run once the weekly subagent limit reset. 4 parallel critics, each refuting a dimension with real probes.
- **Result:** 2 critics (VF-012, coupling suite) could NOT refute (both confirmed by reverting real fixes -> reds); 2 critics CONVERGED on one LOW finding — prototype-unsafe map dispatch (`EVALUATORS[type]` / `HANDLERS[op]` walked the prototype chain). Fixed at all 3 sites with `Object.hasOwn`; locked by a red-capable regression test.
- **Dual/observation contract:** `vitest run` 78/78 (14 files); `bench all` 20/20 both drivers; backend 8 proofs; gates 0. Fix behavior-preserving on real inputs (registry-gated) AND red-capable (reverted guard -> test red, restored).
- **Rubber Duck Pass:** two independent adversaries converging on the same finding = high-confidence it's the only issue; the fix matches the project's own prototype-safety discipline and extended to a pre-existing more-severe handler-dispatch hole. 3 minor non-defect notes accepted. No halts.
- **Files:** src/harness/run.ts, src/driver/driver.ts, src/harness/assertions.ts, tests/consolidation/prototype-safety.test.ts (new).

### Sprint 017 (2026-07-01, closed) — readability arc 4 (COMPLETE): extract assertion evaluators from run.ts
- **Scope:** behavior-preserving extraction of the ~200-line assertion switch into a family-grouped `assertions.ts` (EVALUATORS map); run.ts -> thin orchestration. Second half of arc 4.
- **Dual/observation contract:** PURE behavior-preserving AND teeth-preserving; `bench all` 20/20 both drivers; `vitest run` 75/75 (13 files); backend 8 proofs; gates 0. run.ts 373->173 lines.
- **Rubber Duck Pass:** decoupling risk (an extracted evaluator silently losing its discrimination) checked against the direct-call discrimination tests (assertion-primitives / report-supersession / discrimination) — all still go red on bad input, so every evaluator kept its teeth. Structure realized as one sectioned module, not folder-fragmentation. No halts.
- **Files:** src/harness/assertions.ts (new), src/harness/run.ts (thinned).

### Sprint 016 (2026-07-01, closed) — readability arc 4: modular split of engine.ts
- **Scope:** behavior-preserving split of the dense ~660-line engine.ts into single-responsibility modules (registry/world/projections/handlers/driver) behind a thin re-export barrel; first half of the arc-4 readability pass.
- **Dual/observation contract:** PURE behavior-preserving (same bench/vitest outcomes) AND teeth-preserving (coupling suite still goes red through the barrel); `bench all` 20/20 both drivers; `vitest run` 75/75 (13 files); backend 8 proofs; gates 0.
- **Basis:** researched TS best practices from PRIMARY sources (Node type-stripping: `.ts` extensions, erasable-only, `import type` for pure types; tsdoc.org) + first principles; no blog authority, no third-party tooling (recorded in WORKING_AGREEMENT "Readability-refactor basis").
- **Rubber Duck Pass:** the two refactor obligations (preserve behavior + preserve ability-to-go-red) both checked against external surfaces (bench/backend proofs + coupling suite). The `import type` discipline avoided the type-stripping value-import runtime error; the barrel preserved the shared HANDLERS reference so no assertion decoupled. No halts.
- **Files:** src/driver/{registry,world,projections,handlers,driver}.ts (new), engine.ts (barrel), WORKING_AGREEMENT.md.

### Sprint 015 (2026-07-01, closed) — consolidation audit (mutation-coupling + safety-invariant regression suite)
- **Scope:** codebase-wide distrust-the-green sweep for fossil/decoupled/vacuous/fail-open greens between the per-increment reviews; make the teeth permanent. Observation pass, no product-behavior change.
- **Dual/observation contract:** mutation battery (6 headline behaviors each go red under a targeted defect) + safety-invariant probe (5 accreted fixes hold) + grep audit; `vitest run` 75/75 (13 files); `bench all` 20/20 both drivers.
- **Result:** the first distrust-the-green pass to come back EMPTY (the swept code already carries its teeth). Durable output: `tests/consolidation/coupling.test.ts` (11 tests) so a refactor that decouples an assertion goes red.
- **Rubber Duck Pass:** no fossil/decoupled/vacuous/fail-open found; empty result is meaningful because the mutation battery PROVED the greens can fail. Two initial MISSes explained (broken no-op probe + unit-test-owned coupling, both confirmed red). No halts.
- **Files:** tests/consolidation/coupling.test.ts (new; no product code changed).

### Sprint 014 (2026-07-01, closed) — report supersession after access-policy change (VF-012)
- **Scope:** the deferred VF-012 (Harness §16/§17/§24) — supersede+regenerate a controlled_export report after an access-policy change, never overwrite; plus the value-level report assertion primitive B-Q-23 named as missing.
- **Dual contract:** signal (REPORT_SUPERSEDED once; prior frozen `superseded`, new `generated`); artifact (`bench all` 20/20 both drivers; vitest 64; gates 0; backend 8 durability proofs).
- **Observation:** bench all 20/20; the frozen-snapshot pair (t0=customer_summary_access != t1=customer_extended_access) is value-level asserted and survives a cold reload; a superseded report cannot be superseded again (`state_transition_forbidden`).
- **Rubber Duck Pass:** SupersedeReport was registered-but-unhandled + access_policy_snapshot was a hardcode (false certainty) — both resolved-here (filled; derived from bound scope). Review ran INLINE (weekly subagent limit) and found two real items: phantom B-Q citations (A: wrote B-Q-23(c)/27/28) + no reload proof (B: 8th backend durability proof). B-Q-23(c) RESOLVED; B-Q-27/28 recorded. No halts.
- **Files:** engine.ts, run.ts, run-backend.ts, bench.ts, scenario-assertions.yaml, scenarios/VF-012 (+references), tests/report/report-supersession.test.ts, CONTRACT_GAPS.md.

### Sprint 013 (2026-07-01, closed) — HARDENING: emit poka-yoke + write-boundary idempotency
- **Scope:** close B-Q-16 (runtime emit vocabulary enforcement, SDD technique #2) + B-Q-13 (write-boundary idempotency surviving cold reload). No new product scenario.
- **Dual contract:** signal (emit rejects out-of-vocab; write-boundary conflicts); artifact (`bench all` 19/19 both drivers; vitest 60; gates 0; backend 7 durability proofs).
- **Observation:** all 18 VF unchanged under runtime enforcement (no drift); write-boundary idempotency op-scoped, success-only, cold-reload-proven, locked by IDEM-001.
- **Rubber Duck Pass:** poka-yoke landed clean (handlers already consistent). Review (wkxxaohrk, 8 confirmed incl. 1 major) found un-op-scoped keys (cross-op collision) + failure-poisoning memo + no bench lock — all fixed (op-scoped keys, success-only, IDEM-001). B-Q-13/16 RESOLVED. No halts.
- **Files:** engine.ts, backend.ts, run-backend.ts, run.ts, bench.ts, scenarios/IDEM-001 (+references), tests/hardening/*.test.ts, CONTRACT_GAPS.md.

### Sprint 012 (2026-07-01, closed) — GrammarGap escalation (VF-015); extended arc buildable set COMPLETE
- **Scope:** unsupported machine payload creates a GrammarGap (Harness §21/§24) — the executor rule as a product feature.
- **Dual contract:** signal (escalate un-normalizable / normalize well-formed); artifact (`bench all` 18/18 both drivers; vitest 49; gates 0; backend 6 durability proofs).
- **Observation:** bench all 18/18; trigger discriminates unknown/missing/invalid/prototype/duplicate (all tested); gap survives cold reload; no false certainty.
- **Rubber Duck Pass:** VF-015 red = pre-fix false certainty. Review (w503b13xs, 4 confirmed incl. 2 major) found a present-but-null/wrong-type field still normalized (fixed: type-aware validity) + a prototype-name payload_type CRASHED the normalizer (fixed: Object.hasOwn) + duplicate-gap on re-normalize (guard) + no reload proof (added). B-Q-26; B-Q-24 resolved. No halts.
- **Files:** engine.ts, events.yaml, operations.yaml, run.ts, run-backend.ts, bench.ts, scenarios/VF-015 (+references), tests/grammar-gap/grammar-gap.test.ts, CONTRACT_GAPS.md.

### Sprint 011 (2026-07-01, closed) — extended adversarial (VF-011/013/014)
- **Scope:** duplicate-payload idempotency (VF-011), redline rejected cannot be applied (VF-013), bounded drill-down audit+filter (VF-014). Harness §24.
- **Dual contract:** signal (dedup / reject-refused / drill-down audit); artifact (`bench all` 17/17 both drivers; vitest 42; gates 0; backend 5 durability proofs).
- **Observation:** bench all 17/17; VF-013 branch discrimination (reject->refused, approve->applies) + rejected-redline survives cold reload; memo scoped to required_idempotency_key.
- **Rubber Duck Pass:** VF-013 surfaced a real safety bug (reject force-approved -> applied). Review (wumje2dyj, 7 confirmed incl. 2 major) caught the fix's coercion hazard (guard added), a cold-reload alias-transition bug (fixed + durability proof), and the memo-scoping fix exposing a sprint-007 test on the over-broad memo (re-based). B-Q-22..25. No halts.
- **Files:** engine.ts, backend.ts, run-backend.ts, bench.ts, scenarios/VF-011|013|014 (+references), tests/extended/extended.test.ts, tests/harness/assertion-primitives.test.ts, CONTRACT_GAPS.md.

### Sprint 010 (2026-07-01, closed) — access + report; FIRST-SLICE BENCH COMPLETE (VF-009/010)
- **Scope:** access-filtered serial history (VF-009); run close blocked by missing report definition (VF-010). Completes VF-001..010 (Harness §22).
- **Dual contract:** signal (role-relative serial history full|summary|denied; report_definition_available block); artifact (`bench first_slice` 14/14 both drivers; vitest 38; gates 0; backend 4 durability proofs incl. VF-009 access-dimension reload).
- **Observation:** first_slice 14/14 both drivers; SAME serial reads differently by role (full sees controlled, summary strips it, unresolvable fails closed) + survives reload; report-definition block isolated from the quality path.
- **Rubber Duck Pass:** SDD loop surfaced the whole access-read path + the report rule unexercised. Review (wqkz3n1m1, 7 confirmed incl. 2 SECURITY) caught serialHistory fail-OPEN on unresolvable profile + backend reload dropping access policies — BOTH fixed to fail closed + world-config persistence + VF-009 reload proof. B-Q-20/21. No halts.
- **Files:** engine.ts, backend.ts, run.ts, run-backend.ts, compile.ts, bench.ts, scenarios/VF-009|010 (+references), tests/access/serial-history-access.test.ts, CONTRACT_GAPS.md.

### Sprint 009 (2026-06-30, closed) — effectivity family (VF-007/008)
- **Scope:** ambiguous effectivity blocks the run (VF-007); effectivity snapshot survives a later rule change (VF-008). Harness §19/§22.
- **Dual contract:** signal (EFFECTIVITY_AMBIGUOUS + effectivity_ambiguous blocker + RUN_BLOCKED; twice-resolved snapshot); artifact (`bench effectivity` + `first_slice` 12/12 both drivers; vitest 33; gates 0; backend proves VF-003 closed + VF-006 blocked + VF-008 snapshot durability).
- **Observation:** first_slice 12/12 both drivers; ambiguity is a produced outcome (not a throw), discrimination-tested vs no-match; snapshot provenance proven (captures the resolution, not the input) + survives reload.
- **Rubber Duck Pass:** SDD loop surfaced EFFECTIVITY_AMBIGUOUS unexercised (engine threw). Review (wq5s8d22e, 7 confirmed) caught VF-008 immutability VACUOUS (snapshot echoed the CreateRun input literal) — fixed at root (snapshot from resolution) + provenance test; no-match precedence (B-Q-18), flat fields (B-Q-17), blocker strings (B-Q-19), false VF-006 cross-ref. No halts.
- **Files:** engine.ts, run-backend.ts, bench.ts, scenarios/VF-007|008, scenarios/VF-007/references.yaml, tests/effectivity/effectivity.test.ts, CONTRACT_GAPS.md.

### Sprint 008 (2026-06-30, closed) — build-check-blocker family (VF-004/005/006)
- **Scope:** wrong / quarantined / missing child inventory blocking the build check (Harness §22); the BLOCKED path + `planned -> blocked` run transition, unexercised by VF-003.
- **Dual contract:** signal (distinct blocker spines: wrong_part / quarantined_inventory / missing_bom_inventory + RUN_BLOCKED); artifact (`bench build_check` + `first_slice` 10/10 both drivers; vitest 27; gates 0; backend proves VF-003 closed + VF-006 blocked durability).
- **Observation:** first_slice 10/10 both drivers; three DISTINCT blockers discrimination-tested; red captured before green (VF-004 48/49, VF-005 46/50 pre-fix).
- **Rubber Duck Pass:** SDD loop surfaced QuarantineInventory registered-but-unimplemented (filled) + collapsed blocker label (B-Q-14). Review (w5dmishnx, 9 confirmed) caught the first-cut wrong_part heuristic WORLD-GLOBAL (missing mislabeled wrong; cross-attribution) — fixed by part-identity scoping; + phantom B-Q-14, cosmetic schema gate, VF-003-only durability — all resolved. B-Q-14/15/16. No halts.
- **Files:** engine.ts, run.ts, generate.ts, validate-schemas.ts, run-backend.ts, bench.ts, scenarios/VF-004|005|006, scenarios/VF-006/references.yaml, tests/build-check/build-check.test.ts, CONTRACT_GAPS.md.

### Sprint 007 (2026-06-30, closed) — SDD test primitives + evidence variants
- **Scope:** apply the missing SDD testing techniques (payload/sequence/idempotency assertions) + machine-evidence variants VF-003A/B/C + negative VF-003E.
- **Dual contract:** signal (distinct evidence spines + forbidden refusal); artifact (`bench first_slice` 7/7 both drivers; vitest 18; gates 0).
- **Observation:** first_slice bench 7/7 on both drivers; SDD primitives discrimination-tested (wrong payload/order/cold-key-duplicate all fail).
- **Rubber Duck Pass:** SDD loop surfaced unimplemented disposition handlers (filled §10.2/§7.6). Review found idempotent_replay tautology (blocker) + 9 more — all resolved-here (honest idempotency scope + negative test, filtered-exact sequence, deleted ambiguous transition, VF-003E forbidden test, after_step no-overwrite, validator forbidden-check). B-Q-13 recorded. No halts.
- **Files:** run.ts, engine.ts, validate.ts, scenario-assertions.yaml, state-machines.yaml, scenarios/VF-003A|B|C|E, tests/harness/assertion-primitives.test.ts, bench.ts.
- **Closed:** clean.

### Sprint 006 (2026-06-30, closed) — smoke bench green on both drivers
- **Scope:** VF-001 (happy path) + VF-002 (failed measurement → NC → refuse-to-close) + bench runner (both drivers).
- **Dual contract:** signal (distinct clean vs failure traces); artifact (`bench:smoke`/`bench` exit 0; vitest 14/14; gates 0). Observation: smoke + first_slice benches PASS on both drivers (VF-001 95/95, VF-002 93/93, VF-003 156/156).
- **Rubber Duck Pass:** review found vacuous discriminators — VF-002 hardened to attempt close + prove RUN_CLOSE_CHECK_BLOCKED; NC↔failed-measurement linkage asserted; VF-001 projection_not_contains absence check. Added `projection_not_contains` + alias resolution. No halts.
- **Files:** scenarios/VF-00{1,2}/scenario.yaml, src/harness/bench.ts, tests/bench/smoke.test.ts, run.ts, scenario-assertions.yaml.
- **Closed:** clean. Bench: 3/3 on both drivers at required_pass_rate 1.0.

### Sprint 005 (2026-06-30, closed) — FIRST SLICE COMPLETE (both drivers)
- **Scope:** Persistent backend ProductDriver (node:sqlite) passing the identical VF-003; prove durability from disk.
- **Dual contract:** signal pass (identical 70-event spine on backend); artifact pass (`test:vf003:backend` exit 0; in-memory 156/156 unchanged; vitest 10/10).
- **Observation:** 156/156 on backend; 96/96 persisted-state assertions re-pass on a fresh disk-loaded instance; checkpoint replayed from the event log (047=close_blocked); append-only events + outbox parity.
- **Rubber Duck Pass:** vite/node:sqlite blocker → node CLI gate. Adversarial review found overstated durability (blocker) + no-rollback bug → resolved-here (evaluateDurable scoping, rebuildCheckpointsFromEvents, per-op rollback + fault-injection test); outbox downgraded to writer-only (B-Q-11). No halts.
- **Files:** src/driver/backend.ts, src/harness/{run,run-backend}.ts, tests/vf-003/vf003.backend.test.ts, vitest.config.ts, engine.ts (rollback).
- **Closed:** clean. doc-08 Phases 1–10 complete; VF-003 green on in-memory AND backend.

### Sprint 004 (2026-06-30, closed) — FIRST SUCCESS CONDITION MET
- **Scope:** In-memory ProductDriver + 47 handlers + assertion engine; drive VF-003 end-to-end to `ScenarioResult.status == passed`.
- **Dual contract:** signal pass (full VF-003 event trace in order); artifact pass (`run:VF-003` exit 0; `test:vf003:memory` exit 0, 9 tests). Observation pass (156/156 assertions; discrimination CI-enforced).
- **Rubber Duck Pass:** first run 133/152 → 5 missing rework handlers (resolved-here); import-time `process.exit` bug in compile.ts (guarded); adversarial review found fake-green in SerialHistory/RunBuildCheck/BoundedDrillDown/evidence-guard/§15-invariants (all resolved-here + locked by discrimination.test.ts). No halts.
- **Files:** src/driver/engine.ts, src/harness/run.ts, tests/vf-003/{vf003,discrimination}.test.ts.
- **Closed:** clean. doc-08 first success condition achieved.

### Sprint 003 (2026-06-30, closed)
- **Scope:** VF-003 scenario package (58 steps) + scenario compiler.
- **Dual contract:** signal pass (narrated); artifact pass (`compile:scenario VF-003` exit 0 / 152 assertions; NEG-001 exit 1 / 2 ContractGaps).
- **Observation:** compiler resolves all references + the highlighted dependency table; fails closed on the negative fixture.
- **Rubber Duck Pass:** deviation (inline inputs vs 58 separate files) recorded; no halts.
- **Files:** scenarios/VF-003/scenario.yaml, src/compiler/compile.ts, scenarios/NEG-001/scenario.yaml.
- **Closed:** clean.

### Sprint 002 (2026-06-30, closed)
- **Scope:** Generate the VF-003 JSON Schema surface + the `validate:schemas` gate (doc 08 Phase 2), schemas derived from the registries (Build Readiness §8.1).
- **Dual contract:** signal pass (meta-trace narrated); artifact pass (`generate:schemas` exit 0 → 94 op + 58 event + 1 report; `validate:schemas` exit 0 → all compile, 14/14 fixtures discriminate; `validate:contracts` still exit 0).
- **Observation contract:** pass — quick 2-critic review (w9s39rz1o) surfaced 5 findings (3 major, 2 minor); all applied + re-verified.
- **Rubber Duck Pass:** ajv `$id` double-registration caught by the gate on itself, fixed via memoization. Review fixes: F1 output required → Harness §11 three fields (reversed the critic's §4.3-anchored suggestion, higher authority wins); F2 idempotency_key minLength verbatim (CaptureMeasurement only); F3 gate fail-closed on empty inputs; F4 output-schema fixtures added; F5 blank-ref guard. B-Q-8/B-Q-9 recorded. No halts.
- **Files:** src/schemas/{generate,validate-schemas}.ts, schemas/operations/* (94), schemas/events/* (58), schemas/reports/RunCloseReport.schema.json, tests/fixtures/schema-fixtures.json.
- **Closed:** clean.

### Sprint 001 (2026-06-30, closed)
- **Scope:** Founding act — extract the 11 contract registries from Contract Spec v0.4.1 + build the static validator, scoped to the first-slice + VF-003 reference set, resolving the surfaced tensions as recorded decisions.
- **Dual contract:** signal pass (vacuous-runtime; meta-trace narrated in the Signal Report); artifact pass (`npm run validate:contracts` exit 0 — consistency ok, VF-003 references resolved; 21 modules / 37 records / 113 ops / 121 events / 13 state machines / 5 projections / 1 report / 13 rules).
- **Observation contract:** pass — validator reports 0 unregistered VF-003 references and full dependency-table resolution; 6-critic adversarial verification (workflow w4fsz14rx) returned 5/6 dimensions clean, 1 minor finding resolved-here.
- **Rubber Duck Pass:** B-Q-7 (BUILD_CHECK_FAILED vs BLOCKED) discovered + resolved-here to TAD canonical; SerialHistory rework-name finding resolved-here (B-Q-3, non-dangling); deferred-events scope deferred with re-visit condition. No halts.
- **Files:** contracts/*.yaml (11), scenarios/VF-003/references.yaml, src/registry/{load,validate}.ts, contracts/CONTRACT_GAPS.md, package.json, tsconfig.json, .gitignore.
- **Closed:** clean.

---

*BLACKBOARD.md for the Distributed Factory Execution Record System. Stack TS/Node. Built and adversarially reviewed: the first-slice bench, the extended adversarial arc, the consolidation audit + readability refactor, the nine persona additions, the deferred-items build (§18 reconciliation + §19 report freshness), and BOTH roadmap phases — Phase B (§18 evidence-invalidation auto-cascades, B-Q-29) and Phase A (outbox delivery leg, B-Q-30). A README front door + grouped DOCS index now make the repo navigable. Current green: validate:contracts ok (116 ops / 122 events / 39 records / 13 state machines / 26 assertion types); bench first_slice 14/14 both drivers + whole-bench diff-to-zero over 23 scenarios; backend gate exit 0; vitest 121/121 (24 files); 0 open ContractGaps. Contract registries are the locked vocabulary; nothing is invented, and every handler is accounted for in it.*
